/* @ds-bundle: {"format":4,"namespace":"UnilifeDesignSystem_329c9a","components":[{"name":"Icon","sourcePath":"assets/icons/Icon.jsx"},{"name":"AspectRatio","sourcePath":"components/data-display/AspectRatio.jsx"},{"name":"Avatar","sourcePath":"components/data-display/Avatar.jsx"},{"name":"Card","sourcePath":"components/data-display/Card.jsx"},{"name":"InlineCode","sourcePath":"components/data-display/InlineCode.jsx"},{"name":"Palette","sourcePath":"components/data-display/Palette.jsx"},{"name":"Poster","sourcePath":"components/data-display/Poster.jsx"},{"name":"Progress","sourcePath":"components/data-display/Progress.jsx"},{"name":"ScrollArea","sourcePath":"components/data-display/ScrollArea.jsx"},{"name":"ScrollListItem","sourcePath":"components/data-display/ScrollListItem.jsx"},{"name":"Separator","sourcePath":"components/data-display/Separator.jsx"},{"name":"Slider","sourcePath":"components/data-display/Slider.jsx"},{"name":"TabCard","sourcePath":"components/data-display/TabCard.jsx"},{"name":"Table","sourcePath":"components/data-display/Table.jsx"},{"name":"TableItem","sourcePath":"components/data-display/TableItem.jsx"},{"name":"Button","sourcePath":"components/forms/Button.jsx"},{"name":"Checkbox","sourcePath":"components/forms/Checkbox.jsx"},{"name":"Input","sourcePath":"components/forms/Input.jsx"},{"name":"Label","sourcePath":"components/forms/Label.jsx"},{"name":"RadioButton","sourcePath":"components/forms/RadioButton.jsx"},{"name":"RadioGroup","sourcePath":"components/forms/RadioGroup.jsx"},{"name":"Select","sourcePath":"components/forms/Select.jsx"},{"name":"Switch","sourcePath":"components/forms/Switch.jsx"},{"name":"Textarea","sourcePath":"components/forms/Textarea.jsx"},{"name":"Accordion","sourcePath":"components/navigation/Accordion.jsx"},{"name":"AccordionItem","sourcePath":"components/navigation/AccordionItem.jsx"},{"name":"Collapsible","sourcePath":"components/navigation/Collapsible.jsx"},{"name":"Command","sourcePath":"components/navigation/Command.jsx"},{"name":"ContextMenu","sourcePath":"components/navigation/ContextMenu.jsx"},{"name":"DropdownMenu","sourcePath":"components/navigation/DropdownMenu.jsx"},{"name":"MenuItem","sourcePath":"components/navigation/MenuItem.jsx"},{"name":"MenuSectionTitle","sourcePath":"components/navigation/MenuSectionTitle.jsx"},{"name":"Menubar","sourcePath":"components/navigation/Menubar.jsx"},{"name":"MenubarItem","sourcePath":"components/navigation/MenubarItem.jsx"},{"name":"NavigationMenu","sourcePath":"components/navigation/NavigationMenu.jsx"},{"name":"NavigationMenuContent","sourcePath":"components/navigation/NavigationMenuContent.jsx"},{"name":"NavigationMenuContentItem","sourcePath":"components/navigation/NavigationMenuContentItem.jsx"},{"name":"NavigationMenuItem","sourcePath":"components/navigation/NavigationMenuItem.jsx"},{"name":"SectionItems","sourcePath":"components/navigation/SectionItems.jsx"},{"name":"TabItem","sourcePath":"components/navigation/TabItem.jsx"},{"name":"Tabs","sourcePath":"components/navigation/Tabs.jsx"},{"name":"AlertDialog","sourcePath":"components/overlay/AlertDialog.jsx"},{"name":"AlterDialog","sourcePath":"components/overlay/AlterDialog.jsx"},{"name":"Dialog","sourcePath":"components/overlay/Dialog.jsx"},{"name":"HoverCard","sourcePath":"components/overlay/HoverCard.jsx"},{"name":"Popover","sourcePath":"components/overlay/Popover.jsx"},{"name":"Tooltip","sourcePath":"components/overlay/Tooltip.jsx"}],"sourceHashes":{"assets/icons/Icon.jsx":"b81dfc2559d3","assets/icons/icon-data.js":"6da4dddb3795","components/data-display/AspectRatio.jsx":"6ebd07afe1ee","components/data-display/Avatar.jsx":"311281dac9c3","components/data-display/Card.jsx":"383bddb5090c","components/data-display/InlineCode.jsx":"1b753158cc45","components/data-display/Palette.jsx":"137d90d31280","components/data-display/Poster.jsx":"8543fa53fb8b","components/data-display/Progress.jsx":"faa83474b05d","components/data-display/ScrollArea.jsx":"ca707f919461","components/data-display/ScrollListItem.jsx":"5b24a0ee1684","components/data-display/Separator.jsx":"a8ba18e9106c","components/data-display/Slider.jsx":"90f1e7eff28d","components/data-display/TabCard.jsx":"17ceb672e755","components/data-display/Table.jsx":"a462d1de7657","components/data-display/TableItem.jsx":"3e2e313ffdd1","components/forms/Button.jsx":"ed17e5bdc14b","components/forms/Checkbox.jsx":"3ccaea4633a2","components/forms/Input.jsx":"0d3e412b43e0","components/forms/Label.jsx":"7b373d4fd9bf","components/forms/RadioButton.jsx":"773d3a6ed5cd","components/forms/RadioGroup.jsx":"c41855412e01","components/forms/Select.jsx":"d27655fffd80","components/forms/Switch.jsx":"442801352611","components/forms/Textarea.jsx":"33f9982e42db","components/navigation/Accordion.jsx":"6f0dce6b127f","components/navigation/AccordionItem.jsx":"9236ff34c707","components/navigation/Collapsible.jsx":"711be6cf201b","components/navigation/Command.jsx":"d69ba2520129","components/navigation/ContextMenu.jsx":"31f5a53d4dc3","components/navigation/DropdownMenu.jsx":"f38454499f6d","components/navigation/MenuItem.jsx":"87284ad5b728","components/navigation/MenuSectionTitle.jsx":"c182d3a6e118","components/navigation/Menubar.jsx":"5066fe84600b","components/navigation/MenubarItem.jsx":"8b8b2e35bd94","components/navigation/NavigationMenu.jsx":"3243be05fe60","components/navigation/NavigationMenuContent.jsx":"3bfda822abfd","components/navigation/NavigationMenuContentItem.jsx":"62b507119330","components/navigation/NavigationMenuItem.jsx":"4b96379d4980","components/navigation/SectionItems.jsx":"a19ffdf8b907","components/navigation/TabItem.jsx":"da95847854df","components/navigation/Tabs.jsx":"aed978c42f09","components/overlay/AlertDialog.jsx":"5bc90c1b5403","components/overlay/AlterDialog.jsx":"f3e5bbe7127d","components/overlay/Dialog.jsx":"74d505865a9b","components/overlay/HoverCard.jsx":"477ce047469c","components/overlay/Popover.jsx":"cc14466244a1","components/overlay/Tooltip.jsx":"87bc46875d81","ui_kits/website/Header.jsx":"2df7d55e7798","ui_kits/website/Sections.jsx":"49cf99f379eb","ui_kits/website/image-slot.js":"fff26d081c8d"},"inlinedExternals":[],"unexposedExports":[]} */

(() => {

const __ds_ns = (window.UnilifeDesignSystem_329c9a = window.UnilifeDesignSystem_329c9a || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// assets/icons/icon-data.js
try { (() => {
// Generated by fig_materialize (moduleFormat: 'icon-data') — 39 icon(s)
// as { viewBox, body } SVG-markup entries. Render via the sibling Icon.jsx
// (<Icon name="IconArrowRight" />), or consume the path data directly.
let __ds_default_assets_icons_icon_data_imae2q;
try {
  __ds_default_assets_icons_icon_data_imae2q = {
    "IconArrowRight": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 14 1 C 14.552 1 15 0.552 15 0 C 15 -0.552 14.552 -1 14 -1 L 14 1 Z M 0 1 L 14 1 L 14 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 5 12)\"/><path d=\"M 0.707 -0.707 C 0.317 -1.098 -0.317 -1.098 -0.707 -0.707 C -1.098 -0.317 -1.098 0.317 -0.707 0.707 L 0.707 -0.707 Z M 7 7 L 7.707 7.707 C 8.098 7.317 8.098 6.683 7.707 6.293 L 7 7 Z M -0.707 13.293 C -1.098 13.683 -1.098 14.317 -0.707 14.707 C -0.317 15.098 0.317 15.098 0.707 14.707 L -0.707 13.293 Z M -0.707 0.707 L 6.293 7.707 L 7.707 6.293 L 0.707 -0.707 L -0.707 0.707 Z M 6.293 6.293 L -0.707 13.293 L 0.707 14.707 L 7.707 7.707 L 6.293 6.293 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 12 5)\"/>"
    },
    "IconArrowUpRight": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M -0.707 9.293 C -1.098 9.683 -1.098 10.317 -0.707 10.707 C -0.317 11.098 0.317 11.098 0.707 10.707 L -0.707 9.293 Z M 10.707 0.707 C 11.098 0.317 11.098 -0.317 10.707 -0.707 C 10.317 -1.098 9.683 -1.098 9.293 -0.707 L 10.707 0.707 Z M 0.707 10.707 L 10.707 0.707 L 9.293 -0.707 L -0.707 9.293 L 0.707 10.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 7)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 10 0 L 11 0 C 11 -0.552 10.552 -1 10 -1 L 10 0 Z M 9 10 C 9 10.552 9.448 11 10 11 C 10.552 11 11 10.552 11 10 L 9 10 Z M 0 1 L 10 1 L 10 -1 L 0 -1 L 0 1 Z M 9 0 L 9 10 L 11 10 L 11 0 L 9 0 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 7)\"/>"
    },
    "IconAward": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 11 6 C 11 8.761 8.761 11 6 11 L 6 13 C 9.866 13 13 9.866 13 6 L 11 6 Z M 6 11 C 3.239 11 1 8.761 1 6 L -1 6 C -1 9.866 2.134 13 6 13 L 6 11 Z M 1 6 C 1 3.239 3.239 1 6 1 L 6 -1 C 2.134 -1 -1 2.134 -1 6 L 1 6 Z M 6 1 C 8.761 1 11 3.239 11 6 L 13 6 C 13 2.134 9.866 -1 6 -1 L 6 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 6 2)\"/><path d=\"M 9.463 -0.165 C 9.372 -0.71 8.857 -1.077 8.312 -0.986 C 7.767 -0.895 7.4 -0.38 7.491 0.165 L 9.463 -0.165 Z M 10 9.11 L 9.486 9.967 C 9.82 10.168 10.241 10.156 10.563 9.936 C 10.885 9.717 11.051 9.33 10.986 8.945 L 10 9.11 Z M 5 6.11 L 5.514 5.253 C 5.198 5.062 4.802 5.062 4.486 5.253 L 5 6.11 Z M 0 9.11 L -0.986 8.945 C -1.051 9.33 -0.885 9.717 -0.563 9.936 C -0.241 10.156 0.18 10.168 0.514 9.967 L 0 9.11 Z M 2.509 0.165 C 2.6 -0.38 2.233 -0.895 1.688 -0.986 C 1.143 -1.077 0.628 -0.71 0.537 -0.165 L 2.509 0.165 Z M 7.491 0.165 L 9.014 9.275 L 10.986 8.945 L 9.463 -0.165 L 7.491 0.165 Z M 10.514 8.253 L 5.514 5.253 L 4.486 6.967 L 9.486 9.967 L 10.514 8.253 Z M 4.486 5.253 L -0.514 8.253 L 0.514 9.967 L 5.514 6.967 L 4.486 5.253 Z M 0.986 9.275 L 2.509 0.165 L 0.537 -0.165 L -0.986 8.945 L 0.986 9.275 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 12.890)\"/>"
    },
    "IconCheck": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 16.707 0.707 C 17.098 0.317 17.098 -0.317 16.707 -0.707 C 16.317 -1.098 15.683 -1.098 15.293 -0.707 L 16.707 0.707 Z M 5 11 L 4.293 11.707 C 4.683 12.098 5.317 12.098 5.707 11.707 L 5 11 Z M 0.707 5.293 C 0.317 4.902 -0.317 4.902 -0.707 5.293 C -1.098 5.683 -1.098 6.317 -0.707 6.707 L 0.707 5.293 Z M 15.293 -0.707 L 4.293 10.293 L 5.707 11.707 L 16.707 0.707 L 15.293 -0.707 Z M 5.707 10.293 L 0.707 5.293 L -0.707 6.707 L 4.293 11.707 L 5.707 10.293 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 4 6)\"/>"
    },
    "IconChevronDown": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 0.707 -0.707 C 0.317 -1.098 -0.317 -1.098 -0.707 -0.707 C -1.098 -0.317 -1.098 0.317 -0.707 0.707 L 0.707 -0.707 Z M 6 6 L 5.293 6.707 C 5.683 7.098 6.317 7.098 6.707 6.707 L 6 6 Z M 12.707 0.707 C 13.098 0.317 13.098 -0.317 12.707 -0.707 C 12.317 -1.098 11.683 -1.098 11.293 -0.707 L 12.707 0.707 Z M -0.707 0.707 L 5.293 6.707 L 6.707 5.293 L 0.707 -0.707 L -0.707 0.707 Z M 6.707 6.707 L 12.707 0.707 L 11.293 -0.707 L 5.293 5.293 L 6.707 6.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 6 9)\"/>"
    },
    "IconChevronLeft": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 5.293 12.707 C 5.683 13.098 6.317 13.098 6.707 12.707 C 7.098 12.317 7.098 11.683 6.707 11.293 L 5.293 12.707 Z M 0 6 L -0.707 5.293 C -1.098 5.683 -1.098 6.317 -0.707 6.707 L 0 6 Z M 6.707 0.707 C 7.098 0.317 7.098 -0.317 6.707 -0.707 C 6.317 -1.098 5.683 -1.098 5.293 -0.707 L 6.707 0.707 Z M 6.707 11.293 L 0.707 5.293 L -0.707 6.707 L 5.293 12.707 L 6.707 11.293 Z M 0.707 6.707 L 6.707 0.707 L 5.293 -0.707 L -0.707 5.293 L 0.707 6.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 9 6)\"/>"
    },
    "IconChevronRight": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M -0.707 11.293 C -1.098 11.683 -1.098 12.317 -0.707 12.707 C -0.317 13.098 0.317 13.098 0.707 12.707 L -0.707 11.293 Z M 6 6 L 6.707 6.707 C 7.098 6.317 7.098 5.683 6.707 5.293 L 6 6 Z M 0.707 -0.707 C 0.317 -1.098 -0.317 -1.098 -0.707 -0.707 C -1.098 -0.317 -1.098 0.317 -0.707 0.707 L 0.707 -0.707 Z M 0.707 12.707 L 6.707 6.707 L 5.293 5.293 L -0.707 11.293 L 0.707 12.707 Z M 6.707 5.293 L 0.707 -0.707 L -0.707 0.707 L 5.293 6.707 L 6.707 5.293 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 9 6)\"/>"
    },
    "IconChevronUp": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 11.293 6.707 C 11.683 7.098 12.317 7.098 12.707 6.707 C 13.098 6.317 13.098 5.683 12.707 5.293 L 11.293 6.707 Z M 6 0 L 6.707 -0.707 C 6.317 -1.098 5.683 -1.098 5.293 -0.707 L 6 0 Z M -0.707 5.293 C -1.098 5.683 -1.098 6.317 -0.707 6.707 C -0.317 7.098 0.317 7.098 0.707 6.707 L -0.707 5.293 Z M 12.707 5.293 L 6.707 -0.707 L 5.293 0.707 L 11.293 6.707 L 12.707 5.293 Z M 5.293 -0.707 L -0.707 5.293 L 0.707 6.707 L 6.707 0.707 L 5.293 -0.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 6 9)\"/>"
    },
    "IconChevronsUpDown": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 0.707 -0.707 C 0.317 -1.098 -0.317 -1.098 -0.707 -0.707 C -1.098 -0.317 -1.098 0.317 -0.707 0.707 L 0.707 -0.707 Z M 5 5 L 4.293 5.707 C 4.683 6.098 5.317 6.098 5.707 5.707 L 5 5 Z M 10.707 0.707 C 11.098 0.317 11.098 -0.317 10.707 -0.707 C 10.317 -1.098 9.683 -1.098 9.293 -0.707 L 10.707 0.707 Z M -0.707 0.707 L 4.293 5.707 L 5.707 4.293 L 0.707 -0.707 L -0.707 0.707 Z M 5.707 5.707 L 10.707 0.707 L 9.293 -0.707 L 4.293 4.293 L 5.707 5.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 15)\"/><path d=\"M -0.707 4.293 C -1.098 4.683 -1.098 5.317 -0.707 5.707 C -0.317 6.098 0.317 6.098 0.707 5.707 L -0.707 4.293 Z M 5 0 L 5.707 -0.707 C 5.317 -1.098 4.683 -1.098 4.293 -0.707 L 5 0 Z M 9.293 5.707 C 9.683 6.098 10.317 6.098 10.707 5.707 C 11.098 5.317 11.098 4.683 10.707 4.293 L 9.293 5.707 Z M 0.707 5.707 L 5.707 0.707 L 4.293 -0.707 L -0.707 4.293 L 0.707 5.707 Z M 4.293 0.707 L 9.293 5.707 L 10.707 4.293 L 5.707 -0.707 L 4.293 0.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 4)\"/>"
    },
    "IconCircle": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 19 10 C 19 14.971 14.971 19 10 19 L 10 21 C 16.075 21 21 16.075 21 10 L 19 10 Z M 10 19 C 5.029 19 1 14.971 1 10 L -1 10 C -1 16.075 3.925 21 10 21 L 10 19 Z M 1 10 C 1 5.029 5.029 1 10 1 L 10 -1 C 3.925 -1 -1 3.925 -1 10 L 1 10 Z M 10 1 C 14.971 1 19 5.029 19 10 L 21 10 C 21 3.925 16.075 -1 10 -1 L 10 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 2)\"/>"
    },
    "IconCircleDot": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 19 10 C 19 14.971 14.971 19 10 19 L 10 21 C 16.075 21 21 16.075 21 10 L 19 10 Z M 10 19 C 5.029 19 1 14.971 1 10 L -1 10 C -1 16.075 3.925 21 10 21 L 10 19 Z M 1 10 C 1 5.029 5.029 1 10 1 L 10 -1 C 3.925 -1 -1 3.925 -1 10 L 1 10 Z M 10 1 C 14.971 1 19 5.029 19 10 L 21 10 C 21 3.925 16.075 -1 10 -1 L 10 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 2)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 11 11)\"/>"
    },
    "IconDroplet": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 7 0 L 7.981 -0.196 C 7.887 -0.664 7.477 -1 7 -1 C 6.523 -1 6.113 -0.664 6.019 -0.196 L 7 0 Z M 7 20 C 9.122 20 11.157 19.157 12.657 17.657 L 11.243 16.243 C 10.117 17.368 8.591 18 7 18 L 7 20 Z M 12.657 17.657 C 14.157 16.157 15 14.122 15 12 L 13 12 C 13 13.591 12.368 15.117 11.243 16.243 L 12.657 17.657 Z M 15 12 C 15 9.629 13.804 7.463 11.625 5.719 L 10.375 7.281 C 12.196 8.737 13 10.371 13 12 L 15 12 Z M 11.625 5.719 C 9.801 4.26 8.433 2.064 7.981 -0.196 L 6.019 0.196 C 6.567 2.936 8.199 5.54 10.375 7.281 L 11.625 5.719 Z M 6.019 -0.196 C 5.567 2.064 4.199 4.26 2.375 5.719 L 3.625 7.281 C 5.801 5.54 7.433 2.936 7.981 0.196 L 6.019 -0.196 Z M 2.375 5.719 C 0.196 7.463 -1 9.629 -1 12 L 1 12 C 1 10.371 1.804 8.737 3.625 7.281 L 2.375 5.719 Z M -1 12 C -1 14.122 -0.157 16.157 1.343 17.657 L 2.757 16.243 C 1.632 15.117 1 13.591 1 12 L -1 12 Z M 1.343 17.657 C 2.843 19.157 4.878 20 7 20 L 7 18 C 5.409 18 3.883 17.368 2.757 16.243 L 1.343 17.657 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 5 3)\"/>"
    },
    "IconDroplets": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 4 0 L 4.981 -0.196 C 4.887 -0.664 4.477 -1 4 -1 C 3.523 -1 3.113 -0.664 3.019 -0.196 L 4 0 Z M 4 12 C 6.765 12 9 9.709 9 6.95 L 7 6.95 C 7 8.631 5.635 10 4 10 L 4 12 Z M 9 6.95 C 9 5.426 8.241 4.061 6.922 2.985 L 5.658 4.535 C 6.619 5.319 7 6.154 7 6.95 L 9 6.95 Z M 6.922 2.985 C 5.953 2.195 5.223 1.016 4.981 -0.196 L 3.019 0.196 C 3.357 1.884 4.347 3.465 5.658 4.535 L 6.922 2.985 Z M 3.019 -0.196 C 2.776 1.022 2.052 2.205 1.085 2.979 L 2.335 4.541 C 3.668 3.475 4.644 1.878 4.981 0.196 L 3.019 -0.196 Z M 1.085 2.979 C -0.258 4.054 -1 5.444 -1 6.95 L 1 6.95 C 1 6.156 1.378 5.306 2.335 4.541 L 1.085 2.979 Z M -1 6.95 C -1 9.709 1.235 12 4 12 L 4 10 C 2.365 10 1 8.631 1 6.95 L -1 6.95 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3 5.300)\"/><path d=\"M 2.622 3.049 C 2.329 3.518 2.471 4.135 2.939 4.428 C 3.408 4.721 4.025 4.579 4.318 4.111 L 2.622 3.049 Z M 4.91 0 L 5.891 -0.196 C 5.797 -0.662 5.389 -0.998 4.914 -1 C 4.439 -1.002 4.028 -0.669 3.931 -0.204 L 4.91 0 Z M 11.91 12 L 10.91 12 L 10.91 12.004 L 11.91 12 Z M 0.706 16.262 C 0.315 15.872 -0.318 15.873 -0.708 16.264 C -1.098 16.655 -1.097 17.288 -0.706 17.678 L 0.706 16.262 Z M 4.318 4.111 C 5.068 2.911 5.6 1.589 5.889 0.204 L 3.931 -0.204 C 3.69 0.949 3.247 2.051 2.622 3.049 L 4.318 4.111 Z M 3.929 0.196 C 4.477 2.936 6.109 5.54 8.285 7.281 L 9.535 5.719 C 7.711 4.26 6.343 2.064 5.891 -0.196 L 3.929 0.196 Z M 8.285 7.281 C 10.106 8.737 10.91 10.371 10.91 12 L 12.91 12 C 12.91 9.629 11.714 7.463 9.535 5.719 L 8.285 7.281 Z M 10.91 12.004 C 10.915 13.188 10.568 14.347 9.913 15.334 L 11.58 16.44 C 12.454 15.123 12.917 13.576 12.91 11.996 L 10.91 12.004 Z M 9.913 15.334 C 9.259 16.321 8.326 17.092 7.233 17.548 L 8.003 19.393 C 9.462 18.785 10.707 17.757 11.58 16.44 L 9.913 15.334 Z M 7.233 17.548 C 6.14 18.004 4.936 18.125 3.774 17.896 L 3.388 19.858 C 4.938 20.164 6.545 20.002 8.003 19.393 L 7.233 17.548 Z M 3.774 17.896 C 2.612 17.667 1.545 17.099 0.706 16.262 L -0.706 17.678 C 0.412 18.794 1.837 19.553 3.388 19.858 L 3.774 17.896 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 9.090 3.020)\"/>"
    },
    "IconFacebook": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 11 0 L 12 0 C 12 -0.552 11.552 -1 11 -1 L 11 0 Z M 8 0 L 8 -1 L 8 0 Z M 3 5 L 2 5 L 3 5 Z M 3 8 L 3 9 C 3.552 9 4 8.552 4 8 L 3 8 Z M 0 8 L 0 7 C -0.552 7 -1 7.448 -1 8 L 0 8 Z M 0 12 L -1 12 C -1 12.552 -0.552 13 0 13 L 0 12 Z M 3 12 L 4 12 C 4 11.448 3.552 11 3 11 L 3 12 Z M 3 20 L 2 20 C 2 20.552 2.448 21 3 21 L 3 20 Z M 7 20 L 7 21 C 7.552 21 8 20.552 8 20 L 7 20 Z M 7 12 L 7 11 C 6.448 11 6 11.448 6 12 L 7 12 Z M 10 12 L 10 13 C 10.459 13 10.859 12.688 10.97 12.243 L 10 12 Z M 11 8 L 11.97 8.243 C 12.045 7.944 11.978 7.627 11.788 7.385 C 11.599 7.142 11.308 7 11 7 L 11 8 Z M 7 8 L 6 8 C 6 8.552 6.448 9 7 9 L 7 8 Z M 8 4 L 8 5 L 8 4 Z M 11 4 L 11 5 C 11.552 5 12 4.552 12 4 L 11 4 Z M 11 -1 L 8 -1 L 8 1 L 11 1 L 11 -1 Z M 8 -1 C 6.409 -1 4.883 -0.368 3.757 0.757 L 5.172 2.172 C 5.922 1.421 6.939 1 8 1 L 8 -1 Z M 3.757 0.757 C 2.632 1.883 2 3.409 2 5 L 4 5 C 4 3.939 4.421 2.922 5.172 2.172 L 3.757 0.757 Z M 2 5 L 2 8 L 4 8 L 4 5 L 2 5 Z M 3 7 L 0 7 L 0 9 L 3 9 L 3 7 Z M -1 8 L -1 12 L 1 12 L 1 8 L -1 8 Z M 0 13 L 3 13 L 3 11 L 0 11 L 0 13 Z M 2 12 L 2 20 L 4 20 L 4 12 L 2 12 Z M 3 21 L 7 21 L 7 19 L 3 19 L 3 21 Z M 8 20 L 8 12 L 6 12 L 6 20 L 8 20 Z M 7 13 L 10 13 L 10 11 L 7 11 L 7 13 Z M 10.97 12.243 L 11.97 8.243 L 10.03 7.757 L 9.03 11.757 L 10.97 12.243 Z M 11 7 L 7 7 L 7 9 L 11 9 L 11 7 Z M 8 8 L 8 5 L 6 5 L 6 8 L 8 8 Z M 8 5 L 8 5 L 6.586 3.586 C 6.211 3.961 6 4.47 6 5 L 8 5 Z M 8 5 L 8 5 L 8 3 C 7.47 3 6.961 3.211 6.586 3.586 L 8 5 Z M 8 5 L 11 5 L 11 3 L 8 3 L 8 5 Z M 12 4 L 12 0 L 10 0 L 10 4 L 12 4 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 2)\"/>"
    },
    "IconFactory": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 0 18 L -1 18 L 0 18 Z M 20 6 L 21 6 C 21 5.625 20.791 5.282 20.458 5.111 C 20.125 4.939 19.724 4.969 19.419 5.186 L 20 6 Z M 13 11 L 12 11 C 12 11.375 12.209 11.718 12.542 11.889 C 12.875 12.061 13.276 12.031 13.581 11.814 L 13 11 Z M 13 6 L 14 6 C 14 5.625 13.791 5.282 13.458 5.111 C 13.125 4.939 12.724 4.969 12.419 5.186 L 13 6 Z M 6 11 L 5 11 C 5 11.375 5.209 11.718 5.542 11.889 C 5.875 12.061 6.276 12.031 6.581 11.814 L 6 11 Z M 6 2 L 7 2 L 6 2 Z M 4 0 L 4 -1 L 4 0 Z M 2 0 L 2 -1 L 2 0 Z M 0 2 L -1 2 L 0 2 Z M -1 18 C -1 18.796 -0.684 19.559 -0.121 20.121 L 1.293 18.707 C 1.105 18.52 1 18.265 1 18 L -1 18 Z M -0.121 20.121 C 0.441 20.684 1.204 21 2 21 L 2 19 C 1.735 19 1.48 18.895 1.293 18.707 L -0.121 20.121 Z M 2 21 L 18 21 L 18 19 L 2 19 L 2 21 Z M 18 21 C 18.796 21 19.559 20.684 20.121 20.121 L 18.707 18.707 C 18.52 18.895 18.265 19 18 19 L 18 21 Z M 20.121 20.121 C 20.684 19.559 21 18.796 21 18 L 19 18 C 19 18.265 18.895 18.52 18.707 18.707 L 20.121 20.121 Z M 21 18 L 21 6 L 19 6 L 19 18 L 21 18 Z M 19.419 5.186 L 12.419 10.186 L 13.581 11.814 L 20.581 6.814 L 19.419 5.186 Z M 14 11 L 14 6 L 12 6 L 12 11 L 14 11 Z M 12.419 5.186 L 5.419 10.186 L 6.581 11.814 L 13.581 6.814 L 12.419 5.186 Z M 7 11 L 7 2 L 5 2 L 5 11 L 7 11 Z M 7 2 C 7 1.204 6.684 0.441 6.121 -0.121 L 4.707 1.293 C 4.895 1.48 5 1.735 5 2 L 7 2 Z M 6.121 -0.121 C 5.559 -0.684 4.796 -1 4 -1 L 4 1 C 4.265 1 4.52 1.105 4.707 1.293 L 6.121 -0.121 Z M 4 -1 L 2 -1 L 2 1 L 4 1 L 4 -1 Z M 2 -1 C 1.204 -1 0.441 -0.684 -0.121 -0.121 L 1.293 1.293 C 1.48 1.105 1.735 1 2 1 L 2 -1 Z M -0.121 -0.121 C -0.684 0.441 -1 1.204 -1 2 L 1 2 C 1 1.735 1.105 1.48 1.293 1.293 L -0.121 -0.121 Z M -1 2 L -1 18 L 1 18 L 1 2 L -1 2 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 2)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 1 1 C 1.552 1 2 0.552 2 0 C 2 -0.552 1.552 -1 1 -1 L 1 1 Z M 0 1 L 1 1 L 1 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 17 18)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 1 1 C 1.552 1 2 0.552 2 0 C 2 -0.552 1.552 -1 1 -1 L 1 1 Z M 0 1 L 1 1 L 1 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 12 18)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 1 1 C 1.552 1 2 0.552 2 0 C 2 -0.552 1.552 -1 1 -1 L 1 1 Z M 0 1 L 1 1 L 1 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 18)\"/>"
    },
    "IconGlobe": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 19 10 C 19 14.971 14.971 19 10 19 L 10 21 C 16.075 21 21 16.075 21 10 L 19 10 Z M 10 19 C 5.029 19 1 14.971 1 10 L -1 10 C -1 16.075 3.925 21 10 21 L 10 19 Z M 1 10 C 1 5.029 5.029 1 10 1 L 10 -1 C 3.925 -1 -1 3.925 -1 10 L 1 10 Z M 10 1 C 14.971 1 19 5.029 19 10 L 21 10 C 21 3.925 16.075 -1 10 -1 L 10 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 2)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 20 1 C 20.552 1 21 0.552 21 0 C 21 -0.552 20.552 -1 20 -1 L 20 1 Z M 0 1 L 20 1 L 20 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 12)\"/><path d=\"M 4 0 L 4.738 -0.674 C 4.549 -0.882 4.281 -1 4 -1 C 3.719 -1 3.451 -0.882 3.262 -0.674 L 4 0 Z M 8 10 L 9 10.021 C 9 10.007 9 9.993 9 9.979 L 8 10 Z M 4 20 L 3.262 20.674 C 3.451 20.882 3.719 21 4 21 C 4.281 21 4.549 20.882 4.738 20.674 L 4 20 Z M 0 10 L -1 9.979 C -1 9.993 -1 10.007 -1 10.021 L 0 10 Z M 3.262 0.674 C 5.599 3.234 6.928 6.555 7 10.021 L 9 9.979 C 8.917 6.029 7.403 2.243 4.738 -0.674 L 3.262 0.674 Z M 7 9.979 C 6.928 13.445 5.599 16.766 3.262 19.326 L 4.738 20.674 C 7.403 17.757 8.917 13.971 9 10.021 L 7 9.979 Z M 4.738 19.326 C 2.401 16.766 1.072 13.445 1 9.979 L -1 10.021 C -0.917 13.971 0.597 17.757 3.262 20.674 L 4.738 19.326 Z M 1 10.021 C 1.072 6.555 2.401 3.234 4.738 0.674 L 3.262 -0.674 C 0.597 2.243 -0.917 6.029 -1 9.979 L 1 10.021 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 8 2)\"/>"
    },
    "IconGripVertical": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 8 11)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 8 4)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 8 18)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 14 11)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 14 4)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 14 18)\"/>"
    },
    "IconHeart": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 18.428 1.588 L 17.72 2.294 L 17.721 2.295 L 18.428 1.588 Z M 14.603 0 L 14.603 1 L 14.603 0 Z M 10.778 1.588 L 10.07 0.882 L 10.066 0.886 L 10.778 1.588 Z M 10.008 2.368 L 9.296 3.071 C 9.484 3.261 9.74 3.368 10.008 3.368 C 10.275 3.368 10.532 3.261 10.72 3.071 L 10.008 2.368 Z M 9.238 1.588 L 9.95 0.886 L 9.946 0.882 L 9.238 1.588 Z M 1.588 1.588 L 2.295 2.295 L 2.296 2.294 L 1.588 1.588 Z M 2.008 10.008 L 1.294 10.709 L 1.301 10.715 L 2.008 10.008 Z M 10.008 18.008 L 9.301 18.715 C 9.488 18.903 9.743 19.008 10.008 19.008 C 10.273 19.008 10.527 18.903 10.715 18.715 L 10.008 18.008 Z M 18.008 10.008 L 18.715 10.715 L 18.722 10.709 L 18.008 10.008 Z M 19.136 0.882 C 18.542 0.286 17.835 -0.188 17.057 -0.511 L 16.29 1.336 C 16.825 1.558 17.311 1.884 17.72 2.294 L 19.136 0.882 Z M 17.057 -0.511 C 16.279 -0.834 15.445 -1 14.603 -1 L 14.603 1 C 15.182 1 15.755 1.114 16.29 1.336 L 17.057 -0.511 Z M 14.603 -1 C 13.761 -1 12.927 -0.834 12.149 -0.511 L 12.916 1.336 C 13.45 1.114 14.024 1 14.603 1 L 14.603 -1 Z M 12.149 -0.511 C 11.371 -0.188 10.664 0.286 10.07 0.882 L 11.486 2.294 C 11.895 1.884 12.381 1.558 12.916 1.336 L 12.149 -0.511 Z M 10.066 0.886 L 9.296 1.666 L 10.72 3.071 L 11.49 2.291 L 10.066 0.886 Z M 10.72 1.666 L 9.95 0.886 L 8.526 2.291 L 9.296 3.071 L 10.72 1.666 Z M 9.946 0.882 C 9.352 0.286 8.645 -0.188 7.867 -0.511 L 7.1 1.336 C 7.635 1.558 8.121 1.884 8.53 2.294 L 9.946 0.882 Z M 7.867 -0.511 C 7.089 -0.834 6.255 -1 5.413 -1 L 5.413 1 C 5.992 1 6.565 1.114 7.1 1.336 L 7.867 -0.511 Z M 5.413 -1 C 4.571 -1 3.737 -0.834 2.959 -0.511 L 3.726 1.336 C 4.26 1.114 4.834 1 5.413 1 L 5.413 -1 Z M 2.959 -0.511 C 2.181 -0.188 1.474 0.286 0.88 0.882 L 2.296 2.294 C 2.705 1.884 3.191 1.558 3.726 1.336 L 2.959 -0.511 Z M 0.881 0.881 C -1.665 3.427 -1.722 7.636 1.294 10.709 L 2.722 9.308 C 0.397 6.94 0.601 3.99 2.295 2.295 L 0.881 0.881 Z M 1.301 10.715 L 9.301 18.715 L 10.715 17.301 L 2.715 9.301 L 1.301 10.715 Z M 10.715 18.715 L 18.715 10.715 L 17.301 9.301 L 9.301 17.301 L 10.715 18.715 Z M 18.722 10.709 C 21.737 7.636 21.681 3.427 19.135 0.881 L 17.721 2.295 C 19.415 3.99 19.618 6.94 17.294 9.308 L 18.722 10.709 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 1.992 2.992)\"/>"
    },
    "IconInstagram": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 5 1 L 15 1 L 15 -1 L 5 -1 L 5 1 Z M 15 1 C 17.209 1 19 2.791 19 5 L 21 5 C 21 1.686 18.314 -1 15 -1 L 15 1 Z M 19 5 L 19 15 L 21 15 L 21 5 L 19 5 Z M 19 15 C 19 17.209 17.209 19 15 19 L 15 21 C 18.314 21 21 18.314 21 15 L 19 15 Z M 15 19 L 5 19 L 5 21 L 15 21 L 15 19 Z M 5 19 C 2.791 19 1 17.209 1 15 L -1 15 C -1 18.314 1.686 21 5 21 L 5 19 Z M 1 15 L 1 5 L -1 5 L -1 15 L 1 15 Z M 1 5 C 1 2.791 2.791 1 5 1 L 5 -1 C 1.686 -1 -1 1.686 -1 5 L 1 5 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 2)\"/><path d=\"M 7.957 3.413 L 6.968 3.56 L 7.957 3.413 Z M 4.587 0.043 L 4.44 1.032 L 4.587 0.043 Z M 6.968 3.56 C 7.06 4.184 6.953 4.822 6.663 5.382 L 8.438 6.303 C 8.922 5.369 9.1 4.307 8.946 3.267 L 6.968 3.56 Z M 6.663 5.382 C 6.372 5.942 5.912 6.396 5.349 6.68 L 6.248 8.466 C 7.187 7.993 7.954 7.236 8.438 6.303 L 6.663 5.382 Z M 5.349 6.68 C 4.785 6.963 4.146 7.062 3.523 6.962 L 3.206 8.937 C 4.244 9.104 5.309 8.939 6.248 8.466 L 5.349 6.68 Z M 3.523 6.962 C 2.9 6.862 2.325 6.568 1.879 6.121 L 0.464 7.536 C 1.208 8.279 2.167 8.769 3.206 8.937 L 3.523 6.962 Z M 1.879 6.121 C 1.432 5.675 1.138 5.1 1.038 4.477 L -0.936 4.794 C -0.769 5.833 -0.279 6.792 0.464 7.536 L 1.879 6.121 Z M 1.038 4.477 C 0.938 3.854 1.037 3.215 1.32 2.651 L -0.466 1.752 C -0.939 2.691 -1.104 3.756 -0.936 4.794 L 1.038 4.477 Z M 1.32 2.651 C 1.604 2.088 2.058 1.628 2.618 1.337 L 1.697 -0.438 C 0.764 0.046 0.007 0.813 -0.466 1.752 L 1.32 2.651 Z M 2.618 1.337 C 3.178 1.047 3.816 0.94 4.44 1.032 L 4.733 -0.946 C 3.693 -1.1 2.631 -0.922 1.697 -0.438 L 2.618 1.337 Z M 4.44 1.032 C 5.077 1.127 5.666 1.424 6.121 1.879 L 7.536 0.464 C 6.777 -0.294 5.795 -0.789 4.733 -0.946 L 4.44 1.032 Z M 6.121 1.879 C 6.576 2.334 6.873 2.923 6.968 3.56 L 8.946 3.267 C 8.789 2.205 8.294 1.223 7.536 0.464 L 6.121 1.879 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 8.043 7.957)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 0.01 1 C 0.562 1 1.01 0.552 1.01 0 C 1.01 -0.552 0.562 -1 0.01 -1 L 0.01 1 Z M 0 1 L 0.01 1 L 0.01 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 17.500 6.500)\"/>"
    },
    "IconLeaf": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 7.021 18 L 7.021 17 L 7.018 17 L 7.021 18 Z M 5.821 4.1 L 5.99 5.086 C 5.997 5.085 6.004 5.083 6.011 5.082 L 5.821 4.1 Z M 15.021 0 L 15.916 -0.447 C 15.763 -0.752 15.467 -0.958 15.129 -0.994 C 14.79 -1.031 14.456 -0.893 14.243 -0.628 L 15.021 0 Z M 7.018 17 C 5.513 17.005 4.061 16.443 2.951 15.428 L 1.601 16.903 C 3.082 18.258 5.017 19.006 7.024 19 L 7.018 17 Z M 2.951 15.428 C 1.84 14.412 1.152 13.016 1.022 11.516 L -0.97 11.688 C -0.798 13.687 0.12 15.549 1.601 16.903 L 2.951 15.428 Z M 1.022 11.516 C 0.893 10.017 1.332 8.523 2.252 7.332 L 0.669 6.109 C -0.558 7.698 -1.143 9.689 -0.97 11.688 L 1.022 11.516 Z M 2.252 7.332 C 3.172 6.141 4.506 5.339 5.99 5.086 L 5.653 3.114 C 3.675 3.452 1.896 4.521 0.669 6.109 L 2.252 7.332 Z M 6.011 5.082 C 8.841 4.536 10.75 4.114 12.205 3.486 C 13.729 2.827 14.727 1.958 15.8 0.628 L 14.243 -0.628 C 13.315 0.522 12.563 1.153 11.412 1.649 C 10.192 2.176 8.502 2.564 5.632 3.118 L 6.011 5.082 Z M 14.127 0.447 C 15.095 2.383 16.021 4.402 16.021 8 L 18.021 8 C 18.021 3.958 16.948 1.617 15.916 -0.447 L 14.127 0.447 Z M 16.021 8 C 16.021 12.93 11.707 17 7.021 17 L 7.021 19 C 12.775 19 18.021 14.07 18.021 8 L 16.021 8 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3.979 2)\"/><path d=\"M -1 9 C -1 9.552 -0.552 10 0 10 C 0.552 10 1 9.552 1 9 L -1 9 Z M 5.08 3 L 5.274 3.981 L 5.275 3.981 L 5.08 3 Z M 11.707 0.707 C 12.098 0.317 12.098 -0.317 11.707 -0.707 C 11.317 -1.098 10.683 -1.098 10.293 -0.707 L 11.707 0.707 Z M 1 9 C 1 6.485 2.505 4.53 5.274 3.981 L 4.886 2.019 C 1.195 2.75 -1 5.515 -1 9 L 1 9 Z M 5.275 3.981 C 6.607 3.717 7.932 3.174 9.045 2.576 C 10.151 1.983 11.116 1.299 11.707 0.707 L 10.293 -0.707 C 9.884 -0.299 9.099 0.277 8.1 0.814 C 7.108 1.346 5.973 1.803 4.885 2.019 L 5.275 3.981 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 12)\"/>"
    },
    "IconLinkedin": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 12 13 L 12 14 C 12.552 14 13 13.552 13 13 L 12 13 Z M 8 13 L 7 13 C 7 13.552 7.448 14 8 14 L 8 13 Z M 4 13 L 4 14 C 4.552 14 5 13.552 5 13 L 4 13 Z M 0 13 L -1 13 C -1 13.552 -0.552 14 0 14 L 0 13 Z M 6 1 C 7.326 1 8.598 1.527 9.536 2.464 L 10.95 1.05 C 9.637 -0.263 7.857 -1 6 -1 L 6 1 Z M 9.536 2.464 C 10.473 3.402 11 4.674 11 6 L 13 6 C 13 4.143 12.263 2.363 10.95 1.05 L 9.536 2.464 Z M 11 6 L 11 13 L 13 13 L 13 6 L 11 6 Z M 12 12 L 8 12 L 8 14 L 12 14 L 12 12 Z M 9 13 L 9 6 L 7 6 L 7 13 L 9 13 Z M 9 6 C 9 5.204 8.684 4.441 8.121 3.879 L 6.707 5.293 C 6.895 5.48 7 5.735 7 6 L 9 6 Z M 8.121 3.879 C 7.559 3.316 6.796 3 6 3 L 6 5 C 6.265 5 6.52 5.105 6.707 5.293 L 8.121 3.879 Z M 6 3 C 5.204 3 4.441 3.316 3.879 3.879 L 5.293 5.293 C 5.48 5.105 5.735 5 6 5 L 6 3 Z M 3.879 3.879 C 3.316 4.441 3 5.204 3 6 L 5 6 C 5 5.735 5.105 5.48 5.293 5.293 L 3.879 3.879 Z M 3 6 L 3 13 L 5 13 L 5 6 L 3 6 Z M 4 12 L 0 12 L 0 14 L 4 14 L 4 12 Z M 1 13 L 1 6 L -1 6 L -1 13 L 1 13 Z M 1 6 C 1 4.674 1.527 3.402 2.464 2.464 L 1.05 1.05 C -0.263 2.363 -1 4.143 -1 6 L 1 6 Z M 2.464 2.464 C 3.402 1.527 4.674 1 6 1 L 6 -1 C 4.143 -1 2.363 -0.263 1.05 1.05 L 2.464 2.464 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 10 8)\"/><path d=\"M 0 0 L 0 -1 C -0.552 -1 -1 -0.552 -1 0 L 0 0 Z M 4 0 L 5 0 C 5 -0.552 4.552 -1 4 -1 L 4 0 Z M 4 12 L 4 13 C 4.552 13 5 12.552 5 12 L 4 12 Z M 0 12 L -1 12 C -1 12.552 -0.552 13 0 13 L 0 12 Z M 0 1 L 4 1 L 4 -1 L 0 -1 L 0 1 Z M 3 0 L 3 12 L 5 12 L 5 0 L 3 0 Z M 4 11 L 0 11 L 0 13 L 4 13 L 4 11 Z M 1 12 L 1 0 L -1 0 L -1 12 L 1 12 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 9)\"/><path d=\"M 3 2 C 3 2.552 2.552 3 2 3 L 2 5 C 3.657 5 5 3.657 5 2 L 3 2 Z M 2 3 C 1.448 3 1 2.552 1 2 L -1 2 C -1 3.657 0.343 5 2 5 L 2 3 Z M 1 2 C 1 1.448 1.448 1 2 1 L 2 -1 C 0.343 -1 -1 0.343 -1 2 L 1 2 Z M 2 1 C 2.552 1 3 1.448 3 2 L 5 2 C 5 0.343 3.657 -1 2 -1 L 2 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 2)\"/>"
    },
    "IconMail": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 2 1 L 18 1 L 18 -1 L 2 -1 L 2 1 Z M 18 1 C 18.552 1 19 1.448 19 2 L 21 2 C 21 0.343 19.657 -1 18 -1 L 18 1 Z M 19 2 L 19 14 L 21 14 L 21 2 L 19 2 Z M 19 14 C 19 14.552 18.552 15 18 15 L 18 17 C 19.657 17 21 15.657 21 14 L 19 14 Z M 18 15 L 2 15 L 2 17 L 18 17 L 18 15 Z M 2 15 C 1.448 15 1 14.552 1 14 L -1 14 C -1 15.657 0.343 17 2 17 L 2 15 Z M 1 14 L 1 2 L -1 2 L -1 14 L 1 14 Z M 1 2 C 1 1.448 1.448 1 2 1 L 2 -1 C 0.343 -1 -1 0.343 -1 2 L 1 2 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 4)\"/><path d=\"M 20.536 0.844 C 21.002 0.548 21.14 -0.07 20.844 -0.536 C 20.548 -1.002 19.93 -1.14 19.464 -0.844 L 20.536 0.844 Z M 11.03 5.7 L 11.561 6.547 L 11.566 6.544 L 11.03 5.7 Z M 10 5.996 L 10 4.996 L 10 5.996 Z M 8.97 5.7 L 8.434 6.544 L 8.439 6.547 L 8.97 5.7 Z M 0.536 -0.844 C 0.07 -1.14 -0.548 -1.002 -0.844 -0.536 C -1.14 -0.07 -1.002 0.548 -0.536 0.844 L 0.536 -0.844 Z M 19.464 -0.844 L 10.494 4.856 L 11.566 6.544 L 20.536 0.844 L 19.464 -0.844 Z M 10.499 4.853 C 10.349 4.946 10.177 4.996 10 4.996 L 10 6.996 C 10.552 6.996 11.093 6.841 11.561 6.547 L 10.499 4.853 Z M 10 4.996 C 9.823 4.996 9.651 4.946 9.501 4.853 L 8.439 6.547 C 8.907 6.841 9.448 6.996 10 6.996 L 10 4.996 Z M 9.506 4.856 L 0.536 -0.844 L -0.536 0.844 L 8.434 6.544 L 9.506 4.856 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 7)\"/>"
    },
    "IconMapPin": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 16 8 L 17 8 L 16 8 Z M 8 20 L 7.4 20.8 C 7.756 21.067 8.244 21.067 8.6 20.8 L 8 20 Z M 8 0 L 8 -1 L 8 0 Z M 15 8 C 15 10.605 13.23 13.362 11.253 15.586 C 10.288 16.671 9.319 17.579 8.591 18.216 C 8.228 18.534 7.927 18.783 7.717 18.951 C 7.613 19.035 7.531 19.099 7.477 19.141 C 7.45 19.162 7.43 19.177 7.417 19.187 C 7.41 19.192 7.406 19.196 7.403 19.198 C 7.401 19.199 7.4 19.2 7.4 19.2 C 7.4 19.2 7.4 19.2 7.4 19.2 C 7.4 19.2 7.4 19.2 7.4 19.2 C 7.4 19.2 7.4 19.2 8 20 C 8.6 20.8 8.6 20.8 8.6 20.8 C 8.601 20.8 8.601 20.799 8.601 20.799 C 8.602 20.799 8.602 20.798 8.603 20.798 C 8.604 20.797 8.606 20.795 8.609 20.793 C 8.614 20.79 8.62 20.785 8.629 20.778 C 8.646 20.765 8.671 20.746 8.702 20.722 C 8.765 20.673 8.856 20.602 8.97 20.51 C 9.198 20.327 9.522 20.06 9.909 19.721 C 10.681 19.046 11.712 18.079 12.747 16.914 C 14.77 14.638 17 11.395 17 8 L 15 8 Z M 8 20 C 8.6 19.2 8.6 19.2 8.6 19.2 C 8.6 19.2 8.6 19.2 8.6 19.2 C 8.6 19.2 8.6 19.2 8.6 19.2 C 8.6 19.2 8.599 19.199 8.597 19.198 C 8.594 19.196 8.59 19.192 8.583 19.187 C 8.57 19.177 8.55 19.162 8.523 19.141 C 8.469 19.099 8.387 19.035 8.283 18.951 C 8.073 18.783 7.772 18.534 7.409 18.216 C 6.681 17.579 5.712 16.671 4.747 15.586 C 2.77 13.362 1 10.605 1 8 L -1 8 C -1 11.395 1.23 14.638 3.253 16.914 C 4.288 18.079 5.319 19.046 6.091 19.721 C 6.478 20.06 6.802 20.327 7.03 20.51 C 7.144 20.602 7.235 20.673 7.298 20.722 C 7.329 20.746 7.354 20.765 7.371 20.778 C 7.38 20.785 7.386 20.79 7.391 20.793 C 7.394 20.795 7.396 20.797 7.397 20.798 C 7.398 20.798 7.398 20.799 7.399 20.799 C 7.399 20.799 7.399 20.8 7.4 20.8 C 7.4 20.8 7.4 20.8 8 20 Z M 1 8 C 1 6.143 1.737 4.363 3.05 3.05 L 1.636 1.636 C -0.052 3.324 -1 5.613 -1 8 L 1 8 Z M 3.05 3.05 C 4.363 1.737 6.143 1 8 1 L 8 -1 C 5.613 -1 3.324 -0.052 1.636 1.636 L 3.05 3.05 Z M 8 1 C 9.857 1 11.637 1.737 12.95 3.05 L 14.364 1.636 C 12.676 -0.052 10.387 -1 8 -1 L 8 1 Z M 12.95 3.05 C 14.263 4.363 15 6.143 15 8 L 17 8 C 17 5.613 16.052 3.324 14.364 1.636 L 12.95 3.05 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 4 2)\"/><path d=\"M 5 3 C 5 4.105 4.105 5 3 5 L 3 7 C 5.209 7 7 5.209 7 3 L 5 3 Z M 3 5 C 1.895 5 1 4.105 1 3 L -1 3 C -1 5.209 0.791 7 3 7 L 3 5 Z M 1 3 C 1 1.895 1.895 1 3 1 L 3 -1 C 0.791 -1 -1 0.791 -1 3 L 1 3 Z M 3 1 C 4.105 1 5 1.895 5 3 L 7 3 C 7 0.791 5.209 -1 3 -1 L 3 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 9 7)\"/>"
    },
    "IconMenu": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 16 1 C 16.552 1 17 0.552 17 0 C 17 -0.552 16.552 -1 16 -1 L 16 1 Z M 0 1 L 16 1 L 16 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 4 12)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 16 1 C 16.552 1 17 0.552 17 0 C 17 -0.552 16.552 -1 16 -1 L 16 1 Z M 0 1 L 16 1 L 16 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 4 6)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 16 1 C 16.552 1 17 0.552 17 0 C 17 -0.552 16.552 -1 16 -1 L 16 1 Z M 0 1 L 16 1 L 16 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 4 18)\"/>"
    },
    "IconMinus": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 14 1 C 14.552 1 15 0.552 15 0 C 15 -0.552 14.552 -1 14 -1 L 14 1 Z M 0 1 L 14 1 L 14 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 5 12)\"/>"
    },
    "IconMoreHorizontal": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 11 11)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 18 11)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 4 11)\"/>"
    },
    "IconPackage": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 8.5 6.056 C 8.979 6.332 9.59 6.168 9.866 5.69 C 10.142 5.211 9.978 4.6 9.5 4.324 L 8.5 6.056 Z M 0.5 -0.866 C 0.021 -1.142 -0.59 -0.978 -0.866 -0.5 C -1.142 -0.021 -0.978 0.59 -0.5 0.866 L 0.5 -0.866 Z M 9.5 4.324 L 0.5 -0.866 L -0.5 0.866 L 8.5 6.056 L 9.5 4.324 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7.500 4.210)\"/><path d=\"M 18 13.998 L 19 13.999 L 19 13.998 L 18 13.998 Z M 18 5.998 L 19 5.998 L 19 5.997 L 18 5.998 Z M 17 4.268 L 17.5 3.402 L 17.496 3.4 L 17 4.268 Z M 10 0.268 L 9.5 1.134 L 9.504 1.136 L 10 0.268 Z M 8 0.268 L 8.496 1.136 L 8.5 1.134 L 8 0.268 Z M 1 4.268 L 0.504 3.4 L 0.5 3.402 L 1 4.268 Z M 0 5.998 L -1 5.997 L -1 5.998 L 0 5.998 Z M 0 13.998 L -1 13.998 L -1 13.999 L 0 13.998 Z M 1 15.728 L 0.5 16.594 L 0.504 16.596 L 1 15.728 Z M 8 19.728 L 8.5 18.862 L 8.496 18.86 L 8 19.728 Z M 10 19.728 L 9.504 18.86 L 9.5 18.862 L 10 19.728 Z M 17 15.728 L 17.496 16.596 L 17.5 16.594 L 17 15.728 Z M 19 13.998 L 19 5.998 L 17 5.998 L 17 13.998 L 19 13.998 Z M 19 5.997 C 18.999 5.471 18.861 4.954 18.597 4.499 L 16.866 5.5 C 16.954 5.651 17 5.824 17 5.999 L 19 5.997 Z M 18.597 4.499 C 18.334 4.043 17.956 3.665 17.5 3.402 L 16.5 5.134 C 16.652 5.222 16.778 5.348 16.866 5.5 L 18.597 4.499 Z M 17.496 3.4 L 10.496 -0.6 L 9.504 1.136 L 16.504 5.136 L 17.496 3.4 Z M 10.5 -0.598 C 10.044 -0.861 9.527 -1 9 -1 L 9 1 C 9.176 1 9.348 1.046 9.5 1.134 L 10.5 -0.598 Z M 9 -1 C 8.473 -1 7.956 -0.861 7.5 -0.598 L 8.5 1.134 C 8.652 1.046 8.824 1 9 1 L 9 -1 Z M 7.504 -0.6 L 0.504 3.4 L 1.496 5.136 L 8.496 1.136 L 7.504 -0.6 Z M 0.5 3.402 C 0.044 3.665 -0.334 4.043 -0.597 4.499 L 1.134 5.5 C 1.222 5.348 1.348 5.222 1.5 5.134 L 0.5 3.402 Z M -0.597 4.499 C -0.861 4.954 -0.999 5.471 -1 5.997 L 1 5.999 C 1 5.824 1.046 5.651 1.134 5.5 L -0.597 4.499 Z M -1 5.998 L -1 13.998 L 1 13.998 L 1 5.998 L -1 5.998 Z M -1 13.999 C -0.999 14.525 -0.861 15.042 -0.597 15.497 L 1.134 14.496 C 1.046 14.345 1 14.172 1 13.997 L -1 13.999 Z M -0.597 15.497 C -0.334 15.953 0.044 16.331 0.5 16.594 L 1.5 14.862 C 1.348 14.774 1.222 14.648 1.134 14.496 L -0.597 15.497 Z M 0.504 16.596 L 7.504 20.596 L 8.496 18.86 L 1.496 14.86 L 0.504 16.596 Z M 7.5 20.594 C 7.956 20.857 8.473 20.996 9 20.996 L 9 18.996 C 8.824 18.996 8.652 18.95 8.5 18.862 L 7.5 20.594 Z M 9 20.996 C 9.527 20.996 10.044 20.857 10.5 20.594 L 9.5 18.862 C 9.348 18.95 9.176 18.996 9 18.996 L 9 20.996 Z M 10.496 20.596 L 17.496 16.596 L 16.504 14.86 L 9.504 18.86 L 10.496 20.596 Z M 17.5 16.594 C 17.956 16.331 18.334 15.953 18.597 15.497 L 16.866 14.496 C 16.778 14.648 16.652 14.774 16.5 14.862 L 17.5 16.594 Z M 18.597 15.497 C 18.861 15.042 18.999 14.525 19 13.999 L 17 13.997 C 17 14.172 16.954 14.345 16.866 14.496 L 18.597 15.497 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3 2.002)\"/><path d=\"M 0.498 -0.867 C 0.019 -1.142 -0.592 -0.977 -0.867 -0.498 C -1.142 -0.019 -0.977 0.592 -0.498 0.867 L 0.498 -0.867 Z M 8.71 5 L 8.212 5.867 C 8.52 6.044 8.9 6.044 9.208 5.867 L 8.71 5 Z M 17.918 0.867 C 18.397 0.592 18.562 -0.019 18.287 -0.498 C 18.012 -0.977 17.401 -1.142 16.922 -0.867 L 17.918 0.867 Z M -0.498 0.867 L 8.212 5.867 L 9.208 4.133 L 0.498 -0.867 L -0.498 0.867 Z M 9.208 5.867 L 17.918 0.867 L 16.922 -0.867 L 8.212 4.133 L 9.208 5.867 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3.290 7)\"/><path d=\"M -1 10 C -1 10.552 -0.552 11 0 11 C 0.552 11 1 10.552 1 10 L -1 10 Z M 1 0 C 1 -0.552 0.552 -1 0 -1 C -0.552 -1 -1 -0.552 -1 0 L 1 0 Z M 1 10 L 1 0 L -1 0 L -1 10 L 1 10 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 12 12)\"/>"
    },
    "IconPhone": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 19.888 14.92 L 18.888 14.895 C 18.888 14.904 18.888 14.912 18.888 14.92 L 19.888 14.92 Z M 19.888 17.92 L 18.888 17.92 L 18.888 17.924 L 19.888 17.92 Z M 17.708 19.92 L 17.6 20.914 C 17.606 20.915 17.612 20.915 17.618 20.916 L 17.708 19.92 Z M 9.078 16.85 L 9.622 16.011 L 9.614 16.006 L 9.078 16.85 Z M 3.078 10.85 L 3.922 10.314 L 3.917 10.306 L 3.078 10.85 Z M 0.008 2.18 L -0.988 2.27 C -0.987 2.275 -0.987 2.281 -0.986 2.286 L 0.008 2.18 Z M 1.998 0 L 1.998 -1 L 1.997 -1 L 1.998 0 Z M 4.998 0 L 4.998 1 L 5.008 1 L 4.998 0 Z M 6.998 1.72 L 7.99 1.589 L 7.988 1.58 L 6.998 1.72 Z M 7.698 4.53 L 6.761 4.88 L 6.762 4.882 L 7.698 4.53 Z M 7.248 6.64 L 7.955 7.347 L 7.959 7.343 L 7.248 6.64 Z M 5.978 7.91 L 5.271 7.203 C 4.953 7.521 4.886 8.013 5.109 8.404 L 5.978 7.91 Z M 11.978 13.91 L 11.484 14.779 C 11.875 15.002 12.367 14.936 12.685 14.617 L 11.978 13.91 Z M 13.248 12.64 L 12.545 11.929 L 12.541 11.933 L 13.248 12.64 Z M 15.358 12.19 L 15.006 13.126 L 15.009 13.127 L 15.358 12.19 Z M 18.168 12.89 L 18.308 11.9 L 18.299 11.899 L 18.168 12.89 Z M 18.888 14.92 L 18.888 17.92 L 20.888 17.92 L 20.888 14.92 L 18.888 14.92 Z M 18.888 17.924 C 18.889 18.063 18.86 18.201 18.804 18.329 L 20.637 19.13 C 20.804 18.747 20.89 18.334 20.888 17.916 L 18.888 17.924 Z M 18.804 18.329 C 18.749 18.456 18.667 18.571 18.564 18.665 L 19.916 20.139 C 20.224 19.856 20.47 19.513 20.637 19.13 L 18.804 18.329 Z M 18.564 18.665 C 18.462 18.759 18.34 18.831 18.208 18.876 L 18.849 20.77 C 19.245 20.636 19.608 20.421 19.916 20.139 L 18.564 18.665 Z M 18.208 18.876 C 18.077 18.92 17.937 18.937 17.798 18.924 L 17.618 20.916 C 18.034 20.954 18.453 20.904 18.849 20.77 L 18.208 18.876 Z M 17.816 18.926 C 14.894 18.608 12.088 17.61 9.622 16.011 L 8.534 17.689 C 11.262 19.458 14.367 20.563 17.6 20.914 L 17.816 18.926 Z M 9.614 16.006 C 7.322 14.549 5.379 12.606 3.922 10.314 L 2.234 11.386 C 3.848 13.926 6.002 16.08 8.542 17.694 L 9.614 16.006 Z M 3.917 10.306 C 2.313 7.829 1.314 5.009 1.003 2.074 L -0.986 2.286 C -0.641 5.533 0.463 8.653 2.239 11.394 L 3.917 10.306 Z M 1.004 2.09 C 0.992 1.952 1.008 1.812 1.052 1.681 L -0.843 1.043 C -0.976 1.437 -1.025 1.855 -0.988 2.27 L 1.004 2.09 Z M 1.052 1.681 C 1.097 1.549 1.168 1.428 1.261 1.326 L -0.216 -0.022 C -0.496 0.285 -0.71 0.648 -0.843 1.043 L 1.052 1.681 Z M 1.261 1.326 C 1.355 1.223 1.469 1.141 1.596 1.085 L 0.788 -0.744 C 0.407 -0.576 0.065 -0.33 -0.216 -0.022 L 1.261 1.326 Z M 1.596 1.085 C 1.723 1.029 1.86 1 1.999 1 L 1.997 -1 C 1.581 -1 1.169 -0.912 0.788 -0.744 L 1.596 1.085 Z M 1.998 1 L 4.998 1 L 4.998 -1 L 1.998 -1 L 1.998 1 Z M 5.008 1 C 5.251 0.998 5.486 1.084 5.67 1.242 L 6.974 -0.275 C 6.422 -0.749 5.716 -1.007 4.988 -1 L 5.008 1 Z M 5.67 1.242 C 5.854 1.4 5.974 1.62 6.008 1.86 L 7.988 1.58 C 7.886 0.859 7.526 0.2 6.974 -0.275 L 5.67 1.242 Z M 6.007 1.851 C 6.143 2.886 6.396 3.902 6.761 4.88 L 8.635 4.18 C 8.323 3.344 8.106 2.475 7.99 1.589 L 6.007 1.851 Z M 6.762 4.882 C 6.829 5.061 6.844 5.255 6.804 5.442 L 8.76 5.86 C 8.88 5.299 8.836 4.715 8.634 4.178 L 6.762 4.882 Z M 6.804 5.442 C 6.764 5.629 6.671 5.801 6.537 5.937 L 7.959 7.343 C 8.362 6.935 8.64 6.421 8.76 5.86 L 6.804 5.442 Z M 6.541 5.933 L 5.271 7.203 L 6.685 8.617 L 7.955 7.347 L 6.541 5.933 Z M 5.109 8.404 C 6.621 11.064 8.824 13.267 11.484 14.779 L 12.472 13.041 C 10.125 11.706 8.182 9.763 6.847 7.416 L 5.109 8.404 Z M 12.685 14.617 L 13.955 13.347 L 12.541 11.933 L 11.271 13.203 L 12.685 14.617 Z M 13.951 13.351 C 14.087 13.217 14.259 13.124 14.446 13.084 L 14.029 11.128 C 13.468 11.248 12.953 11.526 12.545 11.929 L 13.951 13.351 Z M 14.446 13.084 C 14.633 13.044 14.827 13.059 15.006 13.126 L 15.71 11.254 C 15.173 11.052 14.59 11.009 14.029 11.128 L 14.446 13.084 Z M 15.009 13.127 C 15.986 13.492 17.003 13.745 18.037 13.882 L 18.299 11.899 C 17.414 11.782 16.544 11.565 15.708 11.253 L 15.009 13.127 Z M 18.028 13.88 C 18.271 13.915 18.493 14.037 18.652 14.224 L 20.178 12.931 C 19.702 12.37 19.036 12.003 18.308 11.9 L 18.028 13.88 Z M 18.652 14.224 C 18.81 14.411 18.895 14.65 18.888 14.895 L 20.888 14.945 C 20.906 14.209 20.653 13.493 20.178 12.931 L 18.652 14.224 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2.112 2)\"/>"
    },
    "IconPlus": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 1 0 C 1 -0.552 0.552 -1 0 -1 C -0.552 -1 -1 -0.552 -1 0 L 1 0 Z M -1 14 C -1 14.552 -0.552 15 0 15 C 0.552 15 1 14.552 1 14 L -1 14 Z M -1 0 L -1 14 L 1 14 L 1 0 L -1 0 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 12 5)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 14 1 C 14.552 1 15 0.552 15 0 C 15 -0.552 14.552 -1 14 -1 L 14 1 Z M 0 1 L 14 1 L 14 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 5 12)\"/>"
    },
    "IconRecycle": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 3.996 10.5 C 4.548 10.5 4.996 10.052 4.996 9.5 C 4.996 8.948 4.548 8.5 3.996 8.5 L 3.996 10.5 Z M 1.811 9.5 L 1.811 8.5 L 1.808 8.5 L 1.811 9.5 Z M 0.241 8.619 L -0.624 9.121 C -0.621 9.126 -0.618 9.132 -0.614 9.138 L 0.241 8.619 Z M 0.237 6.835 L -0.629 6.334 L -0.63 6.337 L 0.237 6.835 Z M 5.057 0.501 C 5.334 0.023 5.171 -0.589 4.693 -0.866 C 4.215 -1.142 3.603 -0.979 3.326 -0.501 L 5.057 0.501 Z M 3.996 8.5 L 1.811 8.5 L 1.811 10.5 L 3.996 10.5 L 3.996 8.5 Z M 1.808 8.5 C 1.665 8.5 1.524 8.464 1.399 8.394 L 0.421 10.138 C 0.846 10.377 1.326 10.501 1.814 10.5 L 1.808 8.5 Z M 1.399 8.394 C 1.275 8.324 1.17 8.223 1.096 8.1 L -0.614 9.138 C -0.361 9.555 -0.005 9.899 0.421 10.138 L 1.399 8.394 Z M 1.106 8.117 C 1.037 7.998 1 7.863 1 7.725 L -1 7.73 C -0.999 8.218 -0.869 8.698 -0.624 9.121 L 1.106 8.117 Z M 1 7.725 C 1 7.588 1.036 7.452 1.104 7.333 L -0.63 6.337 C -0.874 6.761 -1.001 7.241 -1 7.73 L 1 7.725 Z M 1.102 7.336 L 5.057 0.501 L 3.326 -0.501 L -0.629 6.334 L 1.102 7.336 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3.004 9.500)\"/><path d=\"M 0 3.785 C -0.552 3.785 -1 4.233 -1 4.785 C -1 5.337 -0.552 5.785 0 5.785 L 0 3.785 Z M 8.203 4.785 L 8.203 5.785 L 8.211 5.785 L 8.203 4.785 Z M 9.759 3.895 L 10.617 4.409 C 10.62 4.403 10.623 4.398 10.626 4.392 L 9.759 3.895 Z M 9.995 3.007 L 10.995 3.007 L 9.995 3.007 Z M 9.759 2.12 L 10.626 1.623 L 10.625 1.619 L 9.759 2.12 Z M 9.399 -0.501 C 9.122 -0.979 8.51 -1.142 8.032 -0.866 C 7.554 -0.589 7.391 0.023 7.667 0.501 L 9.399 -0.501 Z M 0 5.785 L 8.203 5.785 L 8.203 3.785 L 0 3.785 L 0 5.785 Z M 8.211 5.785 C 8.696 5.781 9.173 5.653 9.594 5.412 L 8.601 3.676 C 8.477 3.746 8.338 3.784 8.195 3.785 L 8.211 5.785 Z M 9.594 5.412 C 10.015 5.171 10.368 4.825 10.617 4.409 L 8.901 3.381 C 8.828 3.504 8.725 3.605 8.601 3.676 L 9.594 5.412 Z M 10.626 4.392 C 10.868 3.971 10.995 3.493 10.995 3.007 L 8.995 3.007 C 8.995 3.144 8.96 3.279 8.892 3.398 L 10.626 4.392 Z M 10.995 3.007 C 10.995 2.522 10.868 2.044 10.626 1.623 L 8.892 2.617 C 8.96 2.736 8.995 2.871 8.995 3.007 L 10.995 3.007 Z M 10.625 1.619 L 9.399 -0.501 L 7.667 0.501 L 8.893 2.621 L 10.625 1.619 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 11 14.215)\"/><path d=\"M 3.707 0.707 C 4.098 0.317 4.098 -0.317 3.707 -0.707 C 3.317 -1.098 2.683 -1.098 2.293 -0.707 L 3.707 0.707 Z M 0 3 L -0.707 2.293 C -1.098 2.683 -1.098 3.317 -0.707 3.707 L 0 3 Z M 2.293 6.707 C 2.683 7.098 3.317 7.098 3.707 6.707 C 4.098 6.317 4.098 5.683 3.707 5.293 L 2.293 6.707 Z M 2.293 -0.707 L -0.707 2.293 L 0.707 3.707 L 3.707 0.707 L 2.293 -0.707 Z M -0.707 3.707 L 2.293 6.707 L 3.707 5.293 L 0.707 2.293 L -0.707 3.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 11 16)\"/><path d=\"M 4.227 4.355 C 4.37 4.888 4.918 5.205 5.452 5.062 C 5.985 4.919 6.302 4.371 6.159 3.837 L 4.227 4.355 Z M 4.096 0 L 5.062 -0.259 C 4.993 -0.515 4.826 -0.733 4.596 -0.866 C 4.366 -0.999 4.093 -1.035 3.837 -0.966 L 4.096 0 Z M -0.259 0.132 C -0.792 0.275 -1.109 0.823 -0.966 1.357 C -0.823 1.89 -0.275 2.207 0.259 2.064 L -0.259 0.132 Z M 6.159 3.837 L 5.062 -0.259 L 3.13 0.259 L 4.227 4.355 L 6.159 3.837 Z M 3.837 -0.966 L -0.259 0.132 L 0.259 2.064 L 4.355 0.966 L 3.837 -0.966 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3.100 9.500)\"/><path d=\"M -0.866 2.311 C -1.142 2.789 -0.978 3.401 -0.5 3.677 C -0.022 3.953 0.59 3.789 0.866 3.311 L -0.866 2.311 Z M 1.093 0.919 L 1.959 1.419 L 1.96 1.417 L 1.093 0.919 Z M 2.641 0 L 2.639 -1 C 2.633 -1 2.626 -1 2.619 -1 L 2.641 0 Z M 4.187 0.888 L 5.053 0.389 L 5.052 0.386 L 4.187 0.888 Z M 7.264 8.23 C 7.539 8.709 8.151 8.873 8.629 8.597 C 9.108 8.322 9.272 7.71 8.996 7.232 L 7.264 8.23 Z M 0.866 3.311 L 1.959 1.419 L 0.227 0.419 L -0.866 2.311 L 0.866 3.311 Z M 1.96 1.417 C 2.032 1.292 2.134 1.189 2.257 1.116 L 1.236 -0.604 C 0.816 -0.355 0.468 -0.002 0.226 0.421 L 1.96 1.417 Z M 2.257 1.116 C 2.38 1.043 2.519 1.003 2.663 1 L 2.619 -1 C 2.132 -0.989 1.655 -0.853 1.236 -0.604 L 2.257 1.116 Z M 2.643 1 C 2.78 1 2.916 1.036 3.035 1.104 L 4.031 -0.63 C 3.607 -0.873 3.127 -1.001 2.639 -1 L 2.643 1 Z M 3.035 1.104 C 3.154 1.173 3.253 1.271 3.322 1.39 L 5.052 0.386 C 4.806 -0.037 4.454 -0.387 4.031 -0.63 L 3.035 1.104 Z M 3.321 1.387 L 7.264 8.23 L 8.996 7.232 L 5.053 0.389 L 3.321 1.387 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 9.344 3)\"/><path d=\"M 0.259 2.032 C -0.275 1.889 -0.823 2.206 -0.966 2.739 C -1.109 3.273 -0.792 3.821 -0.259 3.964 L 0.259 2.032 Z M 4.096 4.096 L 3.837 5.062 C 4.093 5.131 4.366 5.095 4.596 4.962 C 4.826 4.829 4.993 4.611 5.062 4.355 L 4.096 4.096 Z M 6.159 0.259 C 6.302 -0.275 5.985 -0.823 5.452 -0.966 C 4.918 -1.109 4.37 -0.792 4.227 -0.259 L 6.159 0.259 Z M -0.259 3.964 L 3.837 5.062 L 4.355 3.13 L 0.259 2.032 L -0.259 3.964 Z M 5.062 4.355 L 6.159 0.259 L 4.227 -0.259 L 3.13 3.837 L 5.062 4.355 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 13.378 6.635)\"/>"
    },
    "IconSearch": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 15 8 C 15 11.866 11.866 15 8 15 L 8 17 C 12.971 17 17 12.971 17 8 L 15 8 Z M 8 15 C 4.134 15 1 11.866 1 8 L -1 8 C -1 12.971 3.029 17 8 17 L 8 15 Z M 1 8 C 1 4.134 4.134 1 8 1 L 8 -1 C 3.029 -1 -1 3.029 -1 8 L 1 8 Z M 8 1 C 11.866 1 15 4.134 15 8 L 17 8 C 17 3.029 12.971 -1 8 -1 L 8 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3 3)\"/><path d=\"M 3.643 5.057 C 4.033 5.448 4.667 5.448 5.057 5.057 C 5.448 4.667 5.448 4.033 5.057 3.643 L 3.643 5.057 Z M 0.707 -0.707 C 0.317 -1.098 -0.317 -1.098 -0.707 -0.707 C -1.098 -0.317 -1.098 0.317 -0.707 0.707 L 0.707 -0.707 Z M 5.057 3.643 L 0.707 -0.707 L -0.707 0.707 L 3.643 5.057 L 5.057 3.643 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 16.650 16.650)\"/>"
    },
    "IconShieldCheck": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 8 20 L 7.553 20.894 C 7.834 21.035 8.166 21.035 8.447 20.894 L 8 20 Z M 16 3 L 17 3 C 17 2.583 16.741 2.21 16.351 2.064 L 16 3 Z M 8 0 L 8.351 -0.936 C 8.125 -1.021 7.875 -1.021 7.649 -0.936 L 8 0 Z M 0 3 L -0.351 2.064 C -0.741 2.21 -1 2.583 -1 3 L 0 3 Z M 8 20 C 8.447 20.894 8.448 20.894 8.448 20.894 C 8.448 20.894 8.448 20.894 8.449 20.894 C 8.449 20.893 8.45 20.893 8.451 20.893 C 8.452 20.892 8.455 20.891 8.457 20.889 C 8.463 20.887 8.47 20.883 8.479 20.878 C 8.497 20.869 8.522 20.856 8.555 20.839 C 8.619 20.805 8.712 20.756 8.828 20.691 C 9.06 20.563 9.388 20.374 9.78 20.129 C 10.561 19.641 11.608 18.922 12.659 18.003 C 14.718 16.2 17 13.446 17 10 L 15 10 C 15 12.554 13.282 14.8 11.341 16.497 C 10.392 17.328 9.439 17.984 8.72 18.433 C 8.362 18.657 8.065 18.828 7.859 18.942 C 7.757 18.998 7.677 19.041 7.625 19.068 C 7.599 19.082 7.579 19.092 7.567 19.098 C 7.561 19.101 7.557 19.104 7.554 19.105 C 7.553 19.105 7.552 19.106 7.552 19.106 C 7.552 19.106 7.552 19.106 7.552 19.106 C 7.552 19.106 7.552 19.106 7.552 19.106 C 7.553 19.106 7.553 19.106 8 20 Z M 17 10 L 17 3 L 15 3 L 15 10 L 17 10 Z M 16.351 2.064 L 8.351 -0.936 L 7.649 0.936 L 15.649 3.936 L 16.351 2.064 Z M 7.649 -0.936 L -0.351 2.064 L 0.351 3.936 L 8.351 0.936 L 7.649 -0.936 Z M -1 3 L -1 10 L 1 10 L 1 3 L -1 3 Z M -1 10 C -1 13.446 1.282 16.2 3.341 18.003 C 4.392 18.922 5.439 19.641 6.22 20.129 C 6.612 20.374 6.94 20.563 7.172 20.691 C 7.288 20.756 7.381 20.805 7.445 20.839 C 7.478 20.856 7.503 20.869 7.521 20.878 C 7.53 20.883 7.537 20.887 7.543 20.889 C 7.545 20.891 7.548 20.892 7.549 20.893 C 7.55 20.893 7.551 20.893 7.551 20.894 C 7.552 20.894 7.552 20.894 7.552 20.894 C 7.552 20.894 7.553 20.894 8 20 C 8.447 19.106 8.447 19.106 8.448 19.106 C 8.448 19.106 8.448 19.106 8.448 19.106 C 8.448 19.106 8.448 19.106 8.448 19.106 C 8.448 19.106 8.447 19.105 8.446 19.105 C 8.443 19.104 8.439 19.101 8.433 19.098 C 8.421 19.092 8.401 19.082 8.375 19.068 C 8.323 19.041 8.243 18.998 8.141 18.942 C 7.935 18.828 7.638 18.657 7.28 18.433 C 6.561 17.984 5.608 17.328 4.659 16.497 C 2.718 14.8 1 12.554 1 10 L -1 10 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 4 2)\"/><path d=\"M 0.707 1.293 C 0.317 0.902 -0.317 0.902 -0.707 1.293 C -1.098 1.683 -1.098 2.317 -0.707 2.707 L 0.707 1.293 Z M 2 4 L 1.293 4.707 C 1.683 5.098 2.317 5.098 2.707 4.707 L 2 4 Z M 6.707 0.707 C 7.098 0.317 7.098 -0.317 6.707 -0.707 C 6.317 -1.098 5.683 -1.098 5.293 -0.707 L 6.707 0.707 Z M -0.707 2.707 L 1.293 4.707 L 2.707 3.293 L 0.707 1.293 L -0.707 2.707 Z M 2.707 4.707 L 6.707 0.707 L 5.293 -0.707 L 1.293 3.293 L 2.707 4.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 9 10)\"/>"
    },
    "IconShirt": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 18.128 1.46 L 17.812 2.409 L 17.812 2.409 L 18.128 1.46 Z M 13.748 0 L 14.064 -0.949 C 13.759 -1.05 13.424 -0.999 13.163 -0.811 C 12.902 -0.623 12.748 -0.321 12.748 0 L 13.748 0 Z M 9.748 4 L 9.748 5 L 9.748 4 Z M 5.748 0 L 6.748 0 C 6.748 -0.321 6.593 -0.623 6.333 -0.811 C 6.072 -0.999 5.737 -1.05 5.432 -0.949 L 5.748 0 Z M 1.368 1.46 L 1.684 2.409 L 1.684 2.409 L 1.368 1.46 Z M 0.028 3.69 L 1.014 3.525 L 1.014 3.524 L 0.028 3.69 Z M 0.608 7.16 L 1.595 7 L 1.594 6.995 L 0.608 7.16 Z M 1.598 8 L 1.598 7 L 1.595 7 L 1.598 8 Z M 3.748 8 L 4.748 8 C 4.748 7.448 4.3 7 3.748 7 L 3.748 8 Z M 15.748 8 L 15.748 7 C 15.196 7 14.748 7.448 14.748 8 L 15.748 8 Z M 17.898 8 L 17.901 7 L 17.898 7 L 17.898 8 Z M 18.888 7.16 L 17.902 6.995 L 17.901 7 L 18.888 7.16 Z M 19.468 3.69 L 18.482 3.524 L 18.482 3.525 L 19.468 3.69 Z M 18.444 0.511 L 14.064 -0.949 L 13.432 0.949 L 17.812 2.409 L 18.444 0.511 Z M 12.748 0 C 12.748 0.796 12.432 1.559 11.869 2.121 L 13.283 3.536 C 14.221 2.598 14.748 1.326 14.748 0 L 12.748 0 Z M 11.869 2.121 C 11.307 2.684 10.543 3 9.748 3 L 9.748 5 C 11.074 5 12.346 4.473 13.283 3.536 L 11.869 2.121 Z M 9.748 3 C 8.952 3 8.189 2.684 7.627 2.121 L 6.212 3.536 C 7.15 4.473 8.422 5 9.748 5 L 9.748 3 Z M 7.627 2.121 C 7.064 1.559 6.748 0.796 6.748 0 L 4.748 0 C 4.748 1.326 5.275 2.598 6.212 3.536 L 7.627 2.121 Z M 5.432 -0.949 L 1.052 0.511 L 1.684 2.409 L 6.064 0.949 L 5.432 -0.949 Z M 1.052 0.511 C 0.373 0.737 -0.203 1.199 -0.571 1.812 L 1.143 2.842 C 1.266 2.638 1.458 2.484 1.684 2.409 L 1.052 0.511 Z M -0.571 1.812 C -0.94 2.426 -1.077 3.151 -0.958 3.856 L 1.014 3.524 C 0.974 3.289 1.02 3.047 1.143 2.842 L -0.571 1.812 Z M -0.958 3.855 L -0.378 7.325 L 1.594 6.995 L 1.014 3.525 L -0.958 3.855 Z M -0.379 7.32 C -0.303 7.79 -0.062 8.217 0.301 8.525 L 1.595 7 L 1.595 7 L -0.379 7.32 Z M 0.301 8.525 C 0.664 8.833 1.125 9.001 1.601 9 L 1.595 7 L 1.595 7 L 0.301 8.525 Z M 1.598 9 L 3.748 9 L 3.748 7 L 1.598 7 L 1.598 9 Z M 2.748 8 L 2.748 18 L 4.748 18 L 4.748 8 L 2.748 8 Z M 2.748 18 C 2.748 19.652 4.096 21 5.748 21 L 5.748 19 C 5.2 19 4.748 18.548 4.748 18 L 2.748 18 Z M 5.748 21 L 13.748 21 L 13.748 19 L 5.748 19 L 5.748 21 Z M 13.748 21 C 14.543 21 15.307 20.684 15.869 20.121 L 14.455 18.707 C 14.267 18.895 14.013 19 13.748 19 L 13.748 21 Z M 15.869 20.121 C 16.432 19.559 16.748 18.796 16.748 18 L 14.748 18 C 14.748 18.265 14.642 18.52 14.455 18.707 L 15.869 20.121 Z M 16.748 18 L 16.748 8 L 14.748 8 L 14.748 18 L 16.748 18 Z M 15.748 9 L 17.898 9 L 17.898 7 L 15.748 7 L 15.748 9 Z M 17.895 9 C 18.371 9.001 18.832 8.833 19.195 8.525 L 17.901 7 L 17.901 7 L 17.895 9 Z M 19.195 8.525 C 19.558 8.217 19.799 7.79 19.875 7.32 L 17.901 7 L 17.901 7 L 19.195 8.525 Z M 19.874 7.325 L 20.454 3.855 L 18.482 3.525 L 17.902 6.995 L 19.874 7.325 Z M 20.454 3.856 C 20.573 3.151 20.436 2.426 20.067 1.812 L 18.353 2.842 C 18.476 3.047 18.521 3.289 18.482 3.524 L 20.454 3.856 Z M 20.067 1.812 C 19.699 1.199 19.123 0.737 18.444 0.511 L 17.812 2.409 C 18.038 2.484 18.23 2.638 18.353 2.842 L 20.067 1.812 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2.252 2)\"/>"
    },
    "IconShoppingBag": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 3 0 L 3 -1 C 2.685 -1 2.389 -0.852 2.2 -0.6 L 3 0 Z M 0 4 L -0.8 3.4 C -0.93 3.573 -1 3.784 -1 4 L 0 4 Z M 0 18 L -1 18 L 0 18 Z M 18 4 L 19 4 C 19 3.784 18.93 3.573 18.8 3.4 L 18 4 Z M 15 0 L 15.8 -0.6 C 15.611 -0.852 15.315 -1 15 -1 L 15 0 Z M 2.2 -0.6 L -0.8 3.4 L 0.8 4.6 L 3.8 0.6 L 2.2 -0.6 Z M -1 4 L -1 18 L 1 18 L 1 4 L -1 4 Z M -1 18 C -1 18.796 -0.684 19.559 -0.121 20.121 L 1.293 18.707 C 1.105 18.52 1 18.265 1 18 L -1 18 Z M -0.121 20.121 C 0.441 20.684 1.204 21 2 21 L 2 19 C 1.735 19 1.48 18.895 1.293 18.707 L -0.121 20.121 Z M 2 21 L 16 21 L 16 19 L 2 19 L 2 21 Z M 16 21 C 16.796 21 17.559 20.684 18.121 20.121 L 16.707 18.707 C 16.52 18.895 16.265 19 16 19 L 16 21 Z M 18.121 20.121 C 18.684 19.559 19 18.796 19 18 L 17 18 C 17 18.265 16.895 18.52 16.707 18.707 L 18.121 20.121 Z M 19 18 L 19 4 L 17 4 L 17 18 L 19 18 Z M 18.8 3.4 L 15.8 -0.6 L 14.2 0.6 L 17.2 4.6 L 18.8 3.4 Z M 15 -1 L 3 -1 L 3 1 L 15 1 L 15 -1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3 2)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 18 1 C 18.552 1 19 0.552 19 0 C 19 -0.552 18.552 -1 18 -1 L 18 1 Z M 0 1 L 18 1 L 18 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 3 6)\"/><path d=\"M 9 0 C 9 -0.552 8.552 -1 8 -1 C 7.448 -1 7 -0.552 7 0 L 9 0 Z M 4 4 L 4 5 L 4 4 Z M 1 0 C 1 -0.552 0.552 -1 0 -1 C -0.552 -1 -1 -0.552 -1 0 L 1 0 Z M 7 0 C 7 0.796 6.684 1.559 6.121 2.121 L 7.536 3.536 C 8.473 2.598 9 1.326 9 0 L 7 0 Z M 6.121 2.121 C 5.559 2.684 4.796 3 4 3 L 4 5 C 5.326 5 6.598 4.473 7.536 3.536 L 6.121 2.121 Z M 4 3 C 3.204 3 2.441 2.684 1.879 2.121 L 0.464 3.536 C 1.402 4.473 2.674 5 4 5 L 4 3 Z M 1.879 2.121 C 1.316 1.559 1 0.796 1 0 L -1 0 C -1 1.326 -0.473 2.598 0.464 3.536 L 1.879 2.121 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 8 10)\"/>"
    },
    "IconShoppingCart": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 7 20)\"/><path d=\"M 1 1 L 1 3 C 2.105 3 3 2.105 3 1 L 1 1 Z M 1 1 L -1 1 C -1 2.105 -0.105 3 1 3 L 1 1 Z M 1 1 L 1 -1 C -0.105 -1 -1 -0.105 -1 1 L 1 1 Z M 1 1 L 3 1 C 3 -0.105 2.105 -1 1 -1 L 1 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 18 20)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 2 0 L 2.978 -0.209 C 2.879 -0.671 2.472 -1 2 -1 L 2 0 Z M 4.66 12.42 L 3.682 12.629 L 3.682 12.63 L 4.66 12.42 Z M 6.66 14 L 6.66 13 C 6.653 13 6.645 13 6.638 13 L 6.66 14 Z M 16.44 14 L 16.44 15 L 16.442 15 L 16.44 14 Z M 18.39 12.43 L 17.414 12.213 L 17.413 12.215 L 18.39 12.43 Z M 20.04 5 L 21.016 5.217 C 21.082 4.921 21.01 4.611 20.82 4.374 C 20.63 4.138 20.343 4 20.04 4 L 20.04 5 Z M 3.07 4 C 2.518 4 2.07 4.448 2.07 5 C 2.07 5.552 2.518 6 3.07 6 L 3.07 4 Z M 0 1 L 2 1 L 2 -1 L 0 -1 L 0 1 Z M 1.022 0.209 L 3.682 12.629 L 5.638 12.211 L 2.978 -0.209 L 1.022 0.209 Z M 3.682 12.63 C 3.829 13.312 4.208 13.922 4.756 14.355 L 5.996 12.785 C 5.813 12.641 5.687 12.438 5.638 12.21 L 3.682 12.63 Z M 4.756 14.355 C 5.303 14.787 5.985 15.015 6.682 15 L 6.638 13 C 6.405 13.005 6.178 12.929 5.996 12.785 L 4.756 14.355 Z M 6.66 15 L 16.44 15 L 16.44 13 L 6.66 13 L 6.66 15 Z M 16.442 15 C 17.124 14.999 17.786 14.765 18.318 14.337 L 17.064 12.779 C 16.887 12.922 16.666 13 16.438 13 L 16.442 15 Z M 18.318 14.337 C 18.85 13.909 19.22 13.312 19.367 12.645 L 17.413 12.215 C 17.364 12.437 17.241 12.636 17.064 12.779 L 18.318 14.337 Z M 19.366 12.647 L 21.016 5.217 L 19.064 4.783 L 17.414 12.213 L 19.366 12.647 Z M 20.04 4 L 3.07 4 L 3.07 6 L 20.04 6 L 20.04 4 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2.050 2.050)\"/>"
    },
    "IconStar": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 10 0 L 10.897 -0.443 C 10.728 -0.784 10.381 -1 10 -1 C 9.619 -1 9.272 -0.784 9.103 -0.443 L 10 0 Z M 13.09 6.26 L 12.193 6.703 C 12.339 6.997 12.62 7.202 12.945 7.249 L 13.09 6.26 Z M 20 7.27 L 20.698 7.986 C 20.97 7.721 21.069 7.324 20.951 6.962 C 20.834 6.6 20.521 6.336 20.145 6.281 L 20 7.27 Z M 15 12.14 L 14.302 11.424 C 14.066 11.653 13.959 11.985 14.014 12.309 L 15 12.14 Z M 16.18 19.02 L 15.715 19.905 C 16.051 20.082 16.46 20.053 16.768 19.829 C 17.076 19.605 17.23 19.226 17.166 18.851 L 16.18 19.02 Z M 10 15.77 L 10.465 14.885 C 10.174 14.732 9.826 14.732 9.535 14.885 L 10 15.77 Z M 3.82 19.02 L 2.834 18.851 C 2.77 19.226 2.924 19.605 3.232 19.829 C 3.54 20.053 3.949 20.082 4.285 19.905 L 3.82 19.02 Z M 5 12.14 L 5.986 12.309 C 6.041 11.985 5.934 11.653 5.698 11.424 L 5 12.14 Z M 0 7.27 L -0.145 6.281 C -0.521 6.336 -0.834 6.6 -0.951 6.962 C -1.069 7.324 -0.97 7.721 -0.698 7.986 L 0 7.27 Z M 6.91 6.26 L 7.055 7.249 C 7.38 7.202 7.661 6.997 7.807 6.703 L 6.91 6.26 Z M 9.103 0.443 L 12.193 6.703 L 13.987 5.817 L 10.897 -0.443 L 9.103 0.443 Z M 12.945 7.249 L 19.855 8.259 L 20.145 6.281 L 13.235 5.271 L 12.945 7.249 Z M 19.302 6.554 L 14.302 11.424 L 15.698 12.856 L 20.698 7.986 L 19.302 6.554 Z M 14.014 12.309 L 15.194 19.189 L 17.166 18.851 L 15.986 11.971 L 14.014 12.309 Z M 16.645 18.135 L 10.465 14.885 L 9.535 16.655 L 15.715 19.905 L 16.645 18.135 Z M 9.535 14.885 L 3.355 18.135 L 4.285 19.905 L 10.465 16.655 L 9.535 14.885 Z M 4.806 19.189 L 5.986 12.309 L 4.014 11.971 L 2.834 18.851 L 4.806 19.189 Z M 5.698 11.424 L 0.698 6.554 L -0.698 7.986 L 4.302 12.856 L 5.698 11.424 Z M 0.145 8.259 L 7.055 7.249 L 6.765 5.271 L -0.145 6.281 L 0.145 8.259 Z M 7.807 6.703 L 10.897 0.443 L 9.103 -0.443 L 6.013 5.817 L 7.807 6.703 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 2)\"/>"
    },
    "IconTruck": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 8 11 C 7.448 11 7 11.448 7 12 C 7 12.552 7.448 13 8 13 L 8 11 Z M 12 12 L 12 13 C 12.552 13 13 12.552 13 12 L 12 12 Z M 12 0 L 13 0 C 13 -0.552 12.552 -1 12 -1 L 12 0 Z M 0 0 L 0 -1 C -0.552 -1 -1 -0.552 -1 0 L 0 0 Z M 0 12 L -1 12 C -1 12.552 -0.552 13 0 13 L 0 12 Z M 3 13 C 3.552 13 4 12.552 4 12 C 4 11.448 3.552 11 3 11 L 3 13 Z M 8 13 L 12 13 L 12 11 L 8 11 L 8 13 Z M 13 12 L 13 0 L 11 0 L 11 12 L 13 12 Z M 12 -1 L 0 -1 L 0 1 L 12 1 L 12 -1 Z M -1 0 L -1 12 L 1 12 L 1 0 L -1 0 Z M 0 13 L 3 13 L 3 11 L 0 11 L 0 13 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 2 5)\"/><path d=\"M 6 7 C 5.448 7 5 7.448 5 8 C 5 8.552 5.448 9 6 9 L 6 7 Z M 8 8 L 8 9 C 8.552 9 9 8.552 9 8 L 8 8 Z M 8 4.66 L 7 4.659 L 7 4.66 L 8 4.66 Z M 6.83 1.83 L 7.537 1.123 L 7.537 1.123 L 6.83 1.83 Z M 5 0 L 5.707 -0.707 C 5.52 -0.895 5.265 -1 5 -1 L 5 0 Z M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 6 9 L 8 9 L 8 7 L 6 7 L 6 9 Z M 9 8 L 9 4.66 L 7 4.66 L 7 8 L 9 8 Z M 9 4.661 C 9.001 4.004 8.872 3.354 8.621 2.747 L 6.772 3.511 C 6.923 3.875 7 4.265 7 4.659 L 9 4.661 Z M 8.621 2.747 C 8.37 2.14 8.002 1.588 7.537 1.123 L 6.123 2.537 C 6.401 2.816 6.622 3.146 6.772 3.511 L 8.621 2.747 Z M 7.537 1.123 L 5.707 -0.707 L 4.293 0.707 L 6.123 2.537 L 7.537 1.123 Z M 5 -1 L 0 -1 L 0 1 L 5 1 L 5 -1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 14 9)\"/><path d=\"M 0 -1 C -0.552 -1 -1 -0.552 -1 0 C -1 0.552 -0.552 1 0 1 L 0 -1 Z M 1 1 C 1.552 1 2 0.552 2 0 C 2 -0.552 1.552 -1 1 -1 L 1 1 Z M 0 1 L 1 1 L 1 -1 L 0 -1 L 0 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 14 17)\"/><path d=\"M 4 2.5 C 4 3.328 3.328 4 2.5 4 L 2.5 6 C 4.433 6 6 4.433 6 2.5 L 4 2.5 Z M 2.5 4 C 1.672 4 1 3.328 1 2.5 L -1 2.5 C -1 4.433 0.567 6 2.5 6 L 2.5 4 Z M 1 2.5 C 1 1.672 1.672 1 2.5 1 L 2.5 -1 C 0.567 -1 -1 0.567 -1 2.5 L 1 2.5 Z M 2.5 1 C 3.328 1 4 1.672 4 2.5 L 6 2.5 C 6 0.567 4.433 -1 2.5 -1 L 2.5 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 5 15)\"/><path d=\"M 4 2.5 C 4 3.328 3.328 4 2.5 4 L 2.5 6 C 4.433 6 6 4.433 6 2.5 L 4 2.5 Z M 2.5 4 C 1.672 4 1 3.328 1 2.5 L -1 2.5 C -1 4.433 0.567 6 2.5 6 L 2.5 4 Z M 1 2.5 C 1 1.672 1.672 1 2.5 1 L 2.5 -1 C 0.567 -1 -1 0.567 -1 2.5 L 1 2.5 Z M 2.5 1 C 3.328 1 4 1.672 4 2.5 L 6 2.5 C 6 0.567 4.433 -1 2.5 -1 L 2.5 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 15 15)\"/>"
    },
    "IconUser": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 13 6 C 13 6.552 13.448 7 14 7 C 14.552 7 15 6.552 15 6 L 13 6 Z M 10 0 L 10 -1 L 10 0 Z M 4 0 L 4 -1 L 4 0 Z M 0 4 L -1 4 L 0 4 Z M -1 6 C -1 6.552 -0.552 7 0 7 C 0.552 7 1 6.552 1 6 L -1 6 Z M 15 6 L 15 4 L 13 4 L 13 6 L 15 6 Z M 15 4 C 15 2.674 14.473 1.402 13.536 0.464 L 12.121 1.879 C 12.684 2.441 13 3.204 13 4 L 15 4 Z M 13.536 0.464 C 12.598 -0.473 11.326 -1 10 -1 L 10 1 C 10.796 1 11.559 1.316 12.121 1.879 L 13.536 0.464 Z M 10 -1 L 4 -1 L 4 1 L 10 1 L 10 -1 Z M 4 -1 C 2.674 -1 1.402 -0.473 0.464 0.464 L 1.879 1.879 C 2.441 1.316 3.204 1 4 1 L 4 -1 Z M 0.464 0.464 C -0.473 1.402 -1 2.674 -1 4 L 1 4 C 1 3.204 1.316 2.441 1.879 1.879 L 0.464 0.464 Z M -1 4 L -1 6 L 1 6 L 1 4 L -1 4 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 5 15)\"/><path d=\"M 7 4 C 7 5.657 5.657 7 4 7 L 4 9 C 6.761 9 9 6.761 9 4 L 7 4 Z M 4 7 C 2.343 7 1 5.657 1 4 L -1 4 C -1 6.761 1.239 9 4 9 L 4 7 Z M 1 4 C 1 2.343 2.343 1 4 1 L 4 -1 C 1.239 -1 -1 1.239 -1 4 L 1 4 Z M 4 1 C 5.657 1 7 2.343 7 4 L 9 4 C 9 1.239 6.761 -1 4 -1 L 4 1 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 8 3)\"/>"
    },
    "IconX": {
      viewBox: "0 0 24 24",
      body: "<path d=\"M 12.707 0.707 C 13.098 0.317 13.098 -0.317 12.707 -0.707 C 12.317 -1.098 11.683 -1.098 11.293 -0.707 L 12.707 0.707 Z M -0.707 11.293 C -1.098 11.683 -1.098 12.317 -0.707 12.707 C -0.317 13.098 0.317 13.098 0.707 12.707 L -0.707 11.293 Z M 11.293 -0.707 L -0.707 11.293 L 0.707 12.707 L 12.707 0.707 L 11.293 -0.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 6 6)\"/><path d=\"M 0.707 -0.707 C 0.317 -1.098 -0.317 -1.098 -0.707 -0.707 C -1.098 -0.317 -1.098 0.317 -0.707 0.707 L 0.707 -0.707 Z M 11.293 12.707 C 11.683 13.098 12.317 13.098 12.707 12.707 C 13.098 12.317 13.098 11.683 12.707 11.293 L 11.293 12.707 Z M -0.707 0.707 L 11.293 12.707 L 12.707 11.293 L 0.707 -0.707 L -0.707 0.707 Z\" fill=\"currentColor\" fill-rule=\"nonzero\" transform=\"matrix(1 0 0 1 6 6)\"/>"
    }
  };
} catch {}
Object.assign(__ds_scope, { __ds_default_assets_icons_icon_data_imae2q });
})(); } catch (e) { __ds_ns.__errors.push({ path: "assets/icons/icon-data.js", error: String((e && e.message) || e) }); }

__ds_scope.__ds_default_assets_icons_icon_data_imae2q$nab192 = __ds_scope.__ds_default_assets_icons_icon_data_imae2q;

// assets/icons/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Icon({
  name,
  size,
  ...rest
}) {
  const d = __ds_scope.__ds_default_assets_icons_icon_data_imae2q$nab192[name];
  if (!d) return null;
  return /*#__PURE__*/React.createElement("svg", _extends({
    width: size,
    height: size,
    viewBox: d.viewBox,
    fill: "none"
    // body strings are emitter-controlled <path> markup — geometry,
    // numeric fills and transforms only; no .fig-authored text reaches them.
    ,
    dangerouslySetInnerHTML: {
      __html: d.body
    }
  }, rest));
}
Object.assign(__ds_scope, { Icon, __ds_default_assets_icons_Icon_2pj96j: Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "assets/icons/Icon.jsx", error: String((e && e.message) || e) }); }

// components/data-display/AspectRatio.jsx
try { (() => {
function AspectRatio({
  ratio = 16 / 9,
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: "100%",
      paddingTop: `${100 / ratio}%`,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0
    }
  }, children));
}
Object.assign(__ds_scope, { AspectRatio });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/AspectRatio.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Avatar.jsx
try { (() => {
function Avatar({
  src,
  initials,
  size = 40,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: size,
      height: size,
      borderRadius: "50%",
      overflow: "hidden",
      background: "var(--slate-200)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontFamily: "var(--font-sans)",
      fontWeight: 500,
      fontSize: size * 0.35,
      color: "var(--fg-muted)",
      flexShrink: 0,
      ...style
    }
  }, src ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: "",
    style: {
      width: "100%",
      height: "100%",
      objectFit: "cover"
    }
  }) : initials);
}
Object.assign(__ds_scope, { Avatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Card.jsx
try { (() => {
function Card({
  title,
  footer,
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: "var(--radius-lg)",
      background: "var(--bg-surface)",
      boxShadow: "inset 0 0 0 1px var(--border-default)",
      padding: 24,
      display: "flex",
      flexDirection: "column",
      gap: 16,
      ...style
    }
  }, title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontWeight: 600,
      fontSize: 18,
      color: "var(--fg-default)"
    }
  }, title), /*#__PURE__*/React.createElement("div", null, children), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 8
    }
  }, footer));
}
Object.assign(__ds_scope, { Card });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Card.jsx", error: String((e && e.message) || e) }); }

// components/data-display/InlineCode.jsx
try { (() => {
function InlineCode({
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("code", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 13,
      fontWeight: 700,
      background: "var(--bg-subtle)",
      borderRadius: "var(--radius-xs)",
      padding: "3px 6px",
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { InlineCode });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/InlineCode.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Palette.jsx
try { (() => {
function Palette({
  shades = []
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      height: 80
    }
  }, shades.map(s => /*#__PURE__*/React.createElement("div", {
    key: s.value,
    style: {
      flex: 1,
      background: s.hex,
      display: "flex",
      alignItems: "flex-end",
      padding: 6,
      fontSize: 11,
      fontFamily: "var(--font-sans)",
      color: s.value >= 500 ? "#fff" : "var(--fg-default)"
    }
  }, s.value)));
}
Object.assign(__ds_scope, { Palette });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Palette.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Poster.jsx
try { (() => {
function Poster({
  title,
  description,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: 188,
      height: 270,
      borderRadius: "var(--radius-sm)",
      background: "linear-gradient(180deg,var(--brand-400),var(--brand-700))",
      display: "flex",
      alignItems: "flex-end",
      padding: 24,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      color: "#fff"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 500,
      fontSize: 18,
      marginBottom: 8
    }
  }, title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      lineHeight: "18px",
      opacity: 0.9
    }
  }, description)));
}
Object.assign(__ds_scope, { Poster });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Poster.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Progress.jsx
try { (() => {
function Progress({
  value = 0,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "100%",
      height: 8,
      borderRadius: "var(--radius-full)",
      background: "var(--slate-200)",
      overflow: "hidden",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: "100%",
      width: `${Math.max(0, Math.min(100, value))}%`,
      background: "var(--primary)",
      borderRadius: "var(--radius-full)",
      transition: "width var(--duration-base) var(--ease-standard)"
    }
  }));
}
Object.assign(__ds_scope, { Progress });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Progress.jsx", error: String((e && e.message) || e) }); }

// components/data-display/ScrollArea.jsx
try { (() => {
function ScrollArea({
  children,
  height = 200,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height,
      overflowY: "auto",
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { ScrollArea });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/ScrollArea.jsx", error: String((e && e.message) || e) }); }

// components/data-display/ScrollListItem.jsx
try { (() => {
function ScrollListItem({
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 0",
      borderBottom: "1px solid var(--border-default)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)"
    }
  }, children);
}
Object.assign(__ds_scope, { ScrollListItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/ScrollListItem.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Separator.jsx
try { (() => {
function Separator({
  orientation = "horizontal",
  style
}) {
  return orientation === "vertical" ? /*#__PURE__*/React.createElement("div", {
    style: {
      width: 1,
      alignSelf: "stretch",
      background: "var(--border-default)",
      ...style
    }
  }) : /*#__PURE__*/React.createElement("div", {
    style: {
      height: 1,
      width: "100%",
      background: "var(--border-default)",
      ...style
    }
  });
}
Object.assign(__ds_scope, { Separator });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Separator.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Slider.jsx
try { (() => {
function Slider({
  value = 50,
  min = 0,
  max = 100,
  onChange,
  style
}) {
  return /*#__PURE__*/React.createElement("input", {
    type: "range",
    value: value,
    min: min,
    max: max,
    onChange: e => onChange && onChange(Number(e.target.value)),
    style: {
      width: "100%",
      accentColor: "var(--primary)",
      ...style
    }
  });
}
Object.assign(__ds_scope, { Slider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Slider.jsx", error: String((e && e.message) || e) }); }

// components/data-display/TabCard.jsx
try { (() => {
function TabCard({
  description,
  children,
  footer
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: 400,
      borderRadius: "var(--radius-md)",
      background: "#fff",
      boxShadow: "inset 0 0 0 1px var(--border-default)",
      padding: 25,
      display: "flex",
      flexDirection: "column",
      gap: 16
    }
  }, description && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)"
    }
  }, description), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 8
    }
  }, children), footer);
}
Object.assign(__ds_scope, { TabCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/TabCard.jsx", error: String((e && e.message) || e) }); }

// components/data-display/Table.jsx
try { (() => {
function Table({
  columns = [],
  rows = [],
  style
}) {
  return /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      borderCollapse: "collapse",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      ...style
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map(c => /*#__PURE__*/React.createElement("th", {
    key: c,
    style: {
      textAlign: "left",
      padding: "10px 12px",
      borderBottom: "1px solid var(--border-default)",
      color: "var(--fg-muted)",
      fontWeight: 500
    }
  }, c)))), /*#__PURE__*/React.createElement("tbody", null, rows.map((r, i) => /*#__PURE__*/React.createElement("tr", {
    key: i,
    style: {
      background: r.selected ? "var(--bg-muted)" : "transparent"
    }
  }, columns.map(c => /*#__PURE__*/React.createElement("td", {
    key: c,
    style: {
      padding: "10px 12px",
      borderBottom: "1px solid var(--border-default)",
      color: "var(--fg-default)"
    }
  }, r[c]))))));
}
Object.assign(__ds_scope, { Table });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/Table.jsx", error: String((e && e.message) || e) }); }

// components/data-display/TableItem.jsx
try { (() => {
function TableItem({
  cells = [],
  head,
  selected
}) {
  const Tag = head ? "th" : "td";
  return /*#__PURE__*/React.createElement("tr", {
    style: {
      background: selected ? "var(--bg-muted)" : "transparent"
    }
  }, cells.map((c, i) => /*#__PURE__*/React.createElement(Tag, {
    key: i,
    style: {
      textAlign: "left",
      padding: "10px 12px",
      borderBottom: "1px solid var(--border-default)",
      color: head ? "var(--fg-muted)" : "var(--fg-default)",
      fontWeight: head ? 500 : 400
    }
  }, c)));
}
Object.assign(__ds_scope, { TableItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data-display/TableItem.jsx", error: String((e && e.message) || e) }); }

// components/forms/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const base = {
  fontFamily: "var(--font-sans)",
  fontWeight: 500,
  fontSize: 14,
  lineHeight: "24px",
  borderRadius: "var(--radius-md)",
  border: "none",
  cursor: "pointer",
  display: "inline-flex",
  alignItems: "center",
  justifyContent: "center",
  gap: 8,
  transition: "background-color var(--duration-fast) var(--ease-standard),opacity var(--duration-fast)"
};
const sizes = {
  default: {
    padding: "8px 16px",
    height: 40
  },
  sm: {
    padding: "6px 12px",
    height: 36,
    fontSize: 13
  },
  lg: {
    padding: "10px 24px",
    height: 44
  },
  icon: {
    padding: 0,
    width: 40,
    height: 40
  }
};
const variants = {
  primary: {
    backgroundColor: "var(--primary)",
    color: "var(--primary-fg)"
  },
  secondary: {
    backgroundColor: "var(--secondary)",
    color: "var(--secondary-fg)"
  },
  destructive: {
    backgroundColor: "var(--destructive)",
    color: "#fff"
  },
  outline: {
    backgroundColor: "transparent",
    color: "var(--fg-default)",
    boxShadow: "inset 0 0 0 1px var(--border-strong)"
  },
  subtle: {
    backgroundColor: "var(--bg-subtle)",
    color: "var(--fg-default)"
  },
  ghost: {
    backgroundColor: "transparent",
    color: "var(--fg-default)"
  },
  link: {
    backgroundColor: "transparent",
    color: "var(--primary)",
    padding: 0,
    height: "auto",
    textDecoration: "underline",
    textDecorationColor: "transparent"
  }
};
function Button({
  variant = "primary",
  size = "default",
  disabled,
  loading,
  children,
  style,
  ...props
}) {
  const v = variants[variant] || variants.primary,
    s = sizes[size] || sizes.default;
  return /*#__PURE__*/React.createElement("button", _extends({
    disabled: disabled || loading,
    style: {
      ...base,
      ...s,
      ...v,
      opacity: disabled ? 0.5 : 1,
      ...style
    }
  }, props), loading && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 14,
      height: 14,
      borderRadius: "50%",
      border: "2px solid currentColor",
      borderTopColor: "transparent",
      animation: "ds-spin .6s linear infinite"
    }
  }), children);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Button.jsx", error: String((e && e.message) || e) }); }

// components/forms/Checkbox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Checkbox({
  checked,
  onChange,
  label,
  description,
  id,
  style,
  ...props
}) {
  const boxId = id || React.useId();
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 10,
      alignItems: description ? "flex-start" : "center",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: { position: "relative", width: 18, height: 18, flexShrink: 0, marginTop: description ? 1 : 0 }
  },
  /*#__PURE__*/React.createElement("input", _extends({
    id: boxId,
    type: "checkbox",
    checked: checked,
    onChange: onChange
  }, props, {
    style: { position: "absolute", inset: 0, width: "100%", height: "100%", margin: 0, opacity: 0, cursor: "pointer" }
  })),
  /*#__PURE__*/React.createElement("div", {
    style: {
      width: 18,
      height: 18,
      borderRadius: "var(--radius-xs)",
      boxShadow: checked ? "none" : "inset 0 0 0 1.5px var(--border-strong)",
      background: checked ? "var(--primary)" : "var(--white)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      transition: "background 120ms, box-shadow 120ms",
      pointerEvents: "none"
    }
  }, checked && /*#__PURE__*/React.createElement("svg", { width: 12, height: 12, viewBox: "0 0 24 24", fill: "none" },
    /*#__PURE__*/React.createElement("path", { d: "M4 12.5L9.5 18L20 6", stroke: "white", strokeWidth: 3, strokeLinecap: "round", strokeLinejoin: "round" })
  ))
  ), label && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("label", {
    htmlFor: boxId,
    style: {
      fontFamily: "var(--font-sans)",
      fontWeight: 500,
      fontSize: 14,
      lineHeight: "14px",
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, label), description && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontWeight: 400,
      fontSize: 14,
      lineHeight: "20px",
      color: "var(--fg-muted)",
      marginTop: 4
    }
  }, description)));
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/forms/Input.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Input({
  size = "default",
  style,
  onFocus,
  onBlur,
  ...props
}) {
  const [focused, setFocused] = React.useState(false);
  const h = size === "sm" ? 36 : 40;
  return /*#__PURE__*/React.createElement("input", _extends({
    onFocus: e => { setFocused(true); onFocus && onFocus(e); },
    onBlur: e => { setFocused(false); onBlur && onBlur(e); },
    style: {
      height: h,
      width: "100%",
      borderRadius: 8,
      border: focused ? "1px solid var(--primary)" : "1px solid var(--border-default)",
      boxShadow: "none",
      background: "var(--white)",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      color: "var(--fg-default)",
      padding: "9px 14px",
      outline: "none",
      transition: "box-shadow 120ms",
      ...style
    }
  }, props));
}
Object.assign(__ds_scope, { Input });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Input.jsx", error: String((e && e.message) || e) }); }

// components/forms/Label.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Label({
  children,
  style,
  ...props
}) {
  return /*#__PURE__*/React.createElement("label", _extends({
    style: {
      fontFamily: "var(--font-sans)",
      fontWeight: 500,
      fontSize: 14,
      lineHeight: "14px",
      color: "var(--fg-default)",
      display: "block",
      marginBottom: 6,
      ...style
    }
  }, props), children);
}
Object.assign(__ds_scope, { Label });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Label.jsx", error: String((e && e.message) || e) }); }

// components/forms/RadioButton.jsx
try { (() => {
function RadioButton({
  checked,
  onChange,
  label
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: "flex",
      gap: 10,
      alignItems: "center",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("div", { style: { position: "relative", width: 18, height: 18, flexShrink: 0 } },
    /*#__PURE__*/React.createElement("input", {
      type: "radio",
      checked: checked,
      onChange: onChange,
      style: { position: "absolute", inset: 0, width: "100%", height: "100%", margin: 0, opacity: 0, cursor: "pointer" }
    }),
    /*#__PURE__*/React.createElement("div", {
      style: {
        width: 18, height: 18, borderRadius: "50%",
        boxShadow: checked ? "none" : "inset 0 0 0 1.5px var(--border-strong)",
        background: checked ? "var(--primary)" : "var(--white)",
        display: "flex", alignItems: "center", justifyContent: "center", pointerEvents: "none"
      }
    }, checked && /*#__PURE__*/React.createElement("div", { style: { width: 6, height: 6, borderRadius: "50%", background: "var(--white)" } }))
  ), label);
}
Object.assign(__ds_scope, { RadioButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/RadioButton.jsx", error: String((e && e.message) || e) }); }

// components/forms/RadioGroup.jsx
try { (() => {
function Item({
  name,
  value,
  label,
  checked,
  onChange
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: "flex",
      gap: 10,
      alignItems: "center",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("div", { style: { position: "relative", width: 18, height: 18, flexShrink: 0 } },
    /*#__PURE__*/React.createElement("input", {
      type: "radio",
      name: name,
      value: value,
      checked: checked,
      onChange: onChange,
      style: { position: "absolute", inset: 0, width: "100%", height: "100%", margin: 0, opacity: 0, cursor: "pointer" }
    }),
    /*#__PURE__*/React.createElement("div", {
      style: {
        width: 18, height: 18, borderRadius: "50%",
        boxShadow: checked ? "none" : "inset 0 0 0 1.5px var(--border-strong)",
        background: checked ? "var(--primary)" : "var(--white)",
        display: "flex", alignItems: "center", justifyContent: "center", pointerEvents: "none"
      }
    }, checked && /*#__PURE__*/React.createElement("div", { style: { width: 6, height: 6, borderRadius: "50%", background: "var(--white)" } }))
  ), label);
}
function RadioGroup({
  name = "rg",
  options = [],
  value,
  onChange,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    role: "radiogroup",
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 10,
      ...style
    }
  }, options.map(o => /*#__PURE__*/React.createElement(Item, {
    key: o.value,
    name: name,
    value: o.value,
    label: o.label,
    checked: value === o.value,
    onChange: () => onChange && onChange(o.value)
  })));
}
Object.assign(__ds_scope, { RadioGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/RadioGroup.jsx", error: String((e && e.message) || e) }); }

// components/forms/Select.jsx
try { (() => {
function Select({
  options = [],
  value,
  onChange,
  placeholder = "Select…",
  style
}) {
  const [open, setOpen] = React.useState(false);
  const sel = options.find(o => o.value === value);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: "100%",
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setOpen(!open),
    style: {
      width: "100%",
      height: 40,
      borderRadius: 8,
      border: open ? "1px solid var(--primary)" : "1px solid var(--border-default)",
      boxShadow: "none",
      background: "#fff",
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      padding: "9px 14px",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      color: sel ? "var(--fg-default)" : "var(--fg-subtle)",
      cursor: "pointer",
      transition: "box-shadow 120ms"
    }
  }, sel ? sel.label : placeholder, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "IconChevronDown",
    size: 16,
    style: {
      color: "var(--fg-muted)"
    }
  })), open && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      left: 0,
      right: 0,
      background: "#fff",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-popover)",
      padding: 4,
      zIndex: 10
    }
  }, options.map(o => /*#__PURE__*/React.createElement("div", {
    key: o.value,
    onClick: () => {
      onChange && onChange(o.value);
      setOpen(false);
    },
    style: {
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      cursor: "pointer",
      background: o.value === value ? "var(--bg-subtle)" : "transparent"
    }
  }, o.label))));
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Select.jsx", error: String((e && e.message) || e) }); }

// components/forms/Switch.jsx
try { (() => {
function Switch({
  checked,
  onChange,
  style
}) {
  return /*#__PURE__*/React.createElement("button", {
    role: "switch",
    "aria-checked": checked,
    onClick: () => onChange && onChange(!checked),
    style: {
      width: 36,
      height: 20,
      borderRadius: "var(--radius-full)",
      border: "none",
      padding: 2,
      background: checked ? "var(--primary)" : "var(--slate-200)",
      cursor: "pointer",
      display: "inline-flex",
      transition: "background var(--duration-fast)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 16,
      height: 16,
      borderRadius: "50%",
      background: "#fff",
      boxShadow: "var(--shadow-sm)",
      transform: checked ? "translateX(16px)" : "translateX(0)",
      transition: "transform var(--duration-fast) var(--ease-standard)"
    }
  }));
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Switch.jsx", error: String((e && e.message) || e) }); }

// components/forms/Textarea.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Textarea({
  style,
  rows = 4,
  onFocus,
  onBlur,
  ...props
}) {
  const [focused, setFocused] = React.useState(false);
  return /*#__PURE__*/React.createElement("textarea", _extends({
    rows: rows,
    onFocus: e => { setFocused(true); onFocus && onFocus(e); },
    onBlur: e => { setFocused(false); onBlur && onBlur(e); },
    style: {
      width: "100%",
      borderRadius: 8,
      border: focused ? "1px solid var(--primary)" : "1px solid var(--border-default)",
      boxShadow: "none",
      background: "var(--white)",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      color: "var(--fg-default)",
      padding: "9px 14px",
      outline: "none",
      resize: "vertical",
      transition: "box-shadow 120ms",
      ...style
    }
  }, props));
}
Object.assign(__ds_scope, { Textarea });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Textarea.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Accordion.jsx
try { (() => {
function Accordion({
  items = [],
  style
}) {
  const [open, setOpen] = React.useState(null);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      ...style
    }
  }, items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      borderBottom: "1px solid var(--border-default)"
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setOpen(open === i ? null : i),
    style: {
      width: "100%",
      background: "none",
      border: "none",
      display: "flex",
      justifyContent: "space-between",
      alignItems: "center",
      padding: "16px 0",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, it.title, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "IconChevronDown",
    size: 16,
    style: {
      transform: open === i ? "rotate(180deg)" : "none",
      transition: "transform var(--duration-fast)"
    }
  })), open === i && /*#__PURE__*/React.createElement("div", {
    style: {
      paddingBottom: 16,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-muted)"
    }
  }, it.content))));
}
Object.assign(__ds_scope, { Accordion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Accordion.jsx", error: String((e && e.message) || e) }); }

// components/navigation/AccordionItem.jsx
try { (() => {
function AccordionItem({
  title,
  content,
  open,
  onToggle
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderBottom: "1px solid var(--border-default)"
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: onToggle,
    style: {
      width: "100%",
      background: "none",
      border: "none",
      display: "flex",
      justifyContent: "space-between",
      alignItems: "center",
      padding: "16px 0",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, title, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "IconChevronDown",
    size: 16,
    style: {
      transform: open ? "rotate(180deg)" : "none",
      transition: "transform var(--duration-fast)"
    }
  })), open && /*#__PURE__*/React.createElement("div", {
    style: {
      paddingBottom: 16,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-muted)"
    }
  }, content));
}
Object.assign(__ds_scope, { AccordionItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/AccordionItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Collapsible.jsx
try { (() => {
function Collapsible({
  title,
  open,
  onToggle,
  children
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("button", {
    onClick: onToggle,
    style: {
      background: "none",
      border: "none",
      display: "flex",
      alignItems: "center",
      gap: 8,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      color: "var(--fg-default)",
      cursor: "pointer",
      padding: 0
    }
  }, title), open && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8
    }
  }, children));
}
Object.assign(__ds_scope, { Collapsible });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Collapsible.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Command.jsx
try { (() => {
function Command({
  items = [],
  onSelect,
  placeholder = "Type a command or search…"
}) {
  const [q, setQ] = React.useState("");
  const filtered = items.filter(g => true).map(g => ({
    ...g,
    items: g.items.filter(i => i.label.toLowerCase().includes(q.toLowerCase()))
  }));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: 420,
      background: "#fff",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--shadow-lg)",
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 8,
      padding: "12px 16px",
      borderBottom: "1px solid var(--border-default)"
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "IconSearch",
    size: 16,
    style: {
      color: "var(--fg-muted)"
    }
  }), /*#__PURE__*/React.createElement("input", {
    value: q,
    onChange: e => setQ(e.target.value),
    placeholder: placeholder,
    style: {
      border: "none",
      outline: "none",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      flex: 1
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 8,
      maxHeight: 280,
      overflowY: "auto"
    }
  }, filtered.map((g, gi) => /*#__PURE__*/React.createElement("div", {
    key: gi,
    style: {
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 500,
      color: "var(--fg-muted)",
      padding: "4px 8px"
    }
  }, g.section), g.items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    onClick: () => onSelect && onSelect(it.value),
    style: {
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      cursor: "pointer"
    }
  }, it.label))))));
}
Object.assign(__ds_scope, { Command });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Command.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ContextMenu.jsx
try { (() => {
function ContextMenu({
  children,
  items = []
}) {
  const [pos, setPos] = React.useState(null);
  return /*#__PURE__*/React.createElement("div", {
    onContextMenu: e => {
      e.preventDefault();
      setPos({
        x: e.nativeEvent.offsetX,
        y: e.nativeEvent.offsetY
      });
    },
    onClick: () => setPos(null),
    style: {
      position: "relative"
    }
  }, children, pos && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      left: pos.x,
      top: pos.y,
      minWidth: 180,
      background: "#fff",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-popover)",
      padding: 4,
      zIndex: 20
    }
  }, items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, it.label))));
}
Object.assign(__ds_scope, { ContextMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ContextMenu.jsx", error: String((e && e.message) || e) }); }

// components/navigation/DropdownMenu.jsx
try { (() => {
function DropdownMenu({
  trigger,
  items = [],
  open,
  onToggle,
  onSelect
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      display: "inline-block"
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: onToggle
  }, trigger), open && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      left: 0,
      minWidth: 180,
      background: "#fff",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-popover)",
      padding: 4,
      zIndex: 20
    }
  }, items.map((it, i) => it.divider ? /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      height: 1,
      background: "var(--border-default)",
      margin: "4px 0"
    }
  }) : /*#__PURE__*/React.createElement("div", {
    key: i,
    onClick: () => onSelect && onSelect(it.value),
    style: {
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: it.destructive ? "var(--destructive)" : "var(--fg-default)",
      cursor: "pointer",
      display: "flex",
      justifyContent: "space-between"
    }
  }, /*#__PURE__*/React.createElement("span", null, it.label), it.shortcut && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--fg-subtle)",
      fontSize: 12
    }
  }, it.shortcut)))));
}
Object.assign(__ds_scope, { DropdownMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/DropdownMenu.jsx", error: String((e && e.message) || e) }); }

// components/navigation/MenuItem.jsx
try { (() => {
function MenuItem({
  label,
  shortcut,
  icon,
  selected,
  disabled,
  onClick
}) {
  return /*#__PURE__*/React.createElement("div", {
    onClick: disabled ? undefined : onClick,
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: disabled ? "var(--fg-subtle)" : "var(--fg-default)",
      background: selected ? "var(--bg-subtle)" : "transparent",
      cursor: disabled ? "default" : "pointer",
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 8
    }
  }, icon && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 14
  }), label), shortcut && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--fg-subtle)",
      fontSize: 12
    }
  }, shortcut));
}
Object.assign(__ds_scope, { MenuItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/MenuItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/MenuSectionTitle.jsx
try { (() => {
function MenuSectionTitle({
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "6px 8px",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 500,
      color: "var(--fg-muted)"
    }
  }, children);
}
Object.assign(__ds_scope, { MenuSectionTitle });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/MenuSectionTitle.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Menubar.jsx
try { (() => {
function Menubar({
  menus = []
}) {
  const [openIdx, setOpenIdx] = React.useState(null);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 2,
      background: "#fff",
      boxShadow: "inset 0 0 0 1px var(--border-default)",
      borderRadius: "var(--radius-md)",
      padding: 2,
      width: "fit-content"
    }
  }, menus.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      position: "relative"
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setOpenIdx(openIdx === i ? null : i),
    style: {
      border: "none",
      background: openIdx === i ? "var(--bg-subtle)" : "transparent",
      borderRadius: "var(--radius-sm)",
      padding: "6px 12px",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, m.label), openIdx === i && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      left: 0,
      minWidth: 180,
      background: "#fff",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-popover)",
      padding: 4,
      zIndex: 20
    }
  }, m.items.map((it, j) => /*#__PURE__*/React.createElement("div", {
    key: j,
    style: {
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      cursor: "pointer"
    }
  }, it))))));
}
Object.assign(__ds_scope, { Menubar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Menubar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/MenubarItem.jsx
try { (() => {
function MenubarItem({
  label,
  onClick
}) {
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      border: "none",
      background: "transparent",
      borderRadius: "var(--radius-sm)",
      padding: "6px 12px",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, label);
}
Object.assign(__ds_scope, { MenubarItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/MenubarItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/NavigationMenu.jsx
try { (() => {
function NavigationMenu({
  items = []
}) {
  const [openIdx, setOpenIdx] = React.useState(null);
  return /*#__PURE__*/React.createElement("nav", {
    style: {
      display: "flex",
      gap: 4
    }
  }, items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      position: "relative"
    },
    onMouseEnter: () => it.content && setOpenIdx(i),
    onMouseLeave: () => setOpenIdx(null)
  }, /*#__PURE__*/React.createElement("a", {
    href: it.href || "#",
    style: {
      display: "block",
      padding: "8px 12px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      color: "var(--fg-default)",
      textDecoration: "none"
    }
  }, it.label), openIdx === i && it.content && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      top: "100%",
      left: 0,
      minWidth: 320,
      background: "#fff",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--shadow-popover)",
      padding: 16,
      display: "grid",
      gap: 8,
      zIndex: 20
    }
  }, it.content.map((c, j) => /*#__PURE__*/React.createElement("a", {
    key: j,
    href: c.href || "#",
    style: {
      textDecoration: "none",
      color: "var(--fg-default)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 500,
      fontSize: 14
    }
  }, c.label), c.description && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: "var(--fg-muted)"
    }
  }, c.description)))))));
}
Object.assign(__ds_scope, { NavigationMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/NavigationMenu.jsx", error: String((e && e.message) || e) }); }

// components/navigation/NavigationMenuContent.jsx
try { (() => {
function NavigationMenuContent({
  items = []
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 320,
      background: "#fff",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--shadow-popover)",
      padding: 16,
      display: "grid",
      gap: 8
    }
  }, items.map((c, i) => /*#__PURE__*/React.createElement("a", {
    key: i,
    href: c.href || "#",
    style: {
      textDecoration: "none",
      color: "var(--fg-default)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 500,
      fontSize: 14
    }
  }, c.label), c.description && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: "var(--fg-muted)"
    }
  }, c.description))));
}
Object.assign(__ds_scope, { NavigationMenuContent });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/NavigationMenuContent.jsx", error: String((e && e.message) || e) }); }

// components/navigation/NavigationMenuContentItem.jsx
try { (() => {
function NavigationMenuContentItem({
  label,
  description,
  href
}) {
  return /*#__PURE__*/React.createElement("a", {
    href: href || "#",
    style: {
      textDecoration: "none",
      color: "var(--fg-default)",
      display: "block"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 500,
      fontSize: 14
    }
  }, label), description && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: "var(--fg-muted)"
    }
  }, description));
}
Object.assign(__ds_scope, { NavigationMenuContentItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/NavigationMenuContentItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/NavigationMenuItem.jsx
try { (() => {
function NavigationMenuItem({
  label,
  href,
  active,
  onClick
}) {
  return /*#__PURE__*/React.createElement("a", {
    href: href || "#",
    onClick: onClick,
    style: {
      display: "block",
      padding: "8px 12px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      color: "var(--fg-default)",
      background: active ? "var(--bg-subtle)" : "transparent",
      textDecoration: "none"
    }
  }, label);
}
Object.assign(__ds_scope, { NavigationMenuItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/NavigationMenuItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/SectionItems.jsx
try { (() => {
function SectionItems({
  sections = []
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 16,
      minWidth: 240
    }
  }, sections.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i
  }, s.title && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "6px 8px",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 500,
      color: "var(--fg-muted)"
    }
  }, s.title), s.items.map((it, j) => /*#__PURE__*/React.createElement("div", {
    key: j,
    style: {
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, it)))));
}
Object.assign(__ds_scope, { SectionItems });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/SectionItems.jsx", error: String((e && e.message) || e) }); }

// components/navigation/TabItem.jsx
try { (() => {
function TabItem({
  label,
  active,
  onClick
}) {
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      border: "none",
      background: active ? "#fff" : "transparent",
      boxShadow: active ? "var(--shadow-sm)" : "none",
      borderRadius: "var(--radius-sm)",
      padding: "6px 12px",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, label);
}
Object.assign(__ds_scope, { TabItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/TabItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Tabs.jsx
try { (() => {
function Tabs({
  tabs = [],
  value,
  onChange,
  children
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 4,
      background: "var(--bg-subtle)",
      borderRadius: "var(--radius-md)",
      padding: 4,
      width: "fit-content"
    }
  }, tabs.map(t => /*#__PURE__*/React.createElement("button", {
    key: t.value,
    onClick: () => onChange && onChange(t.value),
    style: {
      border: "none",
      background: value === t.value ? "#fff" : "transparent",
      boxShadow: value === t.value ? "var(--shadow-sm)" : "none",
      borderRadius: "var(--radius-sm)",
      padding: "6px 12px",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      color: "var(--fg-default)",
      cursor: "pointer"
    }
  }, t.label))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 16
    }
  }, children));
}
Object.assign(__ds_scope, { Tabs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Tabs.jsx", error: String((e && e.message) || e) }); }

// components/overlay/AlertDialog.jsx
try { (() => {
function AlertDialog({
  open,
  title = "Are you absolutely sure?",
  description,
  onConfirm,
  onCancel,
  confirmLabel = "Continue",
  cancelLabel = "Cancel"
}) {
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      inset: 0,
      background: "rgba(15,23,42,0.5)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      zIndex: 50
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 420,
      background: "#fff",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--shadow-lg)",
      padding: 24,
      display: "flex",
      flexDirection: "column",
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontWeight: 600,
      fontSize: 18,
      color: "var(--fg-default)"
    }
  }, title), description && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-muted)"
    }
  }, description), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "flex-end",
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: onCancel,
    style: {
      background: "transparent",
      border: "none",
      height: 36,
      padding: "0 12px",
      borderRadius: "var(--radius-md)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      cursor: "pointer"
    }
  }, cancelLabel), /*#__PURE__*/React.createElement("button", {
    onClick: onConfirm,
    style: {
      background: "var(--destructive)",
      color: "#fff",
      border: "none",
      height: 36,
      padding: "0 12px",
      borderRadius: "var(--radius-md)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 500,
      cursor: "pointer"
    }
  }, confirmLabel))));
}
Object.assign(__ds_scope, { AlertDialog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlay/AlertDialog.jsx", error: String((e && e.message) || e) }); }

// components/overlay/AlterDialog.jsx
try { (() => {
function AlterDialog(props) {
  return __ds_scope.AlertDialog(props);
}
Object.assign(__ds_scope, { AlterDialog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlay/AlterDialog.jsx", error: String((e && e.message) || e) }); }

// components/overlay/Dialog.jsx
try { (() => {
function Dialog({
  open,
  onClose,
  title,
  description,
  children,
  footer
}) {
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      inset: 0,
      background: "rgba(15,23,42,0.5)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      zIndex: 50
    },
    onClick: onClose
  }, /*#__PURE__*/React.createElement("div", {
    onClick: e => e.stopPropagation(),
    style: {
      width: 480,
      background: "#fff",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--shadow-lg)",
      padding: 24,
      display: "flex",
      flexDirection: "column",
      gap: 16
    }
  }, title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontWeight: 600,
      fontSize: 18,
      color: "var(--fg-default)"
    }
  }, title), description && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      color: "var(--fg-muted)"
    }
  }, description), children, footer && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "flex-end",
      gap: 8
    }
  }, footer)));
}
Object.assign(__ds_scope, { Dialog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlay/Dialog.jsx", error: String((e && e.message) || e) }); }

// components/overlay/HoverCard.jsx
try { (() => {
function HoverCard({
  trigger,
  children
}) {
  const [show, setShow] = React.useState(false);
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      display: "inline-block"
    },
    onMouseEnter: () => setShow(true),
    onMouseLeave: () => setShow(false)
  }, trigger, show && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      top: "calc(100% + 8px)",
      left: 0,
      width: 280,
      background: "#fff",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--shadow-popover)",
      padding: 16,
      zIndex: 20
    }
  }, children));
}
Object.assign(__ds_scope, { HoverCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlay/HoverCard.jsx", error: String((e && e.message) || e) }); }

// components/overlay/Popover.jsx
try { (() => {
function Popover({
  trigger,
  children,
  open,
  onToggle
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      display: "inline-block"
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: onToggle
  }, trigger), open && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      top: "calc(100% + 8px)",
      left: 0,
      width: 280,
      background: "#fff",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--shadow-popover)",
      padding: 16,
      zIndex: 20
    }
  }, children));
}
Object.assign(__ds_scope, { Popover });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlay/Popover.jsx", error: String((e && e.message) || e) }); }

// components/overlay/Tooltip.jsx
try { (() => {
function Tooltip({
  label,
  children
}) {
  const [show, setShow] = React.useState(false);
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      display: "inline-block"
    },
    onMouseEnter: () => setShow(true),
    onMouseLeave: () => setShow(false)
  }, children, show && /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      bottom: "calc(100% + 6px)",
      left: "50%",
      transform: "translateX(-50%)",
      background: "var(--slate-900)",
      color: "#fff",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 500,
      padding: "4px 8px",
      borderRadius: "var(--radius-sm)",
      whiteSpace: "nowrap",
      zIndex: 30
    }
  }, label));
}
Object.assign(__ds_scope, { Tooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlay/Tooltip.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/Header.jsx
try { (() => {
const {
  NavigationMenu,
  Icon,
  Button
} = window.UnilifeDesignSystem_329c9a;
function Header() {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      padding: "16px 32px",
      borderBottom: "1px solid var(--border-default)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 32
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo.svg",
    alt: "Unilife",
    style: {
      height: 36
    }
  }), /*#__PURE__*/React.createElement(NavigationMenu, {
    items: [{
      label: "About Us",
      href: "#about"
    }, {
      label: "Our Products",
      content: [{
        label: "Fabric Care",
        description: "Powders, gels, softeners"
      }, {
        label: "Dishwashing",
        description: "Hand & automatic dishwashing"
      }, {
        label: "Cleaning",
        description: "General & specialized cleaners"
      }, {
        label: "Personal Care",
        description: "Soap, shower gels"
      }]
    }, {
      label: "News",
      href: "#news"
    }]
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 6,
      fontSize: 13,
      fontWeight: 500,
      color: "var(--fg-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--fg-default)"
    }
  }, "EN"), "/", /*#__PURE__*/React.createElement("span", null, "UK")), /*#__PURE__*/React.createElement("a", {
    href: "#",
    style: {
      color: "var(--fg-muted)"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "IconFacebook",
    size: 18
  })), /*#__PURE__*/React.createElement("a", {
    href: "#",
    style: {
      color: "var(--fg-muted)"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "IconInstagram",
    size: 18
  })), /*#__PURE__*/React.createElement("a", {
    href: "#",
    style: {
      color: "var(--fg-muted)"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "IconLinkedin",
    size: 18
  })), /*#__PURE__*/React.createElement(Button, {
    variant: "outline",
    size: "sm"
  }, "Contact")));
}
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/Header.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/Sections.jsx
try { (() => {
const {
  Button,
  Card,
  Icon
} = window.UnilifeDesignSystem_329c9a;
function Section({
  children,
  style,
  bg
}) {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      padding: "64px 32px",
      background: bg,
      ...style
    }
  }, children);
}
function Container({
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1136,
      margin: "0 auto",
      ...style
    }
  }, children);
}
function Hero() {
  return /*#__PURE__*/React.createElement(Section, {
    bg: "var(--bg-muted)",
    style: {
      padding: "64px 32px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: 48,
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 48,
      lineHeight: "48px",
      fontWeight: 800,
      letterSpacing: "-0.012em",
      color: "var(--fg-default)",
      marginBottom: 16
    }
  }, "Clean Home Essentials"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 18,
      lineHeight: "28px",
      color: "var(--fg-muted)",
      marginBottom: 24,
      maxWidth: 440
    }
  }, "Safe, effective home care products \u2014 from laundry to cleaning. Made in Ukraine to EU-certified standards."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(Button, null, "Our Products"), /*#__PURE__*/React.createElement(Button, {
    variant: "outline"
  }, "About Us"))), /*#__PURE__*/React.createElement("image-slot", {
    id: "hero",
    style: {
      width: "100%",
      height: 340
    },
    shape: "rounded",
    radius: "12",
    placeholder: "Hero: product family shot"
  })));
}
function About() {
  return /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(Container, {
    style: {
      maxWidth: 760
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 30,
      lineHeight: "36px",
      fontWeight: 700,
      letterSpacing: "-0.007em",
      marginBottom: 24,
      textAlign: "center",
      color: "var(--brand-800)"
    }
  }, "About Us"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 18,
      lineHeight: "28px",
      color: "var(--fg-default)",
      marginBottom: 20
    }
  }, "We develop and manufacture laundry, home care, and personal care products that combine strong performance, sustainability and fair pricing."), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 16,
      lineHeight: "28px",
      color: "var(--fg-muted)",
      marginBottom: 16
    }
  }, "Our mission is to make high-quality products affordable\u2014without compromising on standards, compliance, or reliability."), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 16,
      lineHeight: "28px",
      color: "var(--fg-muted)",
      marginBottom: 16
    }
  }, "Through efficient manufacturing and a hands-on, market-driven approach to innovation, we respond quickly to the needs of our markets and the people who use our products every day."), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 16,
      lineHeight: "28px",
      color: "var(--fg-muted)",
      marginBottom: 16
    }
  }, "We support our partners with attractive price points, healthy margins and sustainable growth opportunities."), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 18,
      lineHeight: "28px",
      fontWeight: 600,
      color: "var(--fg-default)"
    }
  }, "We don't overpromise. We deliver on our commitments.")));
}
function BrandStrip() {
  const brands = ["GRÜNWALD", "Wells", "Sapfir", "Tino"];
  return /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(Container, null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      fontWeight: 700,
      color: "var(--brand-800)",
      marginBottom: 20,
      textTransform: "uppercase",
      letterSpacing: "0.04em",
      textAlign: "center"
    }
  }, "Our Brands"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4,1fr)",
      gap: 16
    }
  }, brands.map(b => /*#__PURE__*/React.createElement("div", {
    key: b,
    style: {
      height: 100,
      borderRadius: 8,
      boxShadow: "inset 0 0 0 1px var(--border-default)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontSize: 20,
      fontWeight: 700,
      color: "var(--fg-default)"
    }
  }, b)))));
}
function ProductCategories() {
  const cats = [{
    icon: "IconShirt",
    label: "Fabric Care",
    desc: "Powders, gels, softeners"
  }, {
    icon: "IconDroplets",
    label: "Dishwashing",
    desc: "Hand & automatic dishwashing"
  }, {
    icon: "IconRecycle",
    label: "Cleaning",
    desc: "General-purpose cleaners"
  }, {
    icon: "IconHeart",
    label: "Personal Care",
    desc: "Soap, shower gels"
  }];
  return /*#__PURE__*/React.createElement(Section, {
    bg: "var(--bg-muted)"
  }, /*#__PURE__*/React.createElement(Container, null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 30,
      lineHeight: "36px",
      fontWeight: 700,
      letterSpacing: "-0.007em",
      marginBottom: 32,
      textAlign: "center",
      color: "var(--brand-800)"
    }
  }, "Our Products"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4,1fr)",
      gap: 16
    }
  }, cats.map(c => /*#__PURE__*/React.createElement(Card, {
    key: c.label,
    style: {
      textAlign: "center"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: c.icon,
    size: 28,
    style: {
      color: "var(--primary)",
      marginBottom: 12
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 600,
      fontSize: 16,
      marginBottom: 6
    }
  }, c.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      color: "var(--fg-muted)"
    }
  }, c.desc))))));
}
function StatsBanner() {
  const stats = [{
    n: "25+",
    l: "years of experience"
  }, {
    n: "200+",
    l: "products"
  }, {
    n: "12",
    l: "export markets"
  }, {
    n: "9",
    l: "retail partners"
  }];
  return /*#__PURE__*/React.createElement(Section, {
    bg: "var(--primary)"
  }, /*#__PURE__*/React.createElement(Container, {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4,1fr)",
      gap: 24,
      textAlign: "center"
    }
  }, stats.map(s => /*#__PURE__*/React.createElement("div", {
    key: s.l
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 36,
      fontWeight: 800,
      color: "#fff"
    }
  }, s.n), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      color: "rgba(255,255,255,0.85)"
    }
  }, s.l)))));
}
function News() {
  return /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(Container, null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 30,
      lineHeight: "36px",
      fontWeight: 700,
      letterSpacing: "-0.007em",
      marginBottom: 32,
      textAlign: "center",
      color: "var(--brand-800)"
    }
  }, "News"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(3,1fr)",
      gap: 24
    }
  }, [1, 2, 3].map(i => /*#__PURE__*/React.createElement("div", {
    key: i
  }, /*#__PURE__*/React.createElement("image-slot", {
    id: "news-" + i,
    style: {
      width: "100%",
      height: 160,
      marginBottom: 12
    },
    shape: "rounded",
    radius: "8",
    placeholder: "News image"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 600,
      fontSize: 16,
      marginBottom: 6
    }
  }, "News headline ", i), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      color: "var(--fg-muted)"
    }
  }, "Short summary of the update or product news."))))));
}
function Partners() {
  const partners = ["Lidl", "Metro", "Selver", "Cap"];
  return /*#__PURE__*/React.createElement(Section, {
    bg: "var(--bg-muted)"
  }, /*#__PURE__*/React.createElement(Container, null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      fontWeight: 700,
      color: "var(--brand-800)",
      marginBottom: 20,
      textTransform: "uppercase",
      letterSpacing: "0.04em",
      textAlign: "center"
    }
  }, "Our Partners"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 40,
      alignItems: "center",
      flexWrap: "wrap"
    }
  }, partners.map(p => /*#__PURE__*/React.createElement("div", {
    key: p,
    style: {
      fontSize: 22,
      fontWeight: 700,
      color: "var(--fg-muted)"
    }
  }, p)))));
}
function Footer() {
  return /*#__PURE__*/React.createElement(Section, {
    bg: "var(--slate-900)",
    style: {
      padding: "48px 32px"
    }
  }, /*#__PURE__*/React.createElement(Container, {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: 32,
      alignItems: "start"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 32,
      fontSize: 14,
      marginBottom: 24
    }
  }, /*#__PURE__*/React.createElement("a", {
    href: "#",
    style: {
      color: "#fff"
    }
  }, "About Us"), /*#__PURE__*/React.createElement("a", {
    href: "#",
    style: {
      color: "#fff"
    }
  }, "Careers"), /*#__PURE__*/React.createElement("a", {
    href: "#",
    style: {
      color: "#fff"
    }
  }, "Contact")), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: "rgba(255,255,255,0.6)"
    }
  }, "\xA9 Unilife Group")), /*#__PURE__*/React.createElement("image-slot", {
    id: "map",
    style: {
      width: "100%",
      height: 220
    },
    shape: "rect",
    placeholder: "Map \u2014 office / production site"
  })));
}
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/Sections.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/image-slot.js
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
// Copied omelette starter. Re-running copy_starter_component with this kind overwrites this file with the latest version (page content is unaffected).
/* BEGIN USAGE */
/**
 * <image-slot> — user-fillable image placeholder.
 *
 * Drop this into a deck, mockup, or page wherever a design needs an image.
 * You control the slot's shape; it sizes to its container by default. When the search_stock_photos tool
 * is available, prefill the slot by default — write the photo's URL into
 * src (with credit/credit-href); the user can still fill or replace it
 * by dragging an image file onto it (or clicking to browse). The dropped
 * image persists across reloads via a .image-slots.state.json sidecar —
 * same read-via-fetch / write-via-window.omelette pattern as
 * design_canvas.jsx, so the filled slot shows on share links, downloaded
 * zips, and PPTX export. Outside the omelette runtime the slot is read-only.
 *
 * The sidecar is a SIBLING of the HTML file that uses this component: the
 * read is a document-relative fetch, and the host resolves the bridge's
 * sidecar writes into the previewed file's directory to match (same
 * contract as design_canvas.jsx). Pages in the same directory share one
 * sidecar; keep slot ids distinct across them.
 *
 * Attributes:
 *   id           Persistence key. REQUIRED for the drop to survive reload —
 *                every slot on the page needs a distinct id.
 *   shape        'rect' | 'rounded' | 'circle' | 'pill'   (default 'rounded')
 *                'circle' applies 50% border-radius; on a non-square slot
 *                that's an ellipse — set equal width and height for a true
 *                circle.
 *   radius       Corner radius in px for 'rounded'.       (default 12)
 *   mask         Any CSS clip-path value. Overrides `shape` — use this for
 *                hexagons, blobs, arbitrary polygons.
 *   fit          Initial framing baseline: cover | contain.   (default 'cover')
 *                cover starts the image filling the frame (overflow cropped);
 *                contain starts it fully visible (letterboxed). Either way the
 *                user can always pan/scale from there — double-click, or the
 *                Edit control, enters reframe mode (drag to move, scroll or
 *                corner-handles to scale; Escape / click-out commits). The
 *                crop persists alongside the image in the sidecar.
 *   placeholder  Empty-state caption.                      (default 'Drop an image')
 *   src          Optional initial/fallback image URL. Prefill it with a real
 *                photo via search_stock_photos when that tool is available
 *                (set credit/credit-href from the result). A user drop
 *                overrides it; clearing the drop reveals src again.
 *   credit       Attribution text shown as a small overlay at the
 *                bottom-left of the filled slot. REQUIRED whenever src
 *                points at any Unsplash host (images.unsplash.com,
 *                plus.unsplash.com, …): an Unsplash src with no credit
 *                renders an error tile INSTEAD of the photo (Unsplash
 *                terms forbid showing their photos unattributed). Use the
 *                exact form 'Photo by {photographer name} on Unsplash' —
 *                the overlay then links the name to credit-href and
 *                'Unsplash' to the Unsplash homepage, and links back to
 *                unsplash.com automatically get the required utm referral
 *                params appended at render time. The credit belongs to
 *                the src image, so it only shows while src is what's
 *                displayed — a user-dropped image hides it.
 *   credit-href  Link for the photographer's name in the credit overlay
 *                (their Unsplash profile URL from the stock-photo search
 *                results). http(s) URLs only — anything else renders the
 *                name as plain text.
 *
 * Sizing: the slot fills its container by default (width/height 100%).
 * Put it in a sized wrapper — absolutely positioned, a grid cell, a fixed
 * frame — and it takes exactly that box. When the parent's height is
 * indefinite (ordinary flow), it falls back to full width at a 3:2 aspect
 * ratio instead of collapsing. In a shrink-to-fit parent (a float,
 * width:max-content, an unsized absolute wrapper), percentages have
 * nothing to resolve against — size the slot or its wrapper explicitly
 * there. For a fixed-size slot, set
 * width/height on the element itself (inline style), which overrides the
 * default. When
 * layering content above a slot (full-bleed layouts), make the overlay
 * click-through — pointer-events: none on scrims/text plates, re-enabled
 * on interactive children — so the slot's hover controls stay reachable.
 * Keep the slot's bottom-left corner visually clear as well: the credit
 * overlay renders there, and a dark fade or text plate covering it hides
 * the attribution Unsplash's terms require — end the fade above that
 * corner, or keep it nearly transparent where the credit sits.
 *
 * Usage:
 *   <div style="position:relative;width:100%;height:100%">      <!-- full-bleed: -->
 *     <image-slot id="bg" shape="rect"></image-slot>            <!-- fills the wrapper -->
 *   </div>
 *   <image-slot id="hero"   style="width:800px;height:450px" shape="rounded" radius="20"
 *               placeholder="Drop a hero image"></image-slot>
 *   <image-slot id="avatar" style="width:120px;height:120px" shape="circle"></image-slot>
 *   <image-slot id="kite"   style="width:300px;height:300px"
 *               mask="polygon(50% 0, 100% 50%, 50% 100%, 0 50%)"></image-slot>
 */
/* END USAGE */

(() => {
  const STATE_FILE = '.image-slots.state.json';

  // Unsplash terms require visible attribution wherever their photos
  // display, and every link back to unsplash.com must carry utm referral
  // params. Two render-time rules enforce that here:
  //  - an Unsplash-src slot with NO credit attribute renders an error
  //    tile INSTEAD of the photo (an uncredited Unsplash photo on screen
  //    is itself the terms violation, so it never renders bare);
  //  - rendered credit links pointing at unsplash.com get the referral
  //    params appended when absent (credit-href values live in page
  //    content that can't be edited after the fact).
  // Keep the utm_source value in sync with UTM_SOURCE in
  // platform/web-agent/unsplash.ts — this file is a project-local
  // artifact and cannot import it (equality is pinned by tests).
  const UNSPLASH_HOMEPAGE_HREF = 'https://unsplash.com/?utm_source=claude_design&utm_medium=referral';
  // Host rule mirrors the hotlink validator that admits Unsplash srcs into
  // pages in the first place (cdn$ in unsplash.ts: apex or any subdomain)
  // — Unsplash+ results serve from plus.unsplash.com, not just images.*,
  // and an admitted-but-uncredited photo must error whatever unsplash
  // host it rides on.
  // Trailing-dot FQDNs (images.unsplash.com.) are the same host to the
  // browser but would miss the regex — strip one dot so the check fails
  // CLOSED (unrecognized-but-real Unsplash srcs must error, not render).
  const isUnsplashHost = u => {
    try {
      return /(^|\.)unsplash\.com$/.test(new URL(u, document.baseURI).hostname.replace(/\.$/, ''));
    } catch {
      return false;
    }
  };
  // Render-time referral normalization for links back to Unsplash:
  // appends utm_source/utm_medium when absent, preserves every existing
  // query param, never overwrites an existing utm_source, and passes
  // non-Unsplash URLs through untouched. Input is an ABSOLUTE validated
  // http(s) URL (the credit render funnel resolves + validates first).
  const withReferral = href => {
    try {
      const u = new URL(href);
      if (!/(^|\.)unsplash\.com$/.test(u.hostname.replace(/\.$/, ''))) {
        return href;
      }
      if (!u.searchParams.has('utm_source')) {
        u.searchParams.set('utm_source', 'claude_design');
      }
      if (!u.searchParams.has('utm_medium')) {
        u.searchParams.set('utm_medium', 'referral');
      }
      return u.toString();
    } catch (e) {
      return href;
    }
  };
  // 2× a ~600px slot in a 1920-wide deck — retina-sharp without making the
  // sidecar enormous. A 1200px WebP at q=0.85 is ~150-300KB.
  const MAX_DIM = 1200;
  // Raster formats only. SVG is excluded (can carry script; createImageBitmap
  // on SVG blobs is inconsistent). GIF is excluded because the canvas
  // re-encode keeps only the first frame, so an animated GIF would silently
  // go still — better to reject than surprise.
  const ACCEPT = ['image/png', 'image/jpeg', 'image/webp', 'image/avif'];

  // ── Shared sidecar store ────────────────────────────────────────────────
  // One fetch + immediate write-on-change for every <image-slot> on the
  // page. Reads via fetch() so viewing works anywhere the HTML and sidecar
  // are served together; writes go through window.omelette.writeFile, which
  // the host allowlists to *.state.json basenames only.
  const subs = new Set();
  let slots = {};
  // ids explicitly cleared before the sidecar fetch resolved — otherwise
  // the merge below can't tell "never set" from "just deleted" and would
  // resurrect the sidecar's stale value.
  const tombstones = new Set();
  let loaded = false;
  let loadP = null;
  function load() {
    if (loadP) return loadP;
    loadP = fetch(STATE_FILE).then(r => r.ok ? r.json() : null).then(j => {
      // Merge: sidecar loses to any in-memory change that raced ahead of
      // the fetch (drop or clear) so neither is clobbered by hydration.
      if (j && typeof j === 'object') {
        const merged = Object.assign({}, j, slots);
        // A framing-only write that raced ahead of hydration must not
        // drop a user image that's only on disk — inherit u from the
        // sidecar for any in-memory entry that lacks one.
        for (const k in slots) {
          if (merged[k] && !merged[k].u && j[k]) {
            merged[k].u = typeof j[k] === 'string' ? j[k] : j[k].u;
          }
        }
        for (const id of tombstones) delete merged[id];
        slots = merged;
      }
      tombstones.clear();
    }).catch(() => {}).then(() => {
      loaded = true;
      subs.forEach(fn => fn());
    });
    return loadP;
  }

  // Serialize writes so two near-simultaneous drops on different slots
  // can't reorder at the backend and leave the sidecar with only the
  // first. A save requested mid-flight just marks dirty and re-fires on
  // completion with the then-current slots.
  let saving = false;
  let saveDirty = false;
  // Unload-time flush: save()'s serialization defers a mid-RTT re-fire to a
  // .then that never runs in an unloading document, silently dropping a
  // pagehide commit. Post the current slots immediately instead — content
  // is a superset snapshot of any in-flight save's, the write is a
  // whole-file last-writer-wins replace, and postMessage FIFO delivers it
  // to the host after the in-flight one, so a backend-side reorder at
  // worst reproduces the dropped-commit outcome this flush improves on.
  // Guarded on the initial sidecar read: pre-hydration slots can miss
  // other slots' persisted entries, and flushing it would clobber them —
  // that narrow case stays best-effort (the in-memory merge in load()
  // cannot happen in an unloading document anyway).
  function flushNow() {
    if (!loaded) return;
    const w = window.omelette && window.omelette.writeFile;
    if (!w) return;
    try {
      Promise.resolve(w(STATE_FILE, JSON.stringify(slots))).catch(() => {});
    } catch (e) {}
  }
  function save() {
    if (saving) {
      saveDirty = true;
      return;
    }
    const w = window.omelette && window.omelette.writeFile;
    if (!w) return;
    saving = true;
    Promise.resolve(w(STATE_FILE, JSON.stringify(slots))).catch(() => {}).then(() => {
      saving = false;
      if (saveDirty) {
        saveDirty = false;
        save();
      }
    });
  }
  const S_MAX = 5;
  const clampS = s => Math.max(1, Math.min(S_MAX, s));

  // Normalize a stored slot value. Pre-reframe sidecars stored a bare
  // data-URL string; newer ones store {u, s, x, y}. Either shape is valid.
  function getSlot(id) {
    const v = slots[id];
    if (!v) return null;
    return typeof v === 'string' ? {
      u: v,
      s: 1,
      x: 0,
      y: 0
    } : v;
  }
  function setSlot(id, val) {
    if (!id) return;
    if (val) {
      slots[id] = val;
      tombstones.delete(id);
    } else {
      delete slots[id];
      if (!loaded) tombstones.add(id);
    }
    subs.forEach(fn => fn());
    // A drop is rare + high-value — write immediately so nav-away can't lose
    // it. Gate on the initial read so we don't overwrite a sidecar we haven't
    // merged yet; the merge in load() keeps this change once the read lands.
    if (loaded) save();else load().then(save);
  }

  // ── Image downscale ─────────────────────────────────────────────────────
  // Encode through a canvas so the sidecar carries resized bytes, not the
  // raw upload. Longest side is capped at 2× the slot's rendered width
  // (retina) and at MAX_DIM. WebP keeps alpha and is ~10× smaller than PNG
  // for photos, so there's no need for per-image format picking.
  async function toDataUrl(file, targetW) {
    const bitmap = await createImageBitmap(file);
    try {
      const cap = Math.min(MAX_DIM, Math.max(1, Math.round(targetW * 2)) || MAX_DIM);
      const scale = Math.min(1, cap / Math.max(bitmap.width, bitmap.height));
      const w = Math.max(1, Math.round(bitmap.width * scale));
      const h = Math.max(1, Math.round(bitmap.height * scale));
      const canvas = document.createElement('canvas');
      canvas.width = w;
      canvas.height = h;
      canvas.getContext('2d').drawImage(bitmap, 0, 0, w, h);
      return canvas.toDataURL('image/webp', 0.85);
    } finally {
      bitmap.close && bitmap.close();
    }
  }

  // ── Custom element ──────────────────────────────────────────────────────
  const stylesheet =
  // Fill the container by default: slots are usually placed inside a
  // sized wrapper (a hero frame, a grid cell, an inset:0 layer) and are
  // expected to take that box — a fixed intrinsic size would render as
  // a small tile in the corner of a full-bleed wrapper instead.
  // aspect-ratio is the companion fallback that keeps a bare slot
  // visible when the parent's height is indefinite: height:100%
  // resolves to auto there, and the ratio then derives height from
  // width instead of letting the slot collapse to zero height.
  // Explicit width/height on the element override all of this.
  // color:inherit (not a fixed near-black): the placeholder chrome —
  // empty-state icon/caption (currentColor) and the dashed ring — must
  // read on dark decks too, and the slide's own text color is the one
  // color guaranteed to contrast with the slide background. The soft
  // look comes from opacity on those parts, not from a baked-in alpha.
  ':host{display:block;position:relative;' + '  font:13px/1.3 system-ui,-apple-system,sans-serif;' + '  width:100%;height:100%;aspect-ratio:3/2}' + '.empty .cap,.empty .sub{opacity:.75}' + '.frame{position:absolute;inset:0;overflow:hidden;background:rgba(127,127,127,.08)}' +
  // .frame img (clipped) and .spill (unclipped ghost + handles) share the
  // same left/top/width/height in frame-%, computed by _applyView(), so the
  // inside-mask crop and the outside-mask spill stay pixel-aligned.
  '.frame img{position:absolute;max-width:none;transform:translate(-50%,-50%);' + '  -webkit-user-drag:none;user-select:none;touch-action:none}' +
  // Reframe mode (double-click): the full image spills past the mask. The
  // spill layer is sized to the IMAGE bounds so its corners are where the
  // resize handles belong. The ghost <img> inside is translucent; the real
  // clipped <img> underneath shows the opaque in-mask crop.
  // popover=manual promotes the spill to the top layer on reframe, so it is
  // not clipped by any overflow:hidden / clip-path / scroll-container
  // ancestor (a plain z-index can't escape overflow clipping). UA popover
  // defaults (inset:0;margin:auto) are reset; _applyView sets viewport px.
  '.spill{position:fixed;margin:0;inset:auto;border:0;padding:0;background:transparent;' + '  overflow:visible;transform:translate(-50%,-50%);z-index:1;cursor:grab;touch-action:none}' + ':host([data-panning]) .spill{cursor:grabbing}' + '.spill .ghost{position:absolute;inset:0;width:100%;height:100%;opacity:.35;' + '  pointer-events:none;-webkit-user-drag:none;user-select:none;' + '  box-shadow:0 0 0 1px rgba(0,0,0,.2),0 12px 32px rgba(0,0,0,.2)}' + '.spill .handle{position:absolute;width:12px;height:12px;border-radius:50%;' + '  background:#fff;box-shadow:0 0 0 1.5px #c96442,0 1px 3px rgba(0,0,0,.3);' + '  transform:translate(-50%,-50%)}' + '.spill .handle[data-c=nw]{left:0;top:0;cursor:nwse-resize}' + '.spill .handle[data-c=ne]{left:100%;top:0;cursor:nesw-resize}' + '.spill .handle[data-c=sw]{left:0;top:100%;cursor:nesw-resize}' + '.spill .handle[data-c=se]{left:100%;top:100%;cursor:nwse-resize}' + ':host([data-reframe]){z-index:10}' + ':host([data-reframe]) .frame{box-shadow:0 0 0 2px #c96442}' + '.empty{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;' + '  justify-content:center;gap:6px;text-align:center;padding:12px;box-sizing:border-box;' + '  cursor:pointer;user-select:none}' + '.empty svg{opacity:.45}' + '.empty .cap{max-width:90%;font-weight:500;letter-spacing:.01em}' + '.empty .sub{font-size:11px}' + '.empty .sub u{text-underline-offset:2px}' + '.empty:hover .sub{opacity:1}' + ':host([data-over]) .frame{outline:2px solid #c96442;outline-offset:-2px;' + '  background:rgba(201,100,66,.10)}' + '.ring{position:absolute;inset:0;pointer-events:none;border:1.5px dashed currentColor;' + '  opacity:.35;transition:border-color .12s,opacity .12s}' + ':host([data-over]) .ring{border-color:#c96442;opacity:1}' + ':host([data-filled]) .ring{display:none}' +
  // Controls overlay INSIDE the frame, pinned to the top-right corner, so
  // a full-bleed slot in an overflow:hidden container still shows them
  // (the old below-mask placement got clipped). Credit sits bottom-left,
  // so top-right avoids collision. The blurred pill background keeps them
  // legible over the image.
  // The UA [popover] base rule styles the element in EVERY state (only
  // display:none is gated on :not(:popover-open), and the display:flex
  // below overrides that) — so the UA resets live HERE, like .spill's,
  // or the ordinary hover-state strip renders as a bordered Canvas box
  // centered by margin:auto. inset:auto precedes top/right (shorthand).
  '.ctl{position:absolute;inset:auto;top:8px;right:8px;margin:0;border:0;padding:0;' + '  background:transparent;overflow:visible;' + '  display:flex;gap:6px;opacity:0;pointer-events:none;transition:opacity .12s;z-index:2;' + '  white-space:nowrap}' +
  // While reframing, the spill owns the top layer and would swallow every
  // click on the in-frame controls. Promoting .ctl into the top layer
  // ABOVE the spill (shown after it — later popovers stack higher) keeps
  // Edit-as-toggle and Replace clickable mid-reframe. _applyView pins it
  // to the frame's top-right in viewport px (translateX(-100%)
  // right-aligns against the computed left edge); inset:auto clears the
  // base rule's top/right so the inline left/top position it alone.
  '.ctl:popover-open{position:fixed;inset:auto;transform:translateX(-100%)}' + ':host([data-filled][data-editable]:hover) .ctl,:host([data-reframe]) .ctl' + '  {opacity:1;pointer-events:auto}' + '.ctl button{appearance:none;border:0;border-radius:6px;padding:5px 10px;cursor:pointer;' + '  background:rgba(0,0,0,.65);color:#fff;font:11px/1 system-ui,-apple-system,sans-serif;' + '  backdrop-filter:blur(6px)}' + '.ctl button:hover{background:rgba(0,0,0,.8)}' + '.err{position:absolute;left:8px;bottom:8px;right:8px;color:#b3261e;font-size:11px;' + '  background:rgba(255,255,255,.85);padding:4px 6px;border-radius:5px;pointer-events:none}' +
  // Replacement in flight: after a src swap the browser keeps painting
  // the PREVIOUS image until the new one decodes, so a Replace would
  // flash the old photo and then pop. Hide the stale frame (visibility,
  // not display — _applyView geometry still applies) and spin until the
  // new image reports in (load/error clears data-swapping).
  ':host([data-swapping]) .frame img{visibility:hidden}' + '.loading{position:absolute;inset:0;display:none;align-items:center;' + '  justify-content:center;pointer-events:none}' + ':host([data-swapping]) .loading{display:flex}' + '.loading::after{content:"";width:22px;height:22px;border-radius:50%;' + '  border:2px solid rgba(127,127,127,.25);border-top-color:currentColor;' + '  animation:om-slot-spin .7s linear infinite}' + '@keyframes om-slot-spin{to{transform:rotate(360deg)}}' +
  // Reduced motion: the static two-tone ring still reads as "working".
  '@media (prefers-reduced-motion:reduce){.loading::after{animation:none}}' + '.credit{position:absolute;left:6px;bottom:6px;max-width:calc(100% - 12px);display:none;' + '  padding:3px 7px;border-radius:5px;background:rgba(0,0,0,.55);color:#fff;' + '  font:10px/1.2 system-ui,-apple-system,sans-serif;text-decoration:none;' + '  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;backdrop-filter:blur(6px)}' +
  // The credit is a SPAN holding one or two <a>s (Unsplash's prescribed
  // form links the photographer AND Unsplash) — anchors style inline so
  // the overlay reads as one line of text.
  '.credit a{color:inherit;text-decoration:none}' + '.credit a:hover,.credit a:focus-visible{text-decoration:underline}' + ':host([data-filled][data-credit]) .credit{display:block}' +
  // Exports must ship JUST the image — no hover controls, no credit chip
  // (the host marks <html data-om-exporting> for the capture window; the
  // page-level hide script can't reach shadow DOM, this rule can).
  ':host-context([data-om-exporting]) .ctl,' + ':host-context([data-om-exporting]) .credit{display:none !important}' +
  // Print must ship just the image too: the hover-gated controls can be
  // mid-hover when print() fires, and the credit chip is screen chrome —
  // the same rule the capture window gets, keyed on print media instead
  // of the host's data-om-exporting mark (the print path sets no mark).
  '@media print{.ctl,.credit{display:none !important}}' +
  // No export-window mask rules here on purpose: the export capture
  // releases the replacement mask by REMOVING data-swapping (the
  // shadow-root pass in pages/export/shared.ts HIDE_EXPORT_CHROME_SCRIPT)
  // — attribute removal works in every engine (:host-context is
  // Chromium-only), is scoped by construction to slots actually
  // mid-swap, and hides the spinner through the same gate. A masked img
  // would otherwise be silently dropped from PPTX decks (the capture
  // walk skips visibility:hidden imgs).
  // Attribution error tile: REPLACES the photo when an Unsplash src has
  // no credit attribute — rendering the photo uncredited is the terms
  // violation, so the photo must not appear at all.
  // Calm and neutral on purpose (review feedback): the tile informs the
  // user; the fix instructions are machine-facing (usage docblock, tool
  // description, and the turn-end scan's bounce copy name the attributes
  // for the agent).
  '.attr-error{position:absolute;inset:0;display:none;flex-direction:column;align-items:center;' + '  justify-content:center;gap:6px;text-align:center;padding:12px;box-sizing:border-box;' + '  background:#f2f1ef;color:#6e6c66;user-select:none;' + '  font:13px/1.45 system-ui,-apple-system,sans-serif}' + '.attr-error svg{opacity:.55}' + '.attr-error .cap{max-width:92%;font-weight:500;letter-spacing:.01em}' + ':host([data-attribution-error]) .attr-error{display:flex}' + ':host([data-attribution-error]) .ring{display:none}';
  const icon = '<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" ' + 'stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">' + '<rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/>' + '<path d="m21 15-5-5L5 21"/></svg>';
  const warnIcon = '<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" ' + 'stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">' + '<path d="m21.73 18-8-14a2 2 0 0 0-3.46 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3"/>' + '<path d="M12 9v4"/><path d="M12 17h.01"/></svg>';
  class ImageSlot extends HTMLElement {
    static get observedAttributes() {
      return ['shape', 'radius', 'mask', 'fit', 'placeholder', 'src', 'id', 'credit', 'credit-href'];
    }

    /** Duplicate-slide hook (called by deck-stage, see its
     *  _remintDuplicateIds): copy this id's stored image, if any, under a
     *  freshly minted key and return that key — so a duplicated slide's
     *  slot keeps its dropped photo instead of reverting to the
     *  placeholder. 'isFree' is the caller's uniqueness check (document
     *  ids); candidates must ALSO be unused in the sidecar, which can
     *  hold keys from other pages sharing the project root. (An EMPTY
     *  slot on another page leaves no sidecar entry, so its id is not
     *  detectable here — a minted key can collide with it and that slot
     *  would show this photo. Same blast radius as two pages reusing an
     *  id by hand, which the shared sidecar already permits.) Returns null
     *  when no id could be minted (caller strips the id, today's
     *  behavior). */
    static cloneSlot(fromId, isFree) {
      if (typeof fromId !== 'string' || !fromId) return null;
      // Pre-hydration the store can't veto candidates or source the copy
      // — degrade to the strip (today's behavior) rather than mint
      // against keys we can't see yet. Any rendered (= droppable) slot
      // means load() has already settled.
      if (!loaded) return null;
      const stem = fromId.replace(/-\d+$/, '') || fromId;
      for (let n = 2; n < 100; n++) {
        const toId = stem + '-' + n;
        if (toId === fromId) continue;
        if (slots[toId] !== undefined) {
          // Reuse a key holding this exact value (bytes AND crop) if no
          // live element here owns it — a duplicate op the host refused
          // after minting leaves such a key behind, and reusing keeps
          // refused retries from accumulating one orphaned copy per
          // attempt. Full equality (not just bytes) so a byte-identical
          // key another PAGE owns with its own crop is stepped past, not
          // adopted or rewritten. (Entries without .u never match.)
          const prev = getSlot(toId);
          const cur = getSlot(fromId);
          if (!(prev && cur && prev.u && prev.u === cur.u && prev.s === cur.s && prev.x === cur.x && prev.y === cur.y && (typeof isFree !== 'function' || isFree(toId)))) continue;
          return toId;
        }
        if (typeof isFree === 'function' && !isFree(toId)) continue;
        const v = getSlot(fromId);
        if (v) setSlot(toId, Object.assign({}, v));
        return toId;
      }
      return null;
    }
    constructor() {
      super();
      // clonable: rail thumbnails deep-clone slides and carry this shadow
      // along; reuse an already-cloned root so upgrade-after-clone works.
      // (Deliberately NOT serializable — a getHTML consumer would embed
      // multi-MB sidecar data-URLs into serialized page HTML.)
      const root = this.shadowRoot || this.attachShadow({
        mode: 'open',
        clonable: true
      });
      // .spill and .ctl sit OUTSIDE .frame so overflow:hidden + border-radius
      // on the frame (circle, pill, rounded) can't clip them.
      root.innerHTML = '<style>' + stylesheet + '</style>' + '<div class="frame" part="frame">' + '  <img part="image" alt="" draggable="false" style="display:none">' + '  <div class="empty" part="empty">' + icon + '    <div class="cap"></div>' + '    <div class="sub">or <u>browse files</u></div></div>' + '  <div class="attr-error" part="attribution-error">' + warnIcon + '    <div class="cap">This photo needs attribution</div></div>' + '  <div class="loading" part="loading"></div>' + '  <div class="ring" part="ring"></div>' + '</div>' +
      // Outside .frame, like .spill/.ctl — the frame's overflow:hidden +
      // border-radius/clip-path would cut the credit off on circle/pill/mask.
      // A SPAN, not an <a>: the prescribed Unsplash credit holds two links
      // (photographer + Unsplash), built per-render in _render().
      '<span class="credit" part="credit"></span>' + '<div class="spill" popover="manual" data-dc-edit-transparent>' + '  <img class="ghost" alt="" draggable="false">' + '  <div class="handle" data-c="nw"></div><div class="handle" data-c="ne"></div>' + '  <div class="handle" data-c="sw"></div><div class="handle" data-c="se"></div>' + '</div>' +
      // data-dc-edit-transparent: the DC editor's edit-mode picker lets
      // clicks through for chrome marked with it (EDIT_TRANSPARENT_SEL)
      // — without it, Replace/Edit clicks in Edit mode are swallowed by
      // element selection and the controls look dead.
      '<div class="ctl" popover="manual" data-dc-edit-transparent><button data-act="replace" title="Replace image">Replace</button>' + '  <button data-act="edit" title="Reframe image">Edit</button></div>' + '<input type="file" accept="' + ACCEPT.join(',') + '" hidden>';
      this._frame = root.querySelector('.frame');
      this._ring = root.querySelector('.ring');
      this._img = root.querySelector('.frame img');
      this._empty = root.querySelector('.empty');
      this._cap = root.querySelector('.cap');
      this._sub = root.querySelector('.sub');
      this._spill = root.querySelector('.spill');
      this._ctl = root.querySelector('.ctl');
      this._credit = root.querySelector('.credit');
      this._attrError = root.querySelector('.attr-error');
      // Credit clicks open the link, not browse/reframe.
      this._credit.addEventListener('click', e => e.stopPropagation());
      this._credit.addEventListener('dblclick', e => e.stopPropagation());
      this._ghost = root.querySelector('.ghost');
      this._err = null;
      this._input = root.querySelector('input');
      this._depth = 0;
      this._gen = 0;
      // Encode-in-flight marker (the owning _ingest generation): while set,
      // the same-src "nothing in flight" clear in _render must not fire —
      // the stored value still points at the OLD image until the encode
      // lands, so that clear would unmask the stale image mid-replace.
      this._swapGen = 0;
      // Render-owned swap in flight: set when _render assigns a new src,
      // cleared only by the img's own load/error (or the empty branch).
      // img.complete CANNOT stand in for this — setting src only QUEUES
      // the current-request swap (a microtask), so synchronously after an
      // assignment, complete still reports the OLD settled request. The
      // pick path does exactly that: the host sets src, credit, and
      // credit-href back-to-back in one task, and renders #2/#3 would
      // read the stale complete === true and drop the mask one render
      // after it was set.
      this._loadPending = false;
      // See _render's empty branch: a transient attribution-error wipe of a
      // showing image must make the follow-up render a replacement (spinner),
      // not a first fill (blank frame).
      this._hidShowing = false;
      this._view = {
        s: 1,
        x: 0,
        y: 0
      };
      this._subFn = () => this._render();
      // Shadow-DOM listeners live with the shadow DOM — bound once here so
      // disconnect/reconnect (e.g. React remount) doesn't stack handlers.
      this._empty.addEventListener('click', () => this._input.click());
      root.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (!act) return;
        // The hidden controls are opacity-0 but still tabbable — without
        // this gate a keyboard user could drive them on a read-only share
        // link (mirrors the dblclick handler's editable gate).
        if (!this.hasAttribute('data-editable')) return;
        if (act === 'replace') {
          this._exitReframe(true);
          // Host-owned picker (Unsplash modal; it also offers local import).
          this.dispatchEvent(new CustomEvent('image-slot:pick', {
            bubbles: true,
            composed: true,
            detail: {
              id: this.id || null
            }
          }));
        }
        if (act === 'edit') {
          if (!this._reframes()) return;
          if (this.hasAttribute('data-reframe')) this._exitReframe(true);else this._enterReframe();
        }
      });
      this._input.addEventListener('change', () => {
        const f = this._input.files && this._input.files[0];
        if (f) this._ingest(f);
        this._input.value = '';
      });
      // naturalWidth/Height aren't known until load — re-apply so the cover
      // baseline is computed from real dimensions, not the 100%×100% fallback.
      // load/error also release the replacement-in-flight mask (via the
      // single discipline in _releaseMask): the swap is only revealed once
      // the new image can actually paint (on error the frame shows its
      // background, same as a fresh slot with a broken src).
      this._img.addEventListener('load', () => {
        this._loadPending = false;
        this._releaseMask(true);
        this._applyView();
      });
      this._img.addEventListener('error', () => {
        this._loadPending = false;
        this._releaseMask(true);
      });
      // Gated only on editable — any filled slot can be repositioned/scaled,
      // regardless of fit. Share links (no writeFile) stay static.
      this.addEventListener('dblclick', e => {
        if (!this.hasAttribute('data-editable') || !this._reframes()) return;
        e.preventDefault();
        if (this.hasAttribute('data-reframe')) this._exitReframe(true);else this._enterReframe();
      });
      // Pan + resize both originate on the spill layer. A handle pointerdown
      // drives an aspect-locked resize anchored at the opposite corner; any
      // other pointerdown on the spill pans. Offsets are frame-% so a
      // reframed slot survives responsive resize / PPTX export.
      this._spill.addEventListener('pointerdown', e => {
        if (e.button !== 0 || !this.hasAttribute('data-reframe')) return;
        e.preventDefault();
        e.stopPropagation();
        this._spill.setPointerCapture(e.pointerId);
        const rect = this.getBoundingClientRect();
        const fw = rect.width || 1,
          fh = rect.height || 1;
        const corner = e.target.getAttribute && e.target.getAttribute('data-c');
        let move;
        if (corner) {
          // Resize about the OPPOSITE corner. Viewport-px throughout (rect
          // fw/fh, not clientWidth) so the math survives a transform:scale()
          // ancestor — deck_stage renders slides scaled-to-fit.
          const iw = this._img.naturalWidth || 1,
            ih = this._img.naturalHeight || 1;
          const contain = (this.getAttribute('fit') || 'cover').toLowerCase() === 'contain';
          const base = contain ? Math.min(fw / iw, fh / ih) : Math.max(fw / iw, fh / ih);
          const sx = corner.includes('e') ? 1 : -1;
          const sy = corner.includes('s') ? 1 : -1;
          const s0 = this._view.s;
          const w0 = iw * base * s0,
            h0 = ih * base * s0;
          const cx0 = (50 + this._view.x) / 100 * fw;
          const cy0 = (50 + this._view.y) / 100 * fh;
          const ox = cx0 - sx * w0 / 2,
            oy = cy0 - sy * h0 / 2;
          const diag0 = Math.hypot(w0, h0);
          const ux = sx * w0 / diag0,
            uy = sy * h0 / diag0;
          move = ev => {
            const proj = (ev.clientX - rect.left - ox) * ux + (ev.clientY - rect.top - oy) * uy;
            const s = clampS(s0 * proj / diag0);
            const d = diag0 * s / s0;
            this._view.s = s;
            this._view.x = (ox + ux * d / 2) / fw * 100 - 50;
            this._view.y = (oy + uy * d / 2) / fh * 100 - 50;
            this._clampView();
            this._applyView();
          };
        } else {
          this.setAttribute('data-panning', '');
          const start = {
            px: e.clientX,
            py: e.clientY,
            x: this._view.x,
            y: this._view.y
          };
          move = ev => {
            this._view.x = start.x + (ev.clientX - start.px) / fw * 100;
            this._view.y = start.y + (ev.clientY - start.py) / fh * 100;
            this._clampView();
            this._applyView();
          };
        }
        const up = () => {
          try {
            this._spill.releasePointerCapture(e.pointerId);
          } catch {}
          this._spill.removeEventListener('pointermove', move);
          this._spill.removeEventListener('pointerup', up);
          this._spill.removeEventListener('pointercancel', up);
          this.removeAttribute('data-panning');
          this._dragUp = null;
        };
        // Stashed so _exitReframe (Escape / outside-click mid-drag) can
        // tear the capture + listeners down synchronously.
        this._dragUp = up;
        this._spill.addEventListener('pointermove', move);
        this._spill.addEventListener('pointerup', up);
        this._spill.addEventListener('pointercancel', up);
      });
      // Wheel zoom stays available inside reframe mode as a trackpad nicety —
      // zooms toward the cursor (offset' = cursor·(1-k) + offset·k).
      this.addEventListener('wheel', e => {
        if (!this.hasAttribute('data-reframe')) return;
        e.preventDefault();
        const r = this.getBoundingClientRect();
        const cx = (e.clientX - r.left) / r.width * 100 - 50;
        const cy = (e.clientY - r.top) / r.height * 100 - 50;
        const prev = this._view.s;
        const next = clampS(prev * Math.pow(1.0015, -e.deltaY));
        if (next === prev) return;
        const k = next / prev;
        this._view.s = next;
        this._view.x = cx * (1 - k) + this._view.x * k;
        this._view.y = cy * (1 - k) + this._view.y * k;
        this._clampView();
        this._applyView();
      }, {
        passive: false
      });
    }
    connectedCallback() {
      // Warn once per page — an id-less slot works for the session but
      // cannot persist, and two id-less slots would share nothing.
      if (!this.id && !ImageSlot._warned) {
        ImageSlot._warned = true;
        console.warn('<image-slot> without an id will not persist its dropped image.');
      }
      this.addEventListener('dragenter', this);
      this.addEventListener('dragover', this);
      this.addEventListener('dragleave', this);
      this.addEventListener('drop', this);
      subs.add(this._subFn);
      // The host may inject window.omelette.writeFile AFTER the first render;
      // re-render on hover so the editable-gated controls reliably appear.
      this.addEventListener('pointerenter', this._subFn);
      // width%/height% in _applyView encode the frame aspect at call time —
      // a host resize (responsive grid, pane divider) would stretch the
      // image until the next _render. Re-render on size change: _render()
      // re-seeds _view from stored before clamp/apply, so a shrink→grow
      // cycle round-trips instead of ratcheting x/y toward the narrower
      // frame's clamp range.
      this._ro = new ResizeObserver(() => this._render());
      this._ro.observe(this);
      load();
      this._render();
    }
    disconnectedCallback() {
      subs.delete(this._subFn);
      this.removeEventListener('pointerenter', this._subFn);
      this.removeEventListener('dragenter', this);
      this.removeEventListener('dragover', this);
      this.removeEventListener('dragleave', this);
      this.removeEventListener('drop', this);
      if (this._ro) {
        this._ro.disconnect();
        this._ro = null;
      }
      // commit=false: a disconnect is not a user intent — committing here
      // would persist whatever half-finished drag a React remount or DOM
      // splice happened to interrupt. Deliberate exits commit on their own
      // paths (Escape/click-out/toggle), and unloads commit via pagehide.
      this._exitReframe(false);
    }
    _enterReframe() {
      if (this.hasAttribute('data-reframe')) return;
      this.setAttribute('data-reframe', '');
      this._signalReframe(true);
      // Best-effort commit when the document unloads mid-reframe (a host
      // navigation racing the enter signal, a manual reload, tab close):
      // the sidecar write rides the host bridge, which outlives this
      // document, so the crop survives even though the mode dies with the
      // DOM. Held on the instance so _exitReframe detaches exactly what
      // was attached.
      this._pagehide = () => {
        this._exitReframe(true);
        flushNow();
      };
      window.addEventListener('pagehide', this._pagehide);
      // Promote spill to the top layer, then keep it pinned over the frame:
      // scroll/resize cover the common cases, and a per-frame rect check
      // catches layout shifts that fire neither (an image above finishing
      // load, streamed DOM pushing the slot down, an ancestor transform
      // change) so the overlay can't detach from the frame.
      try {
        this._spill.showPopover();
      } catch {}
      // After the spill, so the controls stack above it in the top layer.
      try {
        this._ctl.showPopover();
      } catch {}
      this._reposition = () => {
        if (this.hasAttribute('data-reframe')) this._applyView();
      };
      window.addEventListener('scroll', this._reposition, true);
      window.addEventListener('resize', this._reposition);
      this._lastRect = '';
      this._watch = () => {
        if (!this.hasAttribute('data-reframe')) return;
        const r = this.getBoundingClientRect();
        const key = r.left + ',' + r.top + ',' + r.width + ',' + r.height;
        if (key !== this._lastRect) {
          this._lastRect = key;
          this._applyView();
        }
        this._watchId = requestAnimationFrame(this._watch);
      };
      this._watchId = requestAnimationFrame(this._watch);
      this._applyView();
      // Close on click outside (the spill handler stopPropagation()s so
      // in-image drags don't reach this) and on Escape. Listeners are held
      // on the instance so _exitReframe / disconnectedCallback can detach
      // exactly what was attached.
      this._outside = e => {
        if (e.composedPath && e.composedPath().includes(this)) return;
        this._exitReframe(true);
      };
      this._esc = e => {
        if (e.key === 'Escape') this._exitReframe(true);
      };
      document.addEventListener('pointerdown', this._outside, true);
      document.addEventListener('keydown', this._esc, true);
    }
    _exitReframe(commit) {
      if (!this.hasAttribute('data-reframe')) return;
      if (this._dragUp) this._dragUp();
      this.removeAttribute('data-reframe');
      this.removeAttribute('data-panning');
      if (this._outside) document.removeEventListener('pointerdown', this._outside, true);
      if (this._esc) document.removeEventListener('keydown', this._esc, true);
      this._outside = this._esc = null;
      if (this._reposition) {
        window.removeEventListener('scroll', this._reposition, true);
        window.removeEventListener('resize', this._reposition);
        this._reposition = null;
      }
      if (this._watchId) {
        cancelAnimationFrame(this._watchId);
        this._watchId = 0;
      }
      if (this._pagehide) {
        window.removeEventListener('pagehide', this._pagehide);
        this._pagehide = null;
      }
      try {
        this._spill.hidePopover();
      } catch {}
      try {
        this._ctl.hidePopover();
      } catch {}
      this._ctl.style.left = '';
      this._ctl.style.top = '';
      if (commit) this._commitView();
      this._signalReframe(false);
    }

    // Reframe state lives only in this DOM until commit, invisible to the
    // host's dirty signals — announce enter/exit so the host can hold
    // auto-reloads for exactly the gesture (the guest bundle forwards
    // image-slot:reframe to the host as imageSlotReframe). Dispatched on
    // the element (composed, so it escapes shadow roots) while connected;
    // a disconnected exit (disconnectedCallback) falls back to document so
    // the host still hears it.
    _signalReframe(active) {
      const target = this.isConnected ? this : document;
      target.dispatchEvent(new CustomEvent('image-slot:reframe', {
        bubbles: true,
        composed: true,
        detail: {
          active: active,
          id: this.id || null
        }
      }));
    }

    // Public: host's "Import from computer" calls this to run local browse.
    openFilePicker() {
      this._exitReframe(true);
      this._input.click();
    }

    // A src write is a newer intent for this slot's content — the host
    // pick path (setImageSlotImage) or an agent edit — so it must win
    // over any encode still in flight from an earlier drop: left live,
    // that encode lands later, passes _ingest's gen guard, and its
    // setSlot silently overwrites the pick (the stored value shadows
    // src in _render). Bumping _gen kills the encode before its own
    // _swapGen clear runs, so clear the dead claim here too — otherwise
    // _releaseMask (gated on !_swapGen) never fires and the pick's
    // spinner is stranded. src ONLY: the pick sets credit/credit-href
    // in the same task, and clearing _swapGen on those would let the
    // same-src branch unmask the old image mid-encode.
    attributeChangedCallback(name, oldVal, newVal) {
      if (name === 'src' && oldVal !== newVal) {
        this._gen++;
        this._swapGen = 0;
      }
      if (this.shadowRoot) this._render();
    }

    // handleEvent — one listener object for all four drag events keeps the
    // add/remove symmetric and the depth counter correct.
    handleEvent(e) {
      if (e.type === 'dragenter' || e.type === 'dragover') {
        // Without preventDefault the browser never fires 'drop'.
        e.preventDefault();
        e.stopPropagation();
        if (e.dataTransfer) e.dataTransfer.dropEffect = 'copy';
        if (e.type === 'dragenter') this._depth++;
        this.setAttribute('data-over', '');
      } else if (e.type === 'dragleave') {
        // dragenter/leave fire for every descendant crossing — count depth
        // so hovering the icon inside the empty state doesn't flicker.
        if (--this._depth <= 0) {
          this._depth = 0;
          this.removeAttribute('data-over');
        }
      } else if (e.type === 'drop') {
        e.preventDefault();
        e.stopPropagation();
        this._depth = 0;
        this.removeAttribute('data-over');
        const f = e.dataTransfer && e.dataTransfer.files && e.dataTransfer.files[0];
        if (f) this._ingest(f);
      }
    }
    async _ingest(file) {
      this._setError(null);
      if (!file || ACCEPT.indexOf(file.type) < 0) {
        this._setError('Drop a PNG, JPEG, WebP, or AVIF image.');
        return;
      }
      // toDataUrl can take hundreds of ms on a large photo. A Clear or a
      // newer drop during that window would be clobbered when this await
      // resumes — bump + capture a generation so stale encodes bail.
      const gen = ++this._gen;
      // Replacing a shown image: surface the swap through the encode too,
      // not just the decode — otherwise the old photo sits there with no
      // feedback while the canvas re-encode runs. An empty slot keeps its
      // placeholder (no spinner) until the encode lands, as before.
      // _swapGen guards the mask against re-renders DURING the encode
      // (pointerenter, ResizeObserver, another slot's store write): the
      // stored value still resolves to the old image there, so _render's
      // same-src clear would otherwise unmask it mid-replace.
      if (this.hasAttribute('data-filled')) {
        this.setAttribute('data-swapping', '');
        this._swapGen = gen;
      }
      try {
        const w = this.clientWidth || this.offsetWidth || MAX_DIM;
        const url = await toDataUrl(file, w);
        if (gen !== this._gen) return;
        // Only exit reframe once the new image is in hand — a rejected type
        // or decode failure leaves the in-progress crop untouched.
        this._exitReframe(false);
        // Clear BEFORE setSlot: its synchronous re-render must see no
        // pending encode, so a byte-identical re-upload (same data URL, no
        // load event coming) still clears the mask via the complete branch.
        this._swapGen = 0;
        const val = {
          u: url,
          s: 1,
          x: 0,
          y: 0
        };
        setSlot(this.id || '', val);
        // Keep a session-local copy for id-less slots so the drop still
        // shows, even though it cannot persist.
        if (!this.id) {
          this._local = val;
          this._render();
        }
      } catch (err) {
        if (gen !== this._gen) return;
        this._swapGen = 0;
        // Reveal the kept old image — unless another replacement (a
        // remote pick's src swap) is still in flight, in which case the
        // mask stays until THAT image settles (its load/error releases).
        this._releaseMask();
        this._setError('Could not read that image.');
        console.warn('<image-slot> ingest failed:', err);
      }
    }
    _setError(msg) {
      if (this._err) {
        this._err.remove();
        this._err = null;
      }
      if (!msg) return;
      const d = document.createElement('div');
      d.className = 'err';
      d.textContent = msg;
      this.shadowRoot.appendChild(d);
      this._err = d;
      setTimeout(() => {
        if (this._err === d) {
          d.remove();
          this._err = null;
        }
      }, 3000);
    }

    // Reframing (pan/resize) is available on any filled slot — the user can
    // always reposition/scale. `fit` only sets the initial baseline (see
    // _geom): contain starts fully-visible, cover starts frame-filling.
    _reframes() {
      return this.hasAttribute('data-filled');
    }

    // The single release discipline for the replacement-in-flight mask
    // (data-swapping). The mask comes off only when BOTH hold:
    //  - no encode is pending (_swapGen) — mid-encode the stored value
    //    still resolves to the old image, so any reveal paints it;
    //  - the frame img has settled on its current src — an unsettled src
    //    means some replacement is still in flight (e.g. a remote pick),
    //    whoever started it, and revealing would paint the previous
    //    frame. The load/error listeners pass settled=true (the event IS
    //    the settlement signal, per spec complete is true by then);
    //    other callers rely on the complete flag (covers loaded AND
    //    failed).
    // Every release path funnels through here EXCEPT _render's empty
    // branch (the img is being cleared — nothing will ever settle).
    _releaseMask(settled) {
      if (!this._swapGen && !this._loadPending && (settled || this._img.complete)) {
        this.removeAttribute('data-swapping');
      }
    }

    // Baseline geometry, shared by clamp/apply/resize. `base` is the scale at
    // view-scale s=1: cover = fill the frame (overflow on the looser axis),
    // contain = fit fully inside (letterboxed). Zooming a contain image past
    // s where it overflows naturally becomes a crop. Null until the img has
    // loaded (naturalWidth is 0 before that) or when the slot has no layout
    // box — ResizeObserver fires with a 0×0 rect under display:none, and
    // clamping against a degenerate 1×1 frame would silently pull the stored
    // pan toward zero.
    _geom() {
      const iw = this._img.naturalWidth,
        ih = this._img.naturalHeight;
      const fw = this.clientWidth,
        fh = this.clientHeight;
      if (!iw || !ih || !fw || !fh) return null;
      const contain = (this.getAttribute('fit') || 'cover').toLowerCase() === 'contain';
      const base = contain ? Math.min(fw / iw, fh / ih) : Math.max(fw / iw, fh / ih);
      return {
        iw,
        ih,
        fw,
        fh,
        base
      };
    }
    _clampView() {
      // Pan range on each axis is half the overflow past the frame edge.
      const g = this._geom();
      if (!g) return;
      const mx = Math.max(0, (g.iw * g.base * this._view.s / g.fw - 1) * 50);
      const my = Math.max(0, (g.ih * g.base * this._view.s / g.fh - 1) * 50);
      this._view.x = Math.max(-mx, Math.min(mx, this._view.x));
      this._view.y = Math.max(-my, Math.min(my, this._view.y));
    }
    _applyView() {
      const g = this._geom();
      // Top-layer controls: pin to the frame's top-right in viewport px
      // (the same 8px inset as the in-frame layout; unscaled — top-layer UI
      // reads as chrome, not page content). BEFORE the geometry branch:
      // placement needs only the frame rect, and a not-yet-loaded or broken
      // src must not leave the promoted strip floating unpositioned. Gated
      // on the popover actually being open: without the Popover API,
      // showPopover() threw (swallowed in _enterReframe), .ctl stays in
      // its in-frame absolute layout, and viewport-px coordinates would
      // shove it off-frame — and matches(':popover-open') itself throws
      // there (unknown pseudo-class), hence the try/catch.
      if (this.hasAttribute('data-reframe')) {
        let onTop = false;
        try {
          onTop = this._ctl.matches(':popover-open');
        } catch {}
        if (onTop) {
          const r = this.getBoundingClientRect();
          this._ctl.style.left = r.right - 8 + 'px';
          this._ctl.style.top = r.top + 8 + 'px';
        }
      }
      if (!g) {
        // Dimensions not known yet (before img load) — centered fit so there
        // is no flash of an unpositioned image before the geometry lands.
        const contain = (this.getAttribute('fit') || 'cover').toLowerCase() === 'contain';
        this._img.style.width = '100%';
        this._img.style.height = '100%';
        this._img.style.left = '50%';
        this._img.style.top = '50%';
        this._img.style.objectFit = contain ? 'contain' : 'cover';
        return;
      }
      // Baseline (cover-fill or contain-fit) × view scale. Width/height and
      // left/top are all frame-% — depends only on the frame aspect ratio, so
      // a responsive resize keeps the same crop. The spill layer mirrors the
      // same box so its corners = image corners.
      const k = g.base * this._view.s;
      const w = g.iw * k / g.fw * 100 + '%';
      const h = g.ih * k / g.fh * 100 + '%';
      const l = 50 + this._view.x + '%';
      const t = 50 + this._view.y + '%';
      this._img.style.width = w;
      this._img.style.height = h;
      this._img.style.left = l;
      this._img.style.top = t;
      this._img.style.objectFit = '';
      if (this.hasAttribute('data-reframe')) {
        // Top-layer spill: position in viewport px over the frame. The top
        // layer escapes ancestor transforms entirely, so EVERY term must be
        // in viewport units: getBoundingClientRect gives the frame's scaled
        // origin AND size, and the rect/layout ratio rescales the ghost —
        // sizing from layout px alone renders it 1/scale too large under a
        // scaled deck slide. Inner ghost + handles stay box-relative.
        const r = this.getBoundingClientRect();
        const sx = g.fw ? r.width / g.fw : 1;
        const sy = g.fh ? r.height / g.fh : 1;
        this._spill.style.width = g.iw * k * sx + 'px';
        this._spill.style.height = g.ih * k * sy + 'px';
        this._spill.style.left = r.left + (50 + this._view.x) / 100 * r.width + 'px';
        this._spill.style.top = r.top + (50 + this._view.y) / 100 * r.height + 'px';
      }
    }
    _commitView() {
      const v = {
        s: this._view.s,
        x: this._view.x,
        y: this._view.y
      };
      if (this._userUrl) v.u = this._userUrl;
      // Framing-only (no u) persists too so an author-src slot remembers its
      // crop; clearing the sidecar still falls through to src=.
      if (this.id) setSlot(this.id, v);else {
        this._local = v;
      }
    }
    _render() {
      // Shape / mask. Presets use border-radius so the dashed ring can
      // follow the rounded outline; clip-path is only applied for an
      // explicit `mask` (the ring is hidden there since a rectangle
      // dashed border chopped by an arbitrary polygon looks broken).
      const mask = this.getAttribute('mask');
      const shape = (this.getAttribute('shape') || 'rounded').toLowerCase();
      let radius = '';
      if (shape === 'circle') radius = '50%';else if (shape === 'pill') radius = '9999px';else if (shape === 'rounded') {
        const n = parseFloat(this.getAttribute('radius'));
        radius = (Number.isFinite(n) ? n : 12) + 'px';
      }
      this._frame.style.borderRadius = mask ? '' : radius;
      this._frame.style.clipPath = mask || '';
      this._ring.style.borderRadius = mask ? '' : radius;
      this._ring.style.display = mask ? 'none' : '';

      // Controls and reframe entry gate on this so share links stay read-only.
      const editable = !!(window.omelette && window.omelette.writeFile);
      this.toggleAttribute('data-editable', editable);
      this._sub.style.display = editable ? '' : 'none';

      // Content. The sidecar is also writable by the agent's write_file
      // tool, so its value isn't guaranteed canvas-originated — only accept
      // data:image/ URLs from it. The `src` attribute is author-controlled
      // (Claude wrote it into the HTML) so it passes through unchanged.
      let stored = this.id ? getSlot(this.id) : this._local;
      if (stored && stored.u && !/^data:image\//i.test(stored.u)) stored = null;
      const srcAttr = this.getAttribute('src') || '';
      this._userUrl = stored && stored.u || null;
      const url = this._userUrl || srcAttr;
      // Don't clobber an in-flight reframe with a store-triggered re-render.
      if (!this.hasAttribute('data-reframe')) {
        this._view = {
          s: stored && Number.isFinite(stored.s) ? clampS(stored.s) : 1,
          x: stored && Number.isFinite(stored.x) ? stored.x : 0,
          y: stored && Number.isFinite(stored.y) ? stored.y : 0
        };
      }
      this._cap.textContent = this.getAttribute('placeholder') || 'Drop an image';
      // Toggle via style.display — the [hidden] attribute alone loses to
      // the display:flex / display:block rules in the stylesheet above.
      // An Unsplash src with no credit attribute must NOT render — showing
      // the photo uncredited is the Unsplash-terms violation itself. The
      // error tile replaces the photo until the credit is written. A
      // user-dropped image is the user's own content and always renders.
      // Trimmed: credit is agent/user-editable content, and a whitespace-
      // only value must count as missing — otherwise it would suppress the
      // error tile AND render an empty credit box (no text, no links),
      // exactly the unattributed state this gate exists to prevent.
      const credit = (this.getAttribute('credit') || '').trim();
      const attrError = !!(!credit && !this._userUrl && srcAttr && isUnsplashHost(srcAttr));
      this.toggleAttribute('data-attribution-error', attrError);
      if (url && !attrError) {
        const prev = this._img.getAttribute('src');
        if (prev !== url) {
          // Replacing an already-shown image: mark the swap BEFORE setting
          // src so the stale frame is never revealed (see the data-swapping
          // stylesheet rules). First fill (prev empty) keeps the existing
          // placeholder-until-load behavior — no spinner. _hidShowing
          // covers the pick path's transient attribution-error wipe: prev
          // is gone, but an image WAS showing, so this is a replacement.
          if (prev || this._hidShowing) this.setAttribute('data-swapping', '');
          // Mark the swap BEFORE assigning src: complete keeps reporting
          // the old settled request until the browser's
          // update-the-image-data microtask runs, so same-task re-renders
          // (the pick path's credit/credit-href setAttributes) need this
          // flag, not complete, to know a load is in flight.
          this._loadPending = true;
          this._img.src = url;
          this._ghost.src = url;
        } else {
          // Same-src re-render — release if settled, so an ingest-set
          // spinner can't stick after a byte-identical re-upload (same
          // data URL, no further load event ever fires).
          this._releaseMask();
        }
        this._hidShowing = false;
        this._img.style.display = 'block';
        this._empty.style.display = 'none';
        this.setAttribute('data-filled', '');
        this._clampView();
        this._applyView();
      } else {
        this.removeAttribute('data-swapping');
        // The src is being removed — no load/error will ever fire for it.
        this._loadPending = false;
        // A transient attribution-error wipe of a showing image happens on
        // the pick path: the host sets src one setAttribute before credit,
        // so render N hides the old image (attrError) and render N+1
        // restores a URL. Remember the wipe so that restore renders as a
        // replacement (spinner), not a first fill (blank frame).
        this._hidShowing = attrError && !!this._img.getAttribute('src');
        this._img.style.display = 'none';
        this._img.removeAttribute('src');
        this._ghost.removeAttribute('src');
        // The error tile owns the blocked-photo state; .empty stays for
        // the genuinely-empty slot.
        this._empty.style.display = attrError ? 'none' : 'flex';
        this.removeAttribute('data-filled');
      }

      // Credit belongs to the author src, so a user drop hides it.
      // textContent + the http(s)-only funnel keep external strings inert.
      const showCredit = !!(url && credit && !this._userUrl && !attrError);
      this._credit.textContent = '';
      if (showCredit) {
        // Validate once (resolved against the document, http(s) only),
        // then append the terms-required utm referral params to links
        // that point back at unsplash.com.
        let href = '';
        const rawHref = this.getAttribute('credit-href') || '';
        if (rawHref) {
          try {
            const u = new URL(rawHref, document.baseURI);
            if (u.protocol === 'http:' || u.protocol === 'https:') {
              href = withReferral(u.href);
            }
          } catch {}
        }
        const mkLink = (text, linkHref) => {
          const a = document.createElement('a');
          a.setAttribute('target', '_blank');
          a.setAttribute('rel', 'noopener noreferrer');
          a.setAttribute('href', linkHref);
          a.textContent = text;
          return a;
        };
        // Unsplash's prescribed credit is TWO links — the photographer's
        // name to their profile (credit-href) and 'Unsplash' to the
        // homepage. Render that split whenever the text has the canonical
        // shape; other text keeps the legacy single-link rendering.
        const m = /^Photo by (.+) on Unsplash$/.exec(credit);
        if (m) {
          this._credit.appendChild(document.createTextNode('Photo by '));
          this._credit.appendChild(href ? mkLink(m[1], href) : document.createTextNode(m[1]));
          this._credit.appendChild(document.createTextNode(' on '));
          this._credit.appendChild(mkLink('Unsplash', UNSPLASH_HOMEPAGE_HREF));
        } else if (href) {
          this._credit.appendChild(mkLink(credit, href));
        } else {
          this._credit.textContent = credit;
        }
      }
      this.toggleAttribute('data-credit', showCredit);
    }
  }
  if (!customElements.get('image-slot')) {
    customElements.define('image-slot', ImageSlot);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/image-slot.js", error: String((e && e.message) || e) }); }

if (__ds_scope.__ds_default_assets_icons_icon_data_imae2q$nab192 === undefined) __ds_scope.__ds_default_assets_icons_icon_data_imae2q$nab192 = __ds_scope.__ds_default_assets_icons_icon_data_imae2q;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.AspectRatio = __ds_scope.AspectRatio;

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.Card = __ds_scope.Card;

__ds_ns.InlineCode = __ds_scope.InlineCode;

__ds_ns.Palette = __ds_scope.Palette;

__ds_ns.Poster = __ds_scope.Poster;

__ds_ns.Progress = __ds_scope.Progress;

__ds_ns.ScrollArea = __ds_scope.ScrollArea;

__ds_ns.ScrollListItem = __ds_scope.ScrollListItem;

__ds_ns.Separator = __ds_scope.Separator;

__ds_ns.Slider = __ds_scope.Slider;

__ds_ns.TabCard = __ds_scope.TabCard;

__ds_ns.Table = __ds_scope.Table;

__ds_ns.TableItem = __ds_scope.TableItem;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.Input = __ds_scope.Input;

__ds_ns.Label = __ds_scope.Label;

__ds_ns.RadioButton = __ds_scope.RadioButton;

__ds_ns.RadioGroup = __ds_scope.RadioGroup;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.Textarea = __ds_scope.Textarea;

__ds_ns.Accordion = __ds_scope.Accordion;

__ds_ns.AccordionItem = __ds_scope.AccordionItem;

__ds_ns.Collapsible = __ds_scope.Collapsible;

__ds_ns.Command = __ds_scope.Command;

__ds_ns.ContextMenu = __ds_scope.ContextMenu;

__ds_ns.DropdownMenu = __ds_scope.DropdownMenu;

__ds_ns.MenuItem = __ds_scope.MenuItem;

__ds_ns.MenuSectionTitle = __ds_scope.MenuSectionTitle;

__ds_ns.Menubar = __ds_scope.Menubar;

__ds_ns.MenubarItem = __ds_scope.MenubarItem;

__ds_ns.NavigationMenu = __ds_scope.NavigationMenu;

__ds_ns.NavigationMenuContent = __ds_scope.NavigationMenuContent;

__ds_ns.NavigationMenuContentItem = __ds_scope.NavigationMenuContentItem;

__ds_ns.NavigationMenuItem = __ds_scope.NavigationMenuItem;

__ds_ns.SectionItems = __ds_scope.SectionItems;

__ds_ns.TabItem = __ds_scope.TabItem;

__ds_ns.Tabs = __ds_scope.Tabs;

__ds_ns.AlertDialog = __ds_scope.AlertDialog;

__ds_ns.AlterDialog = __ds_scope.AlterDialog;

__ds_ns.Dialog = __ds_scope.Dialog;

__ds_ns.HoverCard = __ds_scope.HoverCard;

__ds_ns.Popover = __ds_scope.Popover;

__ds_ns.Tooltip = __ds_scope.Tooltip;

})();
